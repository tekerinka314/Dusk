# CALENDAR-SPEC — календарь дедлайнов (Idea 5, гибрид: агенда → сетка)

Статус: спека Fable 2026-07-19. Приоритет НИЖЕ пушей (решение юзера 2026-07-19:
пуши = флагман П1; календарь = П2). Форма утверждена юзером: **гибрид** —
агенда-лента на мобиле (фаза 1, дешёвая, основная платформа), месячная сетка на
широком тире (фаза 2). Механика = Opus; готический дизайн-слой = Fable П2
(превью → вердикт → вживление).

## 1. Ценность и рамки

Календарь = четвёртое представление уже существующих данных (сортировка по
дедлайну, бейджи, дайджест уже есть) → строим ДЁШЕВО, без новых сущностей:
никакого редактирования из календаря в фазе 1 — только обзор + прыжок к задаче.
Тап по записи = переход на страницу задач + скролл + пульс (готовый
railDigestGo-паттерн).

## 2. Данные — одна чистая функция (vitest обязателен)

```ts
calendarItems(fromTs, toTs) -> [{ ts, task, kind: 'deadline' | 'recur' }]
```
- Обычные дедлайны: `task.deadline` в окне, не checked (архив не включаем).
- Повторы: проекция СЛЕДУЮЩИХ вхождений в окно из движка повторов
  (nextReset/shiftDeadline). ⚠ Урок B6-06: границы дня — локальные (`_ymd`),
  НИКАКОГО toISOString/UTC-математики.
- cycleChecked-повторы показываются на дате следующего возврата (kind 'recur').
- Сортировка по ts; функция чистая (state → массив), живёт рядом с движком
  повторов, тестируется на матрице повторов (daily/weekly/monthly/weektime ×
  границы месяца × выполненность).

## 3. Страница и навигация

- 4-я страница: `#calendar-page` в index.html + нав-таб (таб-ряд index.html:71+).
  switchPage расширяется значением `'calendar'`; body[data-page="calendar"].
- Модуль: **новый `dusk/13-calendar.ts`** по конвенциям 2a (globalThis-бриджи,
  функции топом, консты низом; side-effect import в src/main.js ПОСЛЕДНИМ —
  после 12, load-время ни от кого не зависит).
- ⚠ Глиф нав-таба — НОВАЯ иконочная роль (готовит Fable в П2-превью; до тех пор
  временно IC.window/B1-свеча как заглушка). Ратчет: любой новый глиф — в
  icon-registry тем же коммитом.

## 4. Фаза 1 — агенда («Часослов»), мобила и десктоп одинаково

Вертикальная лента ближайших **30 дней** + блок «Просрочено» сверху:

```
[ Просрочено (2) ]        ← красная рубрика, свёрнута если пусто
── Пн · 20 июля ──────────  ← день-рубрика: день недели + число
  18:30  Зажечь свечи      ⚫(цвет группы)  [статус-подсветка дедлайна]
  —      Отчёт кровью
── Ср · 22 июля ──────────
  ...
```

- Пустые дни ПРОПУСКАЮТСЯ (лента плотная); день «Сегодня» помечен.
- Ряд = кнопка (`data-act="calAgendaGo"` → jump-паттерн); справа countdown в
  статус-цвете (переиспользовать deadlineStatus/formatDeadlineCountdown и
  rd-* палитру дайджеста).
- recur-строки с уроборос-микро-значком (после fb4-вердикта — новый AZ).
- Живой тик как у дайджеста: текст/статус из updateDeadlineBadges-таймера,
  без ребилда DOM.
- Пустое состояние: готическая плашка «Грядущее пусто» (голос — как
  empty-state задач).
- Дизайн-язык (Fable П2 доведёт): день-рубрики как рубрики часослова —
  fleur-разделитель (BM1) + капитель; ничего из этого не блокирует механику Ф1.

## 5. Фаза 2 — месячная сетка (≥900px; на мобиле НЕ строится)

- Сетка 7×5-6, заголовок «Июль 2026» + стрелки-мечи (AU1-язык, как архив).
- Ячейка дня: число + до 3 меток-точек в статус-цветах (warn/urgent/over) +
  «+N». Сегодня — рамка-подсветка; чужой месяц — приглушён.
- Тап/клик по дню → поповер-агенда этого дня (движок B4-02 anchorPopover:
  flip/clamp/Esc-стек — готовый) со строками фазы 1.
- Никакого DnD-переноса дедлайнов в сетке (фаза 3+, отдельное «go»).
- ≥1440: календарь наследует широкий shell (1220), сетка слева + мини-агенда
  выбранного дня справа — опционально, решает П2-превью.

## 6. Фазы работ

- **Ф1**: calendarItems + vitest-матрица. Отдельный коммит.
- **Ф2**: страница+таб+switchPage+модуль 13, агенда целиком (мобила первой),
  jump, тик, пустое состояние. Смок в w4-стиле (пробы: агенда рендерится,
  прыжок работает, повторы проецируются).
- **Ф3**: сетка широкого тира + поповер дня.
- **Ф4 (Fable П2)**: готический слой — превью (рубрики-часослов, глиф таба,
  сетка-детали) → вердикт → вживление.

## 7. Риски

- Проекция повторов — единственная содержательная логика; матрица тестов
  обязательна ДО разметки (TDD).
- switchPage inline-display капкан (урок Ф3): календарь-страница получает свой
  #calendar-grid-ребёнок, switchPage владеет только display страницы.
- Не раздувать: фаза 1 без сетки, без редактирования, без drag — ценность
  обзора проверяется дёшево.
