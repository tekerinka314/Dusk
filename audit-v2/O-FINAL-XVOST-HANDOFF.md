# O-FINAL хвост — операционный хэндофф (2026-07-22, после развилок)

**Читать ПОСЛЕ `/clear`: этот файл + CLAUDE.md — самодостаточны для работы.**
Арт и таргеты по каждому пику — inline ниже; открывать нужно только конкретный
превью-файл (за SVG) и конкретный target-файл (за правкой). Память/OPUS-QUEUE/
plan/batch10 перечитывать НЕ требуется — всё нужное сведено сюда.

## Контекст в одну строку
Иконочная программа O-FINAL: Batch-1..10a задеплоены. AZ6-уроборос вживлён
сегодня (`8065ee1`). Остаток хвоста — ниже. Порядок по `remaining-scope-map`:
**иконки-хвост → потом W2** (кириллица квик-эдда первой).

## ⭐ ДВА ПРОЦЕССА (обязательны на КАЖДОМ вживлении)
1. **Ратчет:** после правки `node scripts/icon-inventory.mjs --update`, затем
   проставить `role` новым UNASSIGNED-хэшам в `audit-v2/icon-registry.json` ТЕМ ЖЕ
   коммитом (роль = буква вердикта). Тест `tests/icon-ratchet.test.js` = красный,
   если глиф не в реестре. Формат реестра: `JSON.stringify(d,null,1)+"\n"`.
2. **Замена = ВСЕ точки старого глифа** (правило юзера 2026-07-22, доказано на AZ6):
   не только билдер `IC.*`/`GIC.*` (он покрывает потребителей через `${IC.x}`), но и
   **инлайн-копии** старого svg + **тосты/модалки** с вкопированным svg. Метод:
   `grep` по ФРАГМЕНТАМ пути старого глифа (`d="M..."`), не только по имени константы.
   Пример-урок AZ6: старый уроборос жил ещё инлайном в кнопке «Еженедельно»
   `index.html` — билдер бы его не покрыл.

**Верификация каждого коммита:** `npm test` (37) + `npm run build` (оба зелёные) +
бамп `public/version.json` (BUILD `2026-07-22-N`) + коммит-пуш с `Co-Authored-By`.
Стейджить **поимённо**, НЕ `git add -A` (грабли: затягивает audit-PNG/tmp).
SVG байт-в-байт из превью; параметры подачи (px/stroke/opacity) — сверять с
соседями кластера, не копировать слепо (превью-обвязка ≠ приложение).

---

## РЕШЕНИЯ ЮЗЕРА 2026-07-22 (эта сессия) — свежие, нигде больше нет
- **focusLvl = FL2** («кодекс-разворот»): левая страница-список с буквицей / кромка
  со срезом стопки / одна страница. Арт: `audit-v2/previews/fb4-mini-glyphs.html`,
  ряд FL2 (генератор `gen-fb4-v3.mjs`). Цель: `GIC.focusLvl[0..2]`
  (`dusk/02-grimoire.ts` ~220-224; потребители 02:699/701/1122). Механика (готовый арт).
- **barLvl = BL2 «кованая шторка», НО С ПЕРЕРАБОТКОЙ** (творческий под-блок, спец ниже).
- **P3 gothEye = вживить two-state** (арт готов в `fb2-icons-preview.html`, кандидат P3:
  ВКЛ=полуприкрытое око / ВЫКЛ=открытое). Нужна ЛОГИКА свапа глифа по состоянию на
  ОБЕ кнопки: тулбар «только невыполненные» (`dusk/04-tasks.ts:~109`) И
  subNotesAlwaysOpen (`dusk/05-edit-notes-groups.ts:~271`). Обе сейчас = `IC.gothEye`
  (01:~478, общий constant) → нужен не class-active, а подмена самого svg по состоянию.
- **ОТЛОЖЕНЫ (отдельный творческий блок позже, НЕ в этом заходе):** архив-крипта
  де-алиасинг (`#icon-archive`), N-сигилы уведомлений (`_qaRenderMenu`),
  X1 арки-времени дедлайн-модалки (завязан на N3).

## ПЕРЕОПРЕДЕЛЕНИЯ старых доков (реверсы юзера, ВАЖНО — не спутать)
- **AX → РЕДИЗАЙН AX1** (реверс 07-19). Старое «AX0 keep-current / Tier-A drag-цепь»
  в plan/OPUS-QUEUE **ОТМЕНЕНО**. Проверено: `IC.drag` (01:396, viewBox `16 20`) —
  ещё старый глиф. Арт AX1 готов: `fb3-icons-preview.html` data-pick около :304.
- **noneMoonL/R → C1** — УЖЕ сделан (10a-fix `40dcc8a`). НЕ в активном списке.
- **AA1 нав-табы** — сделаны (Batch-7). НЕ в активном списке.

---

## АКТИВНЫЙ СПИСОК ВЖИВЛЕНИЯ (порядок: механика → P3 → barLvl → потом W2)

> **СТАТУС 2026-07-24: БЛОК A ЗАКРЫТ ЦЕЛИКОМ.** Всё запушено и ОТСМОТРЕНО ЮЗЕРОМ
> на проде.
> ✅ Билды -2…-6 (2026-07-22, вердикт «все иконки прошли ревью»): **AX1**
> (IC.drag) · **FL2** (GIC.focusLvl[0..2]) · **BB1**+**BC1** ·
> **BH1·BR1·BE1·BQ1·BJ1·BF1** (статика index.html) · **BG1**+**BK1**
> (03/07 сплит-заголовки + 02/inline тома fold).
> ✅ Билд `2026-07-24-1` `73ca94b` — **BD1** столярный гроб (кайма + гвозди +
> крест pattée) в `coffinSVG`; арт авторен на канве 21×27 → в билдере живёт как
> `k = w/21`, при w=21 совпадает с превью координата в координату. Столярка
> продублирована в `cycleCoffinSVG` (его комментарий обещает «identical coffin
> body»), метка там осталась своим return-cross, клинья только на свободных
> концах планок. Отсмотрено юзером.
> ✅ Билд `2026-07-24-2` `fb8447b` — **BI1** (степперы dl-dur ×4 + углы ×4) +
> **BM1** флёр-де-лис (`GIC.dividerFleur`). Грабля: разведка 07-18 по BM врала
> про «CSS-дубли style.css:371/8134» — 371 это ДРУГОЙ орнамент (полоса 160×14
> под шапкой: линии + ромбы + кольцо), 8134 = blockquote. У dividerFleur одна
> точка. Полоса под шапкой мотивом «ромб+бусины» теперь расходится с флёр-де-лисом
> — по слову юзера СИНХРОНИЗИРОВАНА: билд `2026-07-24-3` `f59c01a`, полоса
> переведена на тот же BM1 (`.app-header::after`, арт 40×12 → канва 160×14
> сдвигом +60,+1, альфы запечены). Ждёт прод-ревью.
> ✅ Билд `2026-07-24-4` `63ccdff` — **блок B закрыт: P3 two-state**. `IC.gothEye`
> распался на `gothEyeHalf` (ВКЛ) + `gothEyeOpen` (ВЫКЛ), свап через единый
> `eyeGlyph(on)` (01-core, сразу после `IC`). Грабля разведки: тулбар-фильтр НЕ в
> `04-tasks.ts:109` (там кнопка sub-notes) — он **инлайн-svg `index.html:504`**,
> `#btn-filter`, стейт `isFiltered`. Точки свапа: `01:loadUiState` (восстановление),
> `07:toggleFilter` (клик), `04:109` (рендер меты), `04:1988` (живой тогл),
> `05:272` (ленивое создание кнопки). Ратчет: 189 глифов, оба новых = роль `P3`.
> Ждёт прод-ревью.
> ✅ Билд `2026-07-24-6` `62058fb` — **блок C закрыт: BL2 вживлён** в `GIC.barLvl`
> (02-grimoire, тумблер тулбара Гримуара; потребители не трогались — оба идут через
> `GIC.barLvl[grimBarMode]`, инлайн-копий старого свитка нет). Рерабока `7ebb87e`
> принята юзером: пунктир auto → сплошная, засов у open убран, кольцо у closed
> убрано + пики решётки доведены до нижней кромки планки (17.6→18.9), содержимое
> планки переработано на собственные мотивы DUSK (стрельчатая арка с окулюсом ·
> крест pattée идиомой BD1 · флёр-де-лис идиомой BM1). В генератор `gen-fb4-v3.mjs`
> добавлен `tidy()` — режет флоат-хвосты `6.9799999999999995` до 2 знаков, поэтому
> превью и приложение совпадают символ в символ. Ратчет: 189 глифов, роль `BL2`.
> Ждёт прод-ревью.
>
> **🎯 ИКОНОЧНЫЙ ХВОСТ ИСЧЕРПАН** (кроме явно отложенного творческого блока ниже).
> Следующее по `remaining-scope-map` — **W2, кириллица квик-эдда ПЕРВОЙ**.

### Блок A — механика, byte-for-byte (арт готов)
Каждый пик: grep таргет → сверить, что ещё старый → заменить ВСЕ точки → ратчет.

- ~~**AX1** цепь DnD → `IC.drag`~~ ✅ сделано.
- ~~**focusLvl FL2** → `GIC.focusLvl[0..2]`~~ ✅ сделано.
- **10b пики** (`fb3-icons-preview.html`, строки data-pick указаны; таргеты разведаны):
  · ~~**BB** ключ гор.клавиш~~ ✅ · ~~**BC** рептилоид-глаз синка~~ ✅ (стейт-классы целы).
  · ~~**BD** coffinSVG~~ ✅ (`73ca94b`, + `cycleCoffinSVG`; масштаб `k = w/21`).
  · ~~**BE** coffin-emblem `<symbol>`~~ ✅ (shared-градиенты сохранены, inline defs убраны).
  · ~~**BF** be-fil ×2~~ ✅ · ~~**BG** сплит-заголовки~~ ✅ (`.split-chevron` не тронут).
  · ~~**BH** «Свой цвет-спектр»~~ ✅ · ~~**BJ** grim-io ×2~~ ✅ · ~~**BK** тома fold~~ ✅
    (билдер 02 + инлайн `gt-open`/`gt-closed`) · ~~**BQ** grim-seg гроб ×2~~ ✅ ·
    ~~**BR** «Опустошить»~~ ✅.
  · ~~**BI** dl-dur стрелки ×4 + углы ×4~~ ✅ (`fb8447b`; фактич. строки были
    1289/1293/1307/1311 и 1244-1247, а не как в разведке).
  · ~~**BM** dividerFleur~~ ✅ (`fb8447b`; CSS-дублей не существует, точка одна).

  ⚠ Все строки-цитаты выше — из разведки 07-18; **перед каждой правкой grep'ом
  подтвердить актуальную строку** (файлы менялись после: AZ6, drawer и др.).

### Блок B — P3 two-state (полумеханика)
`IC.gothEye`: сделать ПАРУ глифов (ВКЛ полуприкрытое / ВЫКЛ открытое) из `fb2` P3-кандидата
+ логику подмены svg по состоянию на обе кнопки (04:~109 тулбар-фильтр, 05:~271 subNotes).
Не class-active — реальная замена глифа.

### Блок C — barLvl BL2 РЕРАБОТКА (творческий, ревью-цикл)
Правки через генератор `audit-v2/previews/gen-fb4-v3.mjs` (он переписывает
`fb4-mini-glyphs.html` целиком). **⚠ НЕ создавать новый превью-файл** (юзер
2026-07-22, экономия токенов) — работать в СУЩЕСТВУЮЩЕМ `fb4-mini-glyphs.html`.
**4 правки юзера к BL2:**
1. Содержимое в прямоугольнике на состояниях **auto** и **open** (3 фигуры) не
   тянет на макс. готику/детализацию/интуитивность → **переработать содержимое**.
2. **auto:** половина решётки идёт пунктиром → сделать **сплошной линией**.
3. **open:** справа от верхней линии (шторки) «клякса» (засов-штырь) → **убрать**.
4. **closed:** убрать «кольцо-ручку» + продвинуть пики решётки **чуть дальше вниз**
   до её нижней границы.
После рерабоки — **юзер смотрит превью, потом вживление** в `GIC.barLvl.{auto,open,closed}`
(02:~228-232, тулбар-тогл Гримуара). Урок темпа ПРИМЕНИМ здесь (вкус/рисование):
правка генератора → одна проба-рендер → показ, без перебора на глаз.

### Отложенный творческий блок — ⭐ ГРИНЛАЙТ ЮЗЕРА 2026-07-24, можно закрывать
archive-крипта де-алиасинг `#icon-archive`; N-сигилы `_qaRenderMenu` (08); X1 (N3-зависим).
Плюс **новый баг того же сообщения (Гримуар, визуальный):** кнопка **TOC заходит
на границы тулбара**, и **тогл тулбара тоже на грани** — сперва systematic-debugging,
это верстка, а не иконка.
Плюс возможный «CSS-доп-проход» (native-select стрелки→AU+appearance:none, ☰ sync-log
`style.css:8516`, `.group-select` generic-chevron 789, CSS-дубли) — **проверить,
не сделан ли уже** Batch-9; если нет — сюда же отдельным проходом.

## После иконок → **W2** (FIX-PLAN, 9 багов, **кириллица квик-эдда ПЕРВОЙ**:
`!`/`*`/`%` не ловят русские слова = реальный баг). Дальше — `remaining-scope-map`.
