# OPUS-QUEUE — передача от Fable (обновлено 2026-07-17 после пересборки плана)

Ритуал и запреты — OPUS-BRIEF.md. Ф1/Ф2 закрыты: запрет на style.css/03/04/05/
index.html СНЯТ, но правки — хирургические, готика неприкосновенна (CLAUDE.md).
Вердикт Fable: всё ниже Opus тянет без потери качества — это механика по
готовому диагнозу/плану. Дизайн-развилки НЕ решать: в REVIEW-QUEUE.md.

**ПОРЯДОК (STRATEGY.md, утверждён юзером 2026-07-17).**
Сейчас (окна кулдауна Fable, до 19.07): **O-4 TWA → O-1 (ТОЛЬКО замер,
п.1) → seq-сиды idbboot + скелет Этапа 5**. После 19.07: W2 целиком
(FIX-PLAN) → O-1 фиксы (п.2-5) → O-5 → W3 cut-line → синк-стресс → Этап 5.
O-2 У OPUS'А ИЗЪЯТ — поповер-баги на девайсе дебажит Fable (Ф-А),
движок его.

## O-4 — Android TWA (Ф4) — ПЕРВЫЙ, файлово изолирован

По `ANDROID-PRENOTES.md` (binding): **TWA-first**, Bubblewrap; НЕ WebView-обёртка
(OAuth-капкан: GIS в WebView запрещён Google — TWA использует Custom Tabs,
поэтому синк работает). Прод-origin `https://dusk-du4.pages.dev`.
- Новая папка (напр. `android-twa/`), НИЧЕГО в веб-коде не менять; если
  manifest.json требует правок (id/scope/иконки maskable) — минимальный дифф.
- `twa-manifest.json`: **navigationBarColor / navigationBarColorDark = #150933**
  (настоящий фикс системной полосы — см. §Safe-area), themeColor #0a0420,
  display standalone, orientation default.
- assetlinks: юзер-шаги (SHA-256 подписи → `public/.well-known/assetlinks.json`)
  расписать пошагово, юзер — новичок в инфре.
- Выход: сборочный скрипт + инструкция сайдлоада APK; подписанный APK юзеру.

## O-5 — B11+B12 экспресс-аудит + adversarial-линзы (ПОСЛЕ 19.07, слито в одно)

Adversarial-воркфлоу (5 линз sonnet-5 medium + судья opus-4.8 high, план F2 §4)
НЕ отдельная задача — это метод прохода B11+B12 по новым поверхностям
(F2-мобайл, Ф3 рельса/поповеры/хинты, иконки Ф-B, drawer Ф-C). Находки →
FINDINGS.md реестр, вкусовое → REVIEW-QUEUE.md.

## O-1 — Мотион, JS-фаза (по audit-v2/MOTION-PLAN.md «Отложено»)

Порядок обязателен: СНАЧАЛА замер, потом фиксы (не чинить вслепую).

1. **Замер:** web-perf скилл / Chrome DevTools trace, CPU 4x throttle +
   coarse-эмуляция; сценарии: скролл списка 30+ задач, открытие/закрытие
   групп и подзадач, DnD, гримуар collapse. Зафиксировать в
   audit-v2/MOTION-TRACE.md какие фазы кадра переполняются (layout? paint?).
2. **Reflow-батчинг:** dusk/02-grimoire.ts (22 чтения scrollHeight/offset*),
   dusk/07-dnd-filter-progress.ts (13): собрать чтения до записей (батчи
   read→write), убрать чтения из циклов. Поведение 1:1, только порядок.
3. **content-visibility: auto** для месяцев архива (contain-intrinsic-size
   прикинуть по средней высоте; проверить скролл-якоря и поиск).
4. **FLIP для схлопывания щели при уходе ряда** — ТОЛЬКО если trace п.1
   показал, что taskArchive/taskDelete (max-height collapse) реально роняет
   кадры. Иначе не трогать.
5. **Жертвы W3-свипа:** 58 «transition: all» заменены на явный список
   (color/background-color/border-color/box-shadow/opacity/transform).
   Пройти hover/active-состояния визуально; где раньше через `all`
   анимировалось иное свойство (width/letter-spacing/filter) и стало
   мгновенным — вернуть точечной строкой transition.
6. Юзер-репорт: «не 120hz, но терпимо» — цель фазы: устранить оставшиеся
   провалы кадров, найденные замером, НЕ переделывать дизайн мотиона.

Верификация: vitest + build зелёные; trace до/после в MOTION-TRACE.md;
коммит+пуш+бамп version.json.

## O-2 — Мобильные баги адаптации (итеративно, протыкать в браузере)

Метод: Playwright-харнесс (D:\tmp\pw, playwright-core + системный Chrome),
вьюпорты 360×800 и 390×844, coarse-эмуляция, РЕАЛЬНЫЕ клики по каждому
поповеру/дропдауну/шиту. Скрин до/после на каждый фикс.

Известные (от юзера, на девайсе):
1. **Дропдаун видов сортировки открывается влево и уходит за экран.**
   Паттерн решения уже в кодовой базе: body-portal поповеров + clamp в
   viewport (см. memory-урок «поповеры только body-portal»). Не изобретать
   новый механизм — распространить существующий.
2. Пройти ВСЕ поповеры/дропдауны на обоих вьюпортах: выход за экран,
   позиционирование от якоря, закрытие тапом-мимо, перекрытие клавиатурой.
   Найденное — списком в этот файл, чинить по одному.

Связь с Ф3: системный поповер-движок B4-02 будет спроектирован Fable позже —
здесь только точечные фиксы за-экрана, без переархитектуры.

## O-3 — Тач-интуитивность — ✅ ЗАКРЫТО FABLE ЦЕЛИКОМ (2026-07-17, `f19d646`)

Юзер одобрил long-press; Fable сам вживил механику (08, спека ниже выполнена
1:1) + рантайм-аудит лейблов: 183 видимых кнопки, 0 без title/aria-label/текста
— TOUCH-HINTS-INVENTORY.md НЕ НУЖЕН, долг закрыт прежними аудитами. Пруф lp
5/5. Opus'у здесь делать НЕЧЕГО. Также B4-02 ядро+float+sort сделаны Fable
(`6f32654`, `d8a9569`; см. POPOVER-ENGINE-SPEC статус) — из O-2 остаётся
только пройтись по остальным поповерам визуально: сорт-дропдаун теперь
клампится и следует (вероятно чинит баг «уходит влево за экран» — ПЕРЕПРОВЕРИТЬ
на девайсе первым делом).

## ~~O-3 (историческая спека дизайна)~~

Проблема (юзер): на ПК тултипы объясняют кнопки, на телефоне подсказок нет.

**Решение Fable: long-press хинт (Material-идиома, ждёт вердикта юзера в
REVIEW-QUEUE §Ф3 — если юзер выберет иначе, дизайн пересматривает Fable).**
Отвергнуто: режим «?» (новый FAB-хром, оверлей-каша), coach-marks (one-shot,
не помогает вспоминанию, анти-аскетика).

Спека механики:
1. Делегированный `pointerdown` (coarse pointer only) на элементах с
   `title`/`aria-label`; таймер **480мс** удержания без движения (>8px slop —
   отмена, это скролл/драг).
2. По срабатыванию — хинт-плашка у кнопки: текст = aria-label/title,
   `--font-label`, поверхность `--bg-modal` + `--border-mid`, появление
   `--dur-quick`/`--ease-emerge`, позиция над кнопкой (clamp в вьюпорт;
   после B4-02 — через anchorPopover, до него — простой fixed-замер).
3. Отпускание ПОСЛЕ показа хинта — клик ГАСИТСЯ (палец уже «спросил», не
   «нажал»); хинт держится ещё ~1.2с и тает. Отпускание до 480мс — обычный тап.
4. Конфликты: не вешать на драг-хэндлы (Sortable long-press) и на armed-кнопки
   двухшагового подтверждения (у них hold-семантики нет, но armed-пульс свой) —
   исключить селектором.
5. `prefers-reduced-motion` — без анимации, мгновенный показ/скрытие.
6. Вход: реестр кнопок TOUCH-HINTS-INVENTORY.md (уже в
   audit-v2/shots/rework/… очереди) — кнопки БЕЗ title/aria-label получают
   label в рамках этой же задачи (долг закрывается тут).

## Safe-area (закрыто, не трогать)

Нижняя полоса: html::after красит inset при edge-to-edge; если девайс/режим
рисует полосу системно (3-кнопочная навигация, запечённый WebAPK) — из веба
недостижимо. НЕ тратить время. Настоящий фикс = Ф4 TWA: navigationBarColor
задаётся явно в twa-manifest.

## O-6 — ИКОНКИ: вживление партии №1 (вердикт юзера получен 2026-07-18)

Юзер выбрал в `audit-v2/previews/fb1-icons-preview.html`:
**A2 · B1 · C1 · D1 · E2 · F2**. SVG-исходники — в превью-файле, в карточке
`.cand[data-pick="…"]` (первый svg внутри .sz-48; все три размера одного
кандидата идентичны по коду). Вживить КАК ЕСТЬ, пути не «оптимизировать»,
не менять stroke-width/opacity — это выверенный рисунок.

| Пик | Роль | Куда |
|---|---|---|
| A2 (кованый крест) | «Добавить задачу» | index.html `#btn-add` (~строка 117); проверить и `#btn-add-group`-плюсы, если тот же генерик-крест — заменить тем же A2 |
| B1 (свеча в окне) | «Дедлайн» карточки | `IC.window` в dusk/01-core.ts (~286) — заменяет ВСЕ использования (карточка, tpl-биты) |
| C1 (гравированный серп) | полумесяц «пусто» | `IC.moon` в 01-core.ts (~513); НЕ трогать `IC.noneMoonL/R` (это пара для селекта) без отдельного вердикта |
| D1 (новолуние) | Повтор «Нет» | index.html повтор-модалка, кнопка `data-repeat="none"` (~1310-1329) — svg целиком; также repeat-btn "Нет" в дедлайн-модалке, если у неё есть иконная версия |
| E2 (солнце меж шпилей) | «Только на сегодня» | index.html `#btn-today` (~416) |
| F2 (пара рун) | «Выбрать несколько» | index.html `#btn-main-select` (~475) |

Правила: viewBox 0 0 24 24, stroke=currentColor, никаких fixed-цветов;
размер задаёт CSS кнопки — width/height в svg не добавлять. После вживления:
`npm test` (35) + `npm run build` + бамп version.json + коммит-пуш. Глазами:
16px-читаемость в тулбаре/карточке (эмуляция small), ничего не обрезано.
Дальше Fable выдаёт партии №2+ (весь эстейт) — вживлять по тому же ритуалу
после каждого вердикта юзера.

## O-6b — хинт-баг, механизм №4: осиротевший линжер-таймер (диагноз Fable готов, вживить фикс)

Юзер-репро на билде -15: первая удержанная кнопка — хинт держится нормально;
удержание ВТОРОЙ кнопки вскоре после — её хинт гаснет через ~0.5с.
**Корень:** в `dusk/08-quickadd-export-init.ts` `_lpUp` и `pointercancel`
ставят `setTimeout(() => _lpHide(), LP_LINGER_MS)` (1200мс) БЕЗ сохранения
хэндла; `_lpShow`/`pointerdown` его не отменяют. Холд №2 начат в пределах
1.2с после отпускания №1 → старый таймер стреляет посреди нового удержания
и убивает свежую плашку (0.5с после появления = 1200 − время до второго
холда − 480 холда). Механизмы №1-3 (slop / синк-рендер / contextmenu) уже
закрыты билдами -13/-14/-15 — их не трогать.
**Фикс:** `globalThis._lpLingerT = 0;` хранить хэндл
(`_lpLingerT = setTimeout(...)` в обоих местах), и `clearTimeout(_lpLingerT)`
в `_lpShow()` И в pointerdown-хендлере (рядом с `_lpHide(true)`).
**Пруф:** в `D:\tmp\pw\b1\fa_bugs.mjs` добавить тест: hold→release→через
300мс hold второй кнопки 650мс → хинт видим на 600-й мс второго холда
(сейчас FAIL). Прогнать весь fa_bugs (было 15/15) + vitest + build + бамп
version.json + коммит-пуш.

## ⚠ ПОРЯДОК ОКОН OPUS (строгий, утв. 2026-07-18; «СТОП» = конец окна)

1. **Окно №1:** §O-6b (хинт-фикс, быстрый) → §O-4 TWA целиком. СТОП.
2. **Окно №2:** W2 по FIX-PLAN. СТОП.
3. **Окно №3 (ТОЛЬКО после Л5 Fable — все вердикты собраны):** §O-FINAL.
4. **Окно №4 (если осталось):** O-1 фиксы п.2-5 → O-5.

## O-FINAL — registry + вживление ВСЕХ иконок ОДНИМ заходом (после Л5)

Решение юзера 2026-07-18: партии НЕ вживлять по одной. §O-6 (вживление
партии №1) ОТЛОЖЕН — войдёт сюда же.

### МЕГА-БРИФ (дописан Fable в сессии правок-2, 2026-07-18)

**Источник истины:** `audit-v2/previews/fb1-icons-preview.html` (роли A–F) и
`fb2-icons-preview.html` (G–AT, включая правки-2). SVG брать ИЗ превью по
`data-pick` выбранного кандидата, **байт-в-байт** — пути не оптимизировать,
opacity/stroke-width вторичных линий не «чистить». Стартовый гейт: **финальная
строка вердикта от юзера** (после его проверки правок-2); всё ниже — карта
«роль → куда вживлять». Если в финальной строке пик отличается — строка юзера
верховна.

**Вердикт ФИНАЛЬНЫЙ (юзер, 2026-07-18, правки-3 ПРИНЯТЫ):** A2·B1·C1·D1·E2·F2
(fb1) + G1·H2·I2·J2·K1·L1·M3·N3·O2·P3·Q1·R2·S2·T1·U2·V1·W1·X1·Y1·Z2·AA1·AB1·
AC1·AD1·AF1·AI1·AN1·AO1·AP1·AQ2·AR1·AS1·AT1. Брать ПОСЛЕДНЮЮ версию каждого
глифа из превью (правки-3 вжиты в файл поверх правок-2).
**P3 = ПАРА глифов:** фильтр ВКЛ — полуприкрытое око, ВЫКЛ — открытое; кнопка
меняет глиф по состоянию (не только класс active). Оба ряда в P3-кандидате.

**⚠ ГЕЙТ РАСШИРЕН (юзер 2026-07-18): O-FINAL ждёт ещё Ю6** — тотальный проход
Fable по ВСЕМ оставшимся иконкам (инвертированная планка: редизайн по
умолчанию, см. REVIEW-QUEUE/память). Вживление — одним заходом ПОСЛЕ вердикта
по Ю6, эстейт вырастет.

**⚠ ПРАВИЛО КОНСИСТЕНТНОСТИ ПРИ ВЖИВЛЕНИИ (юзер 2026-07-18):** SVG-геометрию
глифа копировать байт-в-байт, но ПАРАМЕТРЫ ПОДАЧИ (размер в px, stroke-width
контекста, яркость/opacity, оптический вес рядом с соседями) НЕ копировать
слепо из превью — сверять с соседними иконками того же ряда/кластера и
приводить к их визуальному весу (новый глиф не должен быть ярче/крупнее
соседей). Превью-обвязка ≠ приложение.

**Порядок захода (B3-02, registry-first):**
1. Консолидация: один глиф на роль — builder-фны IC/GIC, index.html-статики
   переводятся на symbols/builders, убрать ~14 copy-paste дублей.
2. Замена по карте:
   - A→`IC.crossAdd/crossSm` (01:447/430) + шапка index.html · B→`IC.window`
     (01:286) · C→`IC.moon` (01:513) · D→модалка повтора «Нет» (index.html) ·
     E→today-тулбар (index.html) · F→кластер ВЫБОР (index.html).
   - G→`IC.addNote/editNote` (01:426/428) · H→`taskColorGlyph` (04:2265) ·
     I→`IC.pin` (01:526) · J→`IC.dagger` (01:327) · K→`IC.selectEmpty/Checked`
     (01:549/555) · L→`IC.focusMode` (01:319) · M→`IC.sortPriority/sortOrder/
     sortAlpha` (01:299/306/313; кнопка сортировки группы циклит их же) ·
     N→`_qaRenderMenu` (08:166) — глифов там НЕТ, добавить сигилы в строки.
   - O→`IC.soundOn` (01:405); `IC.soundOff` (01:413) = O2-глиф, приглушение
     + X-перечёрк — ряд «выключен» уже нарисован в превью O2 · P→`IC.gothEye`
     (01:478) **+ перенести кнопку фильтра: сейчас после тоггла подпунктов,
     должна стоять после кнопки заметок** · Q/R/S/T/U/X/Y/AA→index.html-статики
     + `_TOOLS_EXPORT/IMPORT/BACKUP_IC`, `_EXPORT_TASKS/MD_IC` (08:260-282),
     `IC.search` (01:377), `IC.template` (01:572) · V→модалка повтора +
     `IC.repeatMonthly` (01:384), бейджи повтора ← V-язык · W→`IC.snooze`
     (01:564) + пресеты снуз-меню · Z→`IC.tombstone` (01:434).
   - AB→`IC.restore` (01:369) · AC→`grimEmptyCrypt` и пустые состояния ·
     AD→`IC.quill` (01:280) · AF→GIC toc-скоба · AI→FIC-ряд (02:3146+) ·
     AN→`IC.twinCoffin` (01:506) · AO→`IC.g4/g4any/g4all` (01:252) ·
     AP→`IC.promote/demote` (01:579/585) · AQ→`IC.more` (01:240) ·
     AR→bulk-бар (index.html).
   - **AS→`IC.crossedSwords` (01:342)** — 7 точек: 04:87/93/543/688,
     03:1449, 02:1600/1692 (все берут новый глиф автоматически через IC).
   - **AT→`IC.skull` (01:270)** — все danger-точки через IC; ВНИМАНИЕ:
     старый viewBox `0 0 20 22`, новый AT1 нарисован в `0 0 24 24` —
     заменять ВЕСЬ svg включая viewBox (размеры задаёт CSS, не viewBox).
3. Derive (не рисовать, произвести): `noneMoonL/R` ← C1 зеркало ·
   `deadlineClear` ← B1 + перечёрк · сепараторы «Без дедлайна» ← C1,
   «Закреплённые» ← I; «С дедлайном» = sundial (Tier-A, не трогать) ·
   склеп-чекбоксы `runeCircle/Checked` (01:353/361) ← K · цвет-фильтр-кристалл
   ← H · грим-песочные меты ← T-язык · bulk-глифы ← U/T/H/K/AB ·
   **`IC.archive` (01:278): U2-крипта встаёт ВО ВСЕ точки** — «Архивировать
   всё» (08:292), per-task «В архив» (04:186), ⋯-шиты (04:2308) — дыра
   покрытия найдена в правках-2, B13: хоронить, не складировать.
4. Верификация: vitest + build + 16px-гейт глазами (эмуляция small экрана,
   пройтись по карточке/тулбару/модалкам/Гримуару/склепу) + бамп
   version.json + коммит-пуш.

Tier-A подтверждён вторым проходом правок-2 (НЕ трогать): sundial, drag-цепь,
spires-карточки, pinSpike, sword-шеврон, chronicle, coffin-emblem, свечи
дедлайн-модалки, рептилоид-глаз, гроб-чекбоксы карточек, penSound, ключ
«Горячие клавиши», «Сосуд», GIC.foldOpen/Closed, dismiss, tocClose,
focusLvl/barLvl. FIC — Tier-C: вес/акценты по AI1, НЕ гравировать.

## O-FINAL-Ю7-ADDENDUM v3 — coverage-сверка ШИРЕ + финальная строка + 4 keep-current + AI1-фолд (Ю7, 2026-07-18, фикс-3: сузил инвентарь в v2, юзер поймал)

**ПРОМПТ ДЛЯ НОВОГО ОКНА OPUS — вставить целиком:**

> Прочти по порядку: `CLAUDE.md` → память `plan-2026-07-17-final-limits`
> (ред.9) → `audit-v2/OPUS-QUEUE-2026-07-17.md` целиком, особое внимание —
> секции `O-FINAL` (мега-бриф, derive-карта, Tier-A список) и вот эта секция
> `O-FINAL-Ю7-ADDENDUM v3`. Задача — закрыть иконочную программу целиком и
> перейти к остальному по STRATEGY.md (после этого окна — TWA → W2 → ...).
> Иконки НЕ рисовать — вся творческая часть закрыта, только код+проверка.
>
> **Скилл-цикл ОБЯЗАТЕЛЕН (CLAUDE.md §Skill usage, память
> skill-cycle-mandatory — железобетонно, не пропускать формально):**
> просканируй ВСЕ установленные скиллы, загрузи ВСЕ, что реально ценны для
> этой задачи (не одну «самую подходящую» — если сомневаешься, грузи), И
> отдельно вызови `find-skills` под конкретно эту задачу (coverage-сверка
> иконок + registry-консолидация + TWA/W2 — это разные классы работы, ищи
> под каждый, если не искал в этой сессии буквально этот же класс). В конце
> явно перечисли, что загрузил, что нашёл через find-skills, что осознанно
> пропустил и почему.
>
> **Обсуждай развилки с юзером, не реши сам.** Если по ходу встретишь
> развилку/неоднозначность/идею/предложение по плану — ЛЮБОЕ место, где
> разумны 2+ трактовки (не только новые находки coverage-сверки из п.4
> ниже, но и архитектурные решения TWA/W2, порядок работ, спорные мелочи
> вживления) — остановись и спроси юзера явно, не додумывай и не продолжай
> молча на своё усмотрение. Мелкие технические детали без развилки решай
> сам как обычно.
>
> **Ресурс-констрейнт: помни, что ЗАВТРА один лимит Fable в любом случае
> идёт не на это** (Ф-C drawer-nav или что юзер решит — иконочная программа
> Fable закрыта, но у Fable остаётся минимум 1 запланированный лимит вне
> этого окна Opus). Это НЕ блокирует и НЕ ускоряет твою работу — Opus и
> Fable идут последовательно, не параллельно (STRATEGY.md) — но учитывай
> это при планировании порядка и не соглашайся негласно «занять» тот лимит
> под задачи из этого окна; если что-то из твоей работы естественно
> пересекается с зоной Fable (дизайн/вкус) — вынеси в REVIEW-QUEUE/спроси
> юзера, а не реши сам.
>
> **Шаг 0 — coverage-сверка (сделай ПЕРВЫМ, до вживления), ШИРЕ чем просто
> IC/GIC/FIC.** Два урока этой сессии (оба поймал юзер, не я сам):
> (a) роль без пика юзера в fb3 ≠ роль без прошлого решения — часть таких
> ролей уже решена под ДРУГИМ именем в fb1/fb2 (текстовая улика: derive-карта,
> Tier-A confirmed-список, пометки «язык X»/«рифма X» в превью);
> (b) v1/v2 этого аддендума ограничивали инвентарь только тремя
> namespace'ами (`IC.*`/`GIC.*`/`FIC.*`) + index.html — но B3-icons.md
> census (2026-07-11) может быть устаревшим: мобильный F1/F2-ревворк после
> него мог добавить новые глифы, которых census не видел. Поэтому:
> 1. Стандартный проход: пройди по каждому builder-конструктору (`IC.*`
>    dusk/01-core.ts:221+, `GIC.*` dusk/02-grimoire.ts:177+, `FIC.*`
>    dusk/02-grimoire.ts:3146+) и по всем уникальным inline-SVG в index.html
>    — для КАЖДОГО grep'ом найди ВСЕ места использования (не только одно) и
>    сверь с derive-картой: ровно одна роль (A-BU) ИЛИ явная derive-запись.
>    Конкретный найденный кейс: `IC.gothEye` (01:478) — ДВА потребителя,
>    тулбар «только невыполненные» (роль P) И кнопка «Показать/скрыть все
>    заметки подпунктов» (dusk/04-tasks.ts:109 + dusk/05-edit-notes-
>    groups.ts:271/272); derive-карта называла только первого. P3 = «пара
>    глифов по состоянию, не только классом active» — **подключи
>    two-state-swap логику к ОБЕИМ кнопкам**.
> 2. **Доп. проход (не пропускать — это то, чего v1/v2 не покрывали):**
>    (i) grep `<symbol` и `<use` по всему репо — 2 общих shared-символа
>    (`icon-archive`, `coffin-emblem`) должны иметь ВСЕ свои `<use>`-точки
>    учтены дерайв-картой, не только по одной; (ii) grep голый `<svg` по
>    ВСЕМ `dusk/*.ts` файлам (не только 01/02) и по ВСЕМ `.html` в репо —
>    любой inline-SVG, который не входит ни в IC/GIC/FIC, ни в известные
>    75 index.html-статики, — потенциально нигде не учтённый глиф; занеси
>    в derive-карту или в список находок п.3. (iii) Сверь итоговые счётчики
>    с census B3-icons.md (47 IC / 18 GIC / 22 FIC / 75 статик) — если числа
>    ВЫРОСЛИ (новые фичи после 11.07: снуз, шаблоны, promote/demote,
>    quick-add typeahead, F1/F2 мобильный tool­bar/rail-digest) — это
>    ОЖИДАЕМО, не баг; но каждый НОВЫЙ найденный глиф идёт в п.3 находок,
>    а не тихо остаётся непокрытым.
> 3. Для каждой роли/строки в fb3, где нет собственного явного пика юзера
>    (список ниже) — НЕ бери автоматически «текущий/без изменений». Сначала
>    grep'ом проверь, нет ли в derive-карте/Tier-A-списке/тексте самого fb2
>    прямой ссылки, что эта же визуальная роль уже решена под другой буквой
>    (как оказалось с BS/BT ниже — они дубль уже принятого AI1). Только если
>    такой ссылки реально нет — тогда «текущий/без изменений» верно.
> 4. **Если сверка найдёт роль/иконку вообще без вердикта ни в одном из
>    fb1/fb2/fb3** (по-настоящему пропущенный глиф) — НЕ придумывай дизайн
>    сам; собери список находок (файл:строка + что это + title-цитата) и
>    остановись перед вживлением ИМЕННО этого пункта — юзер решит, нужен ли
>    микро-Fable-проход на 1-3 иконки. Всё остальное вживляй сразу.
>
> **Финальная строка вердикта (юзер, 2026-07-18, ничего не переспрашивать по
> ней — только реальные новые гэпы от coverage-сверки):**
> `A2·B1·C1·D1·E2·F2 | G1·H2·I2·J2·K1·L1·M3·N3·O2·P3·Q1·R2·S2·T1·U2·V1·W1·X1·
> Y1·Z2·AA1·AB1·AC1·AD1·AF1·AI1·AN1·AO1·AP1·AQ2·AR1·AS1·AT1 | AU1·AV1·AW1·
> AX0·AY1·AZ0·BA0·BB1·BC1·BD1·BE1·BF1·BG1·BH1·BI1·BJ1·BK1·BL0·BM1·BN1·BO1·
> BP0·BQ1·BR1·BU1`
>
> **AX0 (цепь-хэндл `IC.drag`) и BL0 (`GIC.dismiss`+search-x) = keep current,
> ДОКАЗАНО:** оба явно названы в списке «Tier-A подтверждён вторым проходом
> правок-2 (НЕ трогать)» чуть выше в этом файле (секция O-FINAL, строка
> «sundial, drag-цепь, ... dismiss, tocClose, ...») — это буквально «уже
> решено раньше», не догадка. Код для AX/BL НЕ трогать вообще.
>
> **AZ0 (`IC.ouroboros`+`cycleReturn`) и BP0 (цвет-фильтр-галка, 03:1440) =
> keep current, НО без такой же прямой улики** — только исходная пометка
> «мотив залочен» (AZ, концепция ouroboros=repeat, не артворк) и полное
> отсутствие прошлых упоминаний (BP). Код тоже НЕ трогать, но если
> coverage-сверка вдруг найдёт для них ссылку на решённую роль — она
> первична над этой строкой.
>
> **BS (буквоформы B/I/U/S̶) и BT (списки ul/ol/task) ВЫЧЕРКНУТЫ из строки
> вердикта ВООБЩЕ — это не отдельный пункт вживления.** Они byte-for-byte
> подмножество уже принятого **AI1** (fb2, роль AI, «FIC — готизация
> микрознаков редактора», «выбирается ряд целиком») — сверься с текстом
> AI1-кандидата в fb2-icons-preview.html: он дословно описывает и B/I/U/S̶
> (=BS), и ul/ol/task (=BT). **Просто вживи AI1 как единый FIC-ряд
> (02:3146+, derive-карта «AI→FIC-ряд») — это автоматически закрывает и
> BS, и BT. Отдельного шага для них нет.**
>
> Остальные 21 роль AU-BU с собственным пиком (AU1..BU1, кроме
> AX0/AZ0/BL0/BP0/удалённых BS/BT) — реальный редизайн, вживлять байт-в-байт
> из fb3-превью как обычно.
>
> **Источники истины (SVG байт-в-байт, параметры подачи — сверять с
> соседями, не копировать слепо — правило из основного O-FINAL-брифа
> выше):** `fb1-icons-preview.html` (A-F), `fb2-icons-preview.html` (G-AT,
> включая роль AI/AI1 для FIC-ряда), `fb3-icons-preview.html` (AU-BU).
>
> После coverage-сверки + вживления всей строки — обычная верификация
> (vitest + build + 16px-гейт глазами) + version.json bump + коммит-пуш.
> Затем без остановки (если ресурс окна позволяет) — переходи к следующему
> по порядку из STRATEGY.md/§«ПОРЯДОК ОКОН OPUS» (TWA, если ещё не сделан;
> иначе W2). СТОП в конце окна, обнови память.

## §W4-FABLE-HANDOFF (2026-07-19, сессия drawer) — ДОПОЛНЕНИЯ к O-FINAL и TWA

**1. Ратчет-инвентарь иконок (ЖИВОЙ, обязателен к соблюдению).**
Коммит `84c7a01`: `scripts/icon-inventory.mjs` + `audit-v2/icon-registry.json`
(187 глифов) + `tests/icon-ratchet.test.js`. Любой новый/изменённый inline-SVG
без записи в реестре ВАЛИТ `npm test`. Процедура после КАЖДОГО вживления:
`node scripts/icon-inventory.mjs --update` (роли существующих хэшей сохраняются,
изменённый глиф = новый хэш UNASSIGNED) → проставить `role` новым хэшам
(буква вердикта / derive / decor) ТЕМ ЖЕ коммитом. Это заменяет ручную часть
coverage-сверки шага 0 ADDENDUM v3: пропуски теперь ловит тест, но семантическую
сверку «роль → все потребители» (grep usage) делать по-прежнему.

**2. fb4 мини-глифы — вживить ПОСЛЕ вердикта юзера. ⚠ БРАТЬ ВЕРСИЮ v3
(`526a9a0`).** История отбраковки: v1 `c0c2aff` («не змея, детализация на
нуле») → v2 `07e5414` (AZ2 зарублен: «мультяшно», cycleReturn2 — «ни на что не
похожа») → **v3 = действующая**. Файл тот же —
`audit-v2/previews/fb4-mini-glyphs.html`; генератор геометрии заархивирован
рядом: `audit-v2/previews/gen-fb4-v3.mjs` (правки глифов делать ЧЕРЕЗ него,
он переписывает HTML целиком).
- **AZ3 · уроборос по референсу юзера** (гравюра: замкнутое кольцо, голова
  вырастает из тела на макушке, разомкнутая пасть смыкается на кончике хвоста,
  раздвоенный язык глубоко внутрь круга; рогов нет) → `IC.ouroboros`
  (01, потребители: 03:2204, 04:93/182/509/696/1445/2302/2520, index.html:1794).
  **ДВЕ трактовки — юзер выбирает одну:** `AZ3a` сплошной штрих (надёжнее в
  мета-теге 12px) / `AZ3b` двойные кромки + чешуя (богаче на 24-48px).
- **cycleReturn3** (микро-уроборос 10×10 на языке AZ3) → `IC.cycleReturn`
  (01:521, потребитель 04:127). Альтернатива по слову юзера — оставить текущий
  ромб-стрелку.
- **FL1 складень-диптих ×3** — ПРИНЯТ юзером (v1, не менялся) и **FL2
  «Кодекс-разворот» ×3** — полная переделка по запросу «хотел большего»
  (левая страница-список с буквицей / кромка со срезом стопки / одна страница).
  Оба в превью, юзер выбирает → `GIC.focusLvl[0..2]` (02:220-224, потребители
  02:709 + 02:1130). Это настоящий таргет роли BN (мёртвый `GIC.focus` 02:216
  не трогать).
- **BL1 «Портикулис» ×3** — принят, и **BL2 «Кованая шторка над инструментами»
  ×3** — переделка по претензии «голая решётка не говорит тулбар» (виден сам
  ряд инструментов + заслонка: полуопущена пунктиром / запаркована с засовом /
  опущена с кольцом-ручкой). Оба в превью, юзер выбирает →
  `GIC.barLvl.{auto,open,closed}` (02:228-232, тулбар-тогл Гримуара).
НЕ перерисовывать ничего из этого — byte-for-byte из превью (или из генератора).

**3. AX-цепь = чистое вживление готового арта:** fb3 AX1 (fb3-icons-preview
:304) → `IC.drag` (01:~396) byte-for-byte. Рисовать нечего.

**4. TWA-бриф (§O-4) — три добавки одной волной manifest.json:**
(а) manifest `shortcuts`: «Новая задача» (long-press лончер-иконки → открыть с
фокусом в input; хэш-параметр); (б) Web Share Target (пошаренный текст/URL →
новая задача через quick-add парсер); (в) НЕ резать notification-permission в
TWA-обёртке и проверить Web Push на закрытом TWA в первом смоке APK —
см. PUSH-SPEC.md §9 (корень репо). PUSH-SPEC Ф1-Ф3 = твоя механика (после TWA
или по указанию юзера), Ф4-UX = Fable П1.

**5. README-витрина (реестр, не срочно):** полноценный README для будущего
публичного репо (резюме-цель юзера): скриншоты мобилы/десктопа, схема синка
(3-way merge + карантин), стек, история миграции. Скриншот-харнесс есть
(D:\tmp\pw\b1). По явному «go» юзера.

**6. Календарь:** CALENDAR-SPEC.md (корень) — Ф1 (calendarItems+vitest) и далее
только по явному «go» юзера (приоритет НИЖЕ пушей). Готик-слой Ф4 = Fable П2.

**7. Drawer W4 задеплоен Fable и ДОВЕДЁН по прод-ревью юзера.** Коммиты:
`7073e0c` (база) → `c20dd03` (рельса 1-в-1 + прозрачная кромка) → `ed881fe`
(кластер ДАННЫЕ в створке + select-mode закрывает панель), build `2026-07-19-18`.
Файлы: index.html (#drawer-tab, #drawer-scrim), style.css (§W4 MOBILE DRAWER),
dusk/08 (низ файла). Координация «не параллелить index/style» снята — стартуй.
Пробы (перезапускать при правках рядом): `D:\tmp\pw\b1\w4_drawer.mjs` 12/12,
`w4_widths.mjs` 13/13 (360/768/1000). Тонкости, которые легко сломать:
(а) drawer-правила перебивают coarse-медиа F2 (лейблы кластеров,
`:has(#btn-filter)`-шов, кап пилюли 45vw, скрытый `.toolbar-group-data`) —
специфичность подобрана точно, менять осторожно; (б) `.rail-digest` стили
теперь ГЛОБАЛЬНЫЕ, display-гейт по тирам (≤820 и ≥1440 показывают, средний
тир прячет); (в) FAB'ы глушатся при открытой створке (они на body-уровне z50,
панель заперта в стекинг-контексте .todo-app). Если юзер принесёт ещё правки
по drawer — они приоритетнее твоей очереди только по его явному слову.
