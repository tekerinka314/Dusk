# STRATEGY.md — программа работ DUSK

## ⚑ РАМКА (ред. 2026-07-24 — перестройка после апгрейда Opus до 5)

**Сплит моделей ОТМЕНЁН.** Opus 5 = паритет с Fable (местами выше). Всё, что
прежние редакции отводили Fable (дизайн-фазы, вкус-критичное, превью-циклы,
Push Ф4, календарь-дизайн), теперь делает Opus. Таблицы слотов «Ф-А…Ф-D» и
«П1/П2/П3» ниже по файлу — **история**, не план.

**Из этого следует:**
- Порядок задач больше НЕ искажён доступностью модели. Сортировка = ценность ×
  зависимость × риск.
- Бэклог «опционала» (`optional-features-backlog`) существовал в основном из-за
  дефицита Fable-лимитов. Дефицита нет → **опционал переведён в очередь**
  (блок 6). Не обязательный, но желательный.
- Бюджет: ~2 недельных лимита (≈20 сессионных) + $100 кредитов резервом,
  тратятся Opus'ом. Оценки «≈лимитов» ниже — грубый ориентир, НЕ ограничитель;
  качество и глубина важнее скорости (слово юзера 2026-07-24).

**Ключевое решение порядка (юзер: «скорость APK не приоритетна, выбирай
глубже»):** верификационный фундамент и аудит идут **ДО** флагмана
пуш+TWA, а не после. Пуш+TWA — самая крупная и рискованная новая поверхность
за историю проекта (крипта RFC 8291, worker-cron, WebView-OAuth); строить её
на неотаудированной базе без смоук-харнесса в репо = хуже, не быстрее.

---

## Очередь (строгий порядок, ред. 2026-07-24)

### Блок 0 — горячее ⏳ В РАБОТЕ
1. **Баг Гримуара:** кнопка TOC заходит на границы тулбара; тогл тулбара тоже
   на грани. Верстка → systematic-debugging + замер DOM, не иконка.
2. **Творческий хвост O-FINAL** (гринлайт юзера 2026-07-24): archive-крипта
   де-алиасинг (`#icon-archive` шарится `index.html:610` archiveAll и `:721`
   склеп, symbol `:1671`) · N-сигилы уведомлений (`_qaRenderMenu`,
   `08-quickadd-export-init.ts:157`) · X1 арки-времени дедлайн-модалки.

   *«CSS-доп-проход» СНЯТ* — проверено грепом 2026-07-24: generic-chevron
   `polyline 6 9 12 15 18 9` не встречается нигде; `style.css:8516` теперь
   sync-glyph, ☰ sync-log отсутствует. Осталась микро-проверка native-select.

   *Почему первым:* иконочный контекст (ратчет, правило «все точки старого
   глифа», генератор `gen-fb4-v3.mjs`) свежий — после других блоков выветрится.

### Блок 1 — W2, 9 багов
`audit-v2/FIX-PLAN.md` §W2. **Кириллица квик-эдда ПЕРВОЙ** (V2-B1-11: `!`/`*`/`%`
не ловят русские слова = функциональный баг). Далее контраст AA (B5-01/12),
disabled-стили (B4-05), a11y карантина (B5-04), focus-visible (B5-03), `inert`
Параметров (B5-02), reduced-motion тоста (B5-07), RU-имена полей карантина
(B4-07), empty-state голос (B2-04/B5-09).

### Блок 2 — Этап 5: Playwright в репо ✅ ЗАКРЫТ 2026-07-28
Спека и состав — `E2E-SPEC.md`. **31 кейс, `npm run e2e`**, системный Chrome
над собранным `dist/`. Долг W0 (seq-aware сиды вместо `s4_p0_idbboot`) закрыт
кейсами бута в обе стороны. Пиксельных базлайнов НЕТ намеренно (§2 спеки:
автообновление Chrome + системные шрифты + JPEG-фон = ложные красные;
вместо них вердиктные замеры геометрии). CI не заводили — гейт локальный.
`D:\tmp\pw` НЕ хоронится: остаётся песочницей для одноразовых зондов ревью,
в репо переехали только долгоживущие инварианты.

### Блок 3 — аудит и мотион
- **B11+B12 экспресс-аудит + adversarial-линзы** по новым поверхностям
  (F2/F3/иконки/drawer) — одна задача. База чистится ДО наращивания; пуш
  получит собственный adversarial-проход внутри Ф5.
- **O-1 мотион-замер** → `MOTION-TRACE.md` → точечные фиксы reflow (02/07,
  content-visibility архива, жертвы `transition:all`).

### Блок 3.5 — ЯЗЫК ПРИЛОЖЕНИЯ НА ПЛОТНУЮ ГОТИКУ (новое, юзер 2026-07-26)
Сплошной свип копирайта: кнопки, меню, тосты, модалки, подсказки, ошибки,
пустые состояния, панель синка, карантин. Сейчас плотная готика живёт в
Гримуаре (и то не сплошь), остальное — канцелярский интерфейсный русский.
**Условие юзера:** тексты продумывать так, чтобы суть оставалась угадываемой;
частичная жертва интуитивности признана допустимой и нормальной.
Поглощает `V2-B5-11 terminology map` из W3; опора — таблица терминологии в
`audit-v2/B5-ux.md`. Порядок: **словарь-карта (нынешнее → готическое + пометки
«здесь жертвуем понятностью») → вердикт юзера → вживление партиями по экранам.**
Пилот и затравка словаря — W2-9 (empty-state в плотной готике: «обет»,
«алтарь», «деяние», «зов»). Место в очереди уточнить у юзера; по умолчанию
после W3, до флагмана.

### Блок 4 — W3, ~14 polish
`FIX-PLAN.md` §W3. Делается ДО нового слоя, чтобы полировать законченную базу.

### Блок 5 — 🎯 ФЛАГМАН: Push + TWA APK
По `PUSH-SPEC.md`, строгий внутренний порядок:
1. **Ф1** — worker endpoints + KV-схема + cron + пустой пуш + клиентский
   билдер расписания (vitest) + `sw 'push'`. Уже даёт рабочие напоминания.
2. **Ф2** — RFC 8291 aes128gcm по тест-векторам Appendix A (изолированный
   модуль, главный риск).
3. **Ф4** (бывший Fable-слот) — настройки, permission-флоу, готический вид
   уведомления.
4. **TWA Android APK** — по `ANDROID-PRENOTES.md`; одна волна manifest
   (shortcuts + Share Target + notification perm) + смоук пуша на ЗАКРЫТОМ TWA
   + OAuth-капкан WebView.
5. **Ф3 + Ф5** — deep-link по uid, тихие часы, overdue-once, устройства-CRUD,
   двухдевайсный прогон + adversarial-проход по пуш-поверхности.

### Блок 6 — бывший опционал, теперь очередь (по «go» на каждый)
Порядок предложенный, вкусовые развилки за юзером:
1. **Календарь (Idea 5)** — `CALENDAR-SPEC.md` готова (рубрики-часослов, глиф
   таба, месячная сетка дедлайнов). 4-я страница.
2. **Гримуар Этап 4** — картинки в заметках + IndexedDB. ОТДЕЛЬНЫЙ план:
   ключевой вопрос — сайз-бюджет синкуемого блоба (картинки НЕ в state).
3. **Master-detail Гримуара** на широком тире (открытый вопрос Ф3).
4. **Стрик (Idea 2)** — нужен готик-вердикт юзера (сомнения аскет-фита).
5. **Tauri Windows** — десктоп-обёртка (официальный Vite-шаблон).
6. Грим-фичи по `optional-features-backlog`: теги заметок · папки · бейджи ·
   `[[wiki]]`-связи · режим чтения · PDF/HTML-экспорт · slash-меню · блок-DnD.

---

## Правила исполнения (без изменений)

- Один блок/находка/слайс = один коммит + пуш + бамп `public/version.json`
  (доки — без бампа). Стейджить **поимённо**, не `git add -A`.
- `npm test` + `npm run build` зелёные до пуша. Многострочное сообщение
  коммита — через файл и `git commit -F`.
- Ратчет иконок: после вживления `node scripts/icon-inventory.mjs --update` +
  роли тем же коммитом (`tests/icon-ratchet.test.js` красный иначе).
- Вкусовые развилки вне утверждённого — в `audit-v2/REVIEW-QUEUE.md`,
  продолжать со следующей независимой работой.
- Визуальное закрывает **прод-ревью юзера**, не эмуляция.
- Не трогать: merge-семантику `09-sync` (кроме ратифицированных спек),
  `worker/` вне пуш-блока, залоченные готические мотивы.

## Definition of done (программа)

Мобайл первоклассный · дизайн/иконки вживлены · drawer-nav · W2+W3 закрыты ·
аудит-хвост (B11/B12 + adversarial) пройден · Playwright-смоук в репо ·
**пуш-напоминания работают при закрытом приложении** · APK (TWA) на телефоне ·
FINDINGS без открытых ≥UI2/DL1 · опционал блока 6 — по мере «go».

---
---

# ⌛ ИСТОРИЯ (не план)

## Ред. 2026-07-19 — последний расклад со сплитом моделей

Приоритет продукта пересмотрен 2026-07-19 (утверждено юзером): **пуш-уведомления
ВЫМЕСТИЛИ календарь.** Причина: календарь = 4-е представление уже видимых данных
(сортировка, бейджи, дайджест), а push-канала у таск-менеджера нет вообще; плюс
пуш-архитектуру надо заложить ДО/ВМЕСТЕ с TWA, иначе окно теряется. Спеки в
корне: `PUSH-SPEC.md`, `CALENDAR-SPEC.md`. *(Решение о приоритете пуша ОСТАЁТСЯ
в силе; изменилось только то, что календарь снова по бюджету реалистичен —
блок 6.)*

| Слот | Работа | Кто |
|---|---|---|
| **П1** (1-й платный) | Правки drawer по прод-ревью + пуш-UX/дизайн = PUSH-SPEC Ф4 | Fable |
| **П2** | Календарь-дизайн (CALENDAR-SPEC Ф4) + ратификация Opus-пачек + TWA-иконка/splash | Fable |
| **П3** | Резерв: master-detail Гримуара, типографика, иконочные хвосты | Fable |
| Между ними | O-FINAL хвост → TWA APK → PUSH Ф1-Ф3 → W2 → O-1 → B11/B12 → W3 → Этап 5 | Opus |

**Ратчет иконок (2026-07-19):** `npm test` падает на любом inline-SVG вне
`audit-v2/icon-registry.json` — молчаливый дрейф иконочного скоупа (причина
пропусков fb1-fb3) больше невозможен.

## Ред. 2026-07-17 — план на 5 лимитов Fable (ВЫПОЛНЕН)

Условия ресета: ~5 лимитов Fable до 2026-07-19 + ~2–4 лимита Opus; после 19.07 —
~2.5 недельных лимита Opus на 2 недели.

Решения юзера 2026-07-17: adversarial-воркфлоу слит с B11+B12 → Opus; иконочная
программа = 2 лимита Fable; TWA — Opus последовательно, не в параллель;
**xhigh экономно** (только чистый вкус), протокол «спросить перед сменой
/effort» действует; ревью-каденция вечерними пачками.

| Лимит | Работа | Статус |
|---|---|---|
| **Ф-А** | Баги юзера (long-press хинт, сорт-дропдаун coarse) + поповер-свип O-2 | ✅ |
| **Ф-B1** | Иконки: инвентарь misfires → превью-партия юзеру на выбор (xhigh) | ✅ |
| **Ф-B2** | Вживление иконок + эмбер приоритета (B2-02) + палитра меток (B2-03) | ✅ |
| **Ф-C** | Drawer-nav мобайл W4 (`7073e0c`→`c20dd03`→`ed881fe`) | ✅ |
| **Ф-D** | Ратчет иконок, fb4 v2, PUSH/CALENDAR-SPEC, §W4-хэндофф | ✅ |

Классификация того периода: **F0** иконки/дизайн/drawer/вердикты · **O1**
W2-механика, O-1/O-2, TWA, B11/B12, W3, Этап 5, синк-стресс · **O2** B1-10
виртуализация, px→rem, календарь-механика. Классификация мертва вместе со
сплитом — оставлена как след рассуждения.
