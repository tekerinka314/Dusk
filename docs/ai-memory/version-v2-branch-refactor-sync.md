---
name: version-v2-branch-refactor-sync
description: "2026-06-26 веха версионирования — tag v1.86-stable-core (точка отката), активная ветка теперь refactor/sync, папка переименовывается в DUSK_v2.0 (история синхронно)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 02755f52-20d4-4bd9-8bdc-285f24a83684
---

Веха 2026-06-26 (организация версий перед рефактор-стадией).

- **Tag `v1.86-stable-core`** — annotated, запушен на origin. Стабильный снимок
  ДО рефактора (дедлайн-блок + «Свеча», polish-бэклог закрыт, коммит `60c73ac`).
  Точка отката: `git checkout v1.86-stable-core`.
- **Ветка `refactor/sync`** — создана + запушена; переключились на неё. Это
  **рабочая ветка для всей дальнейшей работы**. Старая `fix/ui-repeat-meta-subtasks`
  заморожена как история. CLAUDE.md строка 141 обновлена → ветку больше НЕ
  переспрашивать. (Правка CLAUDE.md на момент записи НЕ закоммичена — читается с диска.)
- **Переименование папки** (делает пользователь вручную после выхода из сессии,
  скрипт выдан): `D:\VSCode projects\DUSK_1_86` → `D:\VSCode projects\DUSK_v2.0`.
  СИНХРОННО переименовать папку истории
  `C:\Users\serge\.claude\projects\D--VSCode-projects-DUSK-1-86` →
  `D--VSCode-projects-DUSK-v2-0`, иначе сессии/память отвяжутся. **Правило
  кодировки пути в имя папки истории:** каждый спецсимвол (`:` `\` `_` `.` пробел)
  → один дефис (`D:\VSCode projects\DUSK_1_86` → `D--VSCode-projects-DUSK-1-86`).
  После переименования пути во всех memory/CLAUDE.md, ссылающиеся на старую папку,
  станут историческими — фактическое местоположение = новая папка.
- **GATED стадия не стартовала.** Рефактор/синк (Idea 8 uuid+tombstones → 7c split
  → Google Drive sync → Capacitor/Tauri) всё ещё ждёт явного «go». Переключение
  ветки ≠ «go».

Связано: [[audit-2026-06-25-crossapp]] (текущее состояние работ).
