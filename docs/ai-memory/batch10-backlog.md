---
name: batch10-backlog
description: O-FINAL Batch-10 БЭКЛОГ + список правок юзера 2026-07-19 (после 10a). Точная карта что вживлено/что осталось/что редизайнить-самому + верификационный свип «все ли иконки заменены в живом сайте». Читать вместе с plan-2026-07-17-final-limits.
metadata: 
  node_type: memory
  type: project
  originSessionId: 4e71c408-2bbc-400a-b2cc-a9ca8913a61e
  modified: 2026-07-20T10:33:18.513Z
---

# O-FINAL Batch-10 — бэклог (после 10a/10a-fix, 2026-07-19)

Задеплоено: **10a** `95d782b` build -13 (AV1·AW1·AY1·BO1·BU1), **10a-fix**
`40dcc8a` build -14 (sortPriority→AW1 реверс, noneMoonL/R→C1, task-restore-all→AY1).

Контекст сессии был ~259k (разведка 19 ролей). Свежая сессия: читать эту память +
[[plan-2026-07-17-final-limits]] (там точные строки 10b) — превью НЕ перечитывать
целиком (карта уже извлечена), только точечные файлы.

## РЕШЕНИЯ/РЕВЕРСЫ ЮЗЕРА 2026-07-19 (записаны, часть сделана)
- **AW1 ВЕЗДЕ** (реверс): и приоритет-кнопка (IC.spires) И сорт-по-приоритету
  (sortPriority) = AW1. Старый **M3** chain-tower РЕТАЙРНУТ. Юзер сказал «M3 тогда
  просто в архиве» — но у архива НЕТ отдельного сорт-по-приоритету (архив по
  месяцам), M3-таргета в архиве не найдено → трактовано как «M3 на полку». ⚠ если
  юзер хотел M3 конкретно где-то в архиве — уточнить. (СДЕЛАНО 10a-fix.)
- **noneMoonL/R → C1** детальный серп (реверс прежнего keep). (СДЕЛАНО 10a-fix.)
- **AX DnD-цепь = РЕДИЗАЙН** (реверс keep0!): юзер хочет заменить цепь перетаскивания
  → **AX1** (fb3:304, «Кованая цепь язык AQ2»). IC.drag (01:~397). НЕ сделано.
- **AZ уроборос повтора = РЕДИЗАЙН САМОМУ** (не был в вердикте): юзер хочет более
  заметную ЗМЕИНУЮ ГОЛОВУ. + ВОПРОС юзера: традиционно ОДНИМ КРУГОМ или как сейчас
  восьмёркой(∞)? **Моя рекомендация = ОДИН КРУГ** (истинный уроборос, голова-кусающая-
  хвост читается чётче в 16px; ∞ сейчас читается как generic-refresh). Ждёт: нарисовать
  AZ (один круг, выраженная голова+глаз+чешуя) + микро-бейдж cycleReturn, вживить.
  fb3 AZ1 (fb3:420, змей-восьмёрка) — можно взять как язык, но переверстать в круг.
- **focusLvl «свернуть список в рейл» (3 состояния) = РЕДИЗАЙН САМОМУ + вживить**:
  готика/детализация/интуитивность. Это `GIC.focusLvl[0..2]` (02:~, схемы уровней).
  ⚠ ЭТО и есть настоящий таргет роли BN (не мёртвый GIC.focus 02:216 — тот unused).
  BN1-стрела (fb3:1027) можно как язык одного из состояний, но нужны ВСЕ 3 схемы.
- **barLvl «скрытие тулбара»**: простовата но интуитивна — юзер НЕ уверен, редизайнить
  ли. Предложить идею, если есть; не форсить.

## БАГИ (репорт юзера)
1. **Иконка архива (U2-крипта) выглядит криво из-за алиасинга даже на QHD.**
   Symbol `#icon-archive` (index.html ~1615). Проверить геометрию/stroke-width/
   под-пиксельные координаты; возможно нужен ре-хинтинг путей или чуть толще stroke.
2. **«Вернуть всё» в склепе (гримуар) ОТСУТСТВУЕТ как кнопка — ДОБАВИТЬ.** Task-архив
   имеет `#btn-restore-all` (restoreAll), у склепа заметок такой нет (только
   grimBulkRestore в select-режиме). ФУНКЦИОНАЛ: новая кнопка в баре склепа +
   хэндлер (restore всех архивных заметок разом, вне select). Иконка = AY1.
   (Task-архив «Вернуть всё» иконка УЖЕ пофикшена → AY1 в 10a-fix.)

## ВЕРИФИКАЦИОННЫЙ СВИП — «все ли редизайненные иконки реально в сайте»
Юзер: его список НЕ полный, проследить самому что ВСЁ, что редизайнили, заменено.
Точечно он назвал (проверить каждую в живом коде):
- **«Показать все заметки подпунктов»** (04:109) + (05:272) = `IC.gothEye` (01:492) —
  **P3 НЕ вживлён** (gothEye старый). Нужен P3 two-state (fb2-кандидат P3: ВКЛ=
  полуприкрытое око, ВЫКЛ=открытое) на ОБЕ кнопки (тулбар-фильтр + эта). Творческий/
  fb2-исходник.
- Параметры формы: «без цвета» colorClear (Batch-6 → перечёрк H2, ПРОВЕРИТЬ live);
  «свой цвет» = **BH** (index 188, в 10b, НЕ вживлён); «добавить подпункт» (глиф?
  проверить/редизайн); «закрепить» pin = I2 (Batch-2a, проверить); «сохранить как
  шаблон» template = Y1 (Batch-2a, проверить).
- Модалка дедлайна: «очистка времени/даты» deadlineClear (Batch-9, проверить);
  «выбор времени» = роль X (отложена, завязана на N3); «стрелки ↑↓ год/день месяца»
  (date-степперы — вероятно generic, НЕ BI-dl-dur; проверить/редизайн); «открытие
  календаря» (глиф? проверить/редизайн); луна L/R без-группы (СДЕЛАНО 10a-fix).
- Тулбар: «только невыполненные» gothEye (=P3, см.выше); «по цвету» цвет-фильтр
  (index ~497 кристалл — BP0 keep, ПОДТВЕРДИТЬ что ок/заменён); «уведомления»
  (глиф? роль N _qaRenderMenu отложена; проверить).
- Задачи/подпункты: DnD-цепь AX1 (см.решения); повтор-уроборос AZ (см.решения).
- Гримуар: focusLvl 3-состояния (см.решения); + ОБЩИЙ свип всех grim-иконок что все
  редизайны на месте.

## 10b ОСТАТОК (точные строки — в [[plan-2026-07-17-final-limits]], picks
BB1·BC1·BE1·BF1·BG1·BH1·BI1·BJ1·BK1·BM1·BQ1·BR1 + BD parametric). НЕ вживлены.

## ПРОГРЕСС 2026-07-19 (сессия Opus)
- **⚠⚠ КОРРЕКЦИЯ (юзер поймал 2-й раз):** мой «свип» был ТОЧЕЧНЫМ (только 8
  названных worry-items) → ошибочно обобщил до «вживлено всё кроме P3». НЕВЕРНО.
  Весь **Batch-10b (12 пиков) + хвост fb2/fb3 НЕ вживлены**. УРОК: «свип» = ПОЛНЫЙ
  инвентарь КАЖДОГО ратифицированного пика (строка вердикта A2..BU) → grep таргет →
  вживлён/в-очереди, а НЕ спот-чек названных пунктов. НЕ ОБЪЯВЛЯТЬ «done» с частичного.
- **Точечно ПОДТВЕРЖДЕНО вживлено:** add-subtail=`IC.crossSm`=A2 ✓; date-степперы
  год/день=готич.копьё (1120/1129) ✓; месяц-пикер триггер=меч 1086 ✓; pin=`IC.pin`✓;
  template=`IC.template`✓; deadlineClear=`IC.deadlineClear` 01:1027 ✓; colorClear=
  перечёрк-H2 Batch-6 (проверить live).
- **РЕАЛЬНЫЕ ГЭПЫ (НЕ вживлены), из перепроверки списка юзера:** P3 gothEye
  (3 потребителя: 04:109+05:272+статик index.html:483) · **BH «свой цвет»** (index
  188/189, 10b) · **X «выбор времени»** дедлайн-модалка (отложен, N3) · **N
  «уведомления»** _qaRenderMenu (сигилы нет) · AX цепь · AZ уроборос · focusLvl ×3 ·
  **bulk гримуар+склеп** (BQ/BJ/BR fb3) · весь 10b (BB..BR+BD).
- **🔄 НОВЫЕ РЕШЕНИЯ ЮЗЕРА 2026-07-19 (реверсы, записать):**
  1. **noneMoon L/R «без группы» → применить детальную C1** (РЕВЕРС прежнего
     keep-current «плоские серпы»; юзер теперь ХОЧЕТ сложную луну L/R, как предлагалось).
     03:2014. — ⚠ противоречит старой строке «noneMoonL/R keep — не переспрашивать»:
     ОТМЕНЕНА, теперь C1.
  2. **bulk-иконки гримуара И склепа** — явно в список вживления (BQ грим-гроб,
     BJ крылатый свиток, BR опустошить-урна и пр.).
  3. barLvl (скрытие тулбара) — опц., предложить идею, не форсить.
- **СКЛЕП «ВЕРНУТЬ ВСЁ» — DONE** (`be5c38a`, build -15, vitest 35/35+build ✓).
  #grim-restore-all в грим-баре (склеп+вне-select+непусто, зеркало #grim-empty-crypt
  591-596) + grimRestoreAll() 02-grimoire (зеркало grimBulkRestore, все архивные,
  undo, без confirm) + globalThis-мост + диспетчер 01-core. Иконка AY1 byte-for-byte.
- **⚠ КООРДИНАЦИЯ:** мои иконки правят index.html/style.css; drawer-nav Fable тоже.
  Правило = НЕ параллелить. Fable-сессию запускать ПОСЛЕ моих коммитов.

## ⚠ ОБНОВЛЕНИЕ 2026-07-19 (сессия Fable, drawer-лимит) — НЕ ДУБЛИРОВАТЬ:
- **AZ + focusLvl + barLvl НАРИСОВАНЫ Fable** → `audit-v2/previews/fb4-mini-glyphs.html`
  + генератор геометрии `audit-v2/previews/gen-fb4-v3.mjs` (правки — через него).
  ⚠ **Актуальна ВЕРСИЯ v3 (`526a9a0`).** Отбраковка: v1 `c0c2aff` («не змея,
  детализация на нуле») → v2 `07e5414` (AZ2 «мультяшно», cycleReturn2 «ни на что
  не похожа») → v3. Состав v3: **AZ3** уроборос ПО РЕФЕРЕНСУ ЮЗЕРА (гравюра;
  голова вырастает из тела, пасть на хвосте, раздвоенный язык внутрь круга, без
  рогов) в ДВУХ трактовках — a сплошной штрих / b двойные кромки с чешуёй, юзер
  выбирает → IC.ouroboros; **cycleReturn3** микро-уроборос → IC.cycleReturn
  (альтернатива: оставить текущий ромб); **FL1 принят + FL2 «кодекс-разворот»**
  (полная переделка по «ожидал большего») → GIC.focusLvl[0..2] (=таргет роли BN);
  **BL1 принят + BL2 «кованая шторка над инструментами»** (по претензии «голая
  решётка не говорит тулбар») → GIC.barLvl. ЖДУТ вердикта → Opus вживляет
  byte-for-byte. НЕ перерисовывать.
  ⛔ **2026-07-19 ОТКАТ `c495ddd`:** превью возвращено к версии `8da6acd`
  (голова, которую юзер принял). Дальнейшие попытки переделать голову/хвост/
  язык ЗАБРАКОВАНЫ и отменены целиком. **Юзер доделает иконку САМ в новой
  сессии и сам пропишет постановку — не начинать по своей инициативе.**
- **AX** = вживить готовый fb3 AX1 (fb3:304) byte-for-byte — рисовать не надо.
- **Ратчет-инвентарь ЖИВОЙ** (`84c7a01`): scripts/icon-inventory.mjs +
  audit-v2/icon-registry.json + tests/icon-ratchet.test.js. Любой новый/изменённый
  svg без записи в реестре валит npm test. После КАЖДОГО вживления: `node
  scripts/icon-inventory.mjs --update` + проставить role новым хэшам тем же коммитом.
- **Drawer W4 вживлён и доведён по ревью** (`7073e0c`→`c20dd03`→`ed881fe`, build -18):
  index.html получил #drawer-tab (новый глиф, уже в реестре) + #drawer-scrim;
  style.css — секция W4 MOBILE DRAWER (перебивает coarse-медиа F2: лейблы,
  `:has(#btn-filter)`-шов, кап пилюли 45vw, `.toolbar-group-data`); координация
  «не параллелить index/style» СНЯТА — Opus может стартовать.
- **P3 gothEye / крипта-деалиасинг** — остаются Opus-креативом (см. ниже), Fable их
  НЕ рисовал.

## ОСТАЛОСЬ (след. Opus-сессия — креатив + механика):
1. **Креатив-превью** (icon-designer+impeccable+visual-design-foundations, HTML→юзер):
   **P3 gothEye two-state** (ВКЛ полуприкрытое / ВЫКЛ открытое, 3 потребителя,
   нужна логика two-state-swap не по .active) · **архив-крипта де-алиасинг**
   (#icon-archive index.html:1609 — слишком много мелочи в 24px: ступени 1.2px,
   финиал ~1.8px, смешанные stroke → мешанина на 16-20px; редизайн-под-размер) ·
   **AX** DnD-цепь (IC.drag 01:~397, fb3:304) · **AZ** уроборос ОДИН КРУГ
   (IC.ouroboros/cycleReturn 01:492/503, реком.согласована) · **focusLvl ×3**
   (GIC.focusLvl[0..2] 02, «свернуть список в рейл», редизайн-сам) · barLvl (опц.,
   предложить-не-форсить).
2. **10b byte-for-byte implant** (BB1·BC1·BE1·BF1·BG1·BH1·BI1·BJ1·BK1·BM1·BQ1·BR1
   + BD parametric coffinSVG отдельным ревью) — точные строки ниже §10b.
   ⚠ пересоздать verdict-SVG из fb3-превью (scratchpad session-specific).
