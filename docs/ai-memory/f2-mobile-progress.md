---
name: f2-mobile-progress
description: "F2-мобайл ЗАКРЫТ вкл. долг+свип (билды 2026-07-17-1…-5): вердикты юзера вжиты, известные болячки выжжены с пруфами, свип 29 экранов нашёл+закрыл баг гроба-в-выборе; ОСТАЛОСЬ: adversarial-воркфлоу (отложен юзером) + МОТИОН-перестройка (отдельная сессия) + проверка safe-area на устройстве"
metadata:
  node_type: memory
  type: project
  originSessionId: 719ad938-2ebd-40e3-ab39-90ad36d1ea63
---

**Статус 2026-07-17 (день): F2 включая долг ЗАКРЫТ. Билды -1…-5 на проде.**
План audit-v2/F2-MOBILE-PLAN.md выполнен кроме воркфлоу; норт-стар F2-DESIGN.md.
Тег отката `v2.4-pre-f2-mobile`. Слайсы A-D — история в git (38913d0…f53bbc7).

**Сессия закрытия долга (коммиты 504f3cf билд -4, 9af1447 билд -5):**
- Ревью юзера по телефону → вердикты вжиты: forced-dark опт-аут
  (`color-scheme:dark` meta+:root — Chrome Auto Dark Mode инвертировал гроб
  в белый; верификация = CDP `Emulation.setAutoDarkModeOverride`, контрольная
  светлая страница инвертируется, приложение 0-дифф); гроб/⋯ по центру первой
  строки заголовка (-7px); «Редактировать» в sub-⋯ шите И десктопном ⋯-меню;
  саб-гэпы 7→4px; групп-шит все строки 48px (role=menuitemcheckbox/radio
  выпадали из 48px-правила — расширен селектор style.css:1189); тулбар-штрих
  `:has(#btn-filter)::before`; ПАРАМЕТРЫ = две капсулы 100%; theme-color
  #0a0420 (безопасная зона; НА УСТРОЙСТВЕ НЕ ПРОВЕРЕНО).
- Развилки REVIEW-QUEUE все «оставляем» (A-карточка, квик-бар, синк-шит,
  дедлайн-чип).
- Свип 29 поверхностей × pixel7/small/landscape (харнесс f2_sweep.mjs,
  f2_geo.mjs, f2_autodark.mjs в D:\tmp\pw\b1): единственный жирный баг —
  в режиме выбора тап по ГРОБУ выполнял задачу (не выделял); фикс в
  toggleCheck (04-tasks). Плюс: bulk-бар висячие разделители на переносах
  убраны; dashed-инпуты добавления → solid. Гримуар/архив/модалки/лендскейп
  чистые. Десктоп-дифф 0.000% на каждом коммите; vitest 35/35.
- Известные болячки плана выжжены с пруф-скринами → audit-v2/shots/f2/sweep/
  + geo_*/autodark; итожник в REVIEW-QUEUE.md §F2.

**ОСТАЛОСЬ (по приоритету):**
1. **Adversarial-воркфлоу** — юзер отложил на несколько лимитов, НЕ отменён.
   Параметры binding (план §4): 5 линз sonnet-5 medium (владелец-10-сек /
   плотность / touch / готика / motion+CSS) + судья opus-4.8 high, 1-2 раунда.
2. **МОТИОН-перестройка** — юзер: анимации лагают на мощном телефоне (возможно
   и на ПК) — обсуждать ОТДЕЛЬНО, возможно полная перестройка motion-системы.
   Вход: transition:all на .task-item (style.css:1944), тяжёлые box-shadow/
   backdrop-filter кандидаты; данные не собраны — начать с профилирования.
3. **Ревью-раунд 2 (билд -6, ef130fd) — вжито:** саб-гэпы 3px + гроб 32px +
   фантомный гэп в checked убран (display:none на handle) + прыжок хэндла при
   тапе (липкий desktop-:hover margin 2px/-2px — нейтрализован на coarse);
   шапка раздышана (DUSK 28px, отступы 18/14/18, табы ~159px — вердикт
   «пережато»). Гроб при форс-дарке ✓, режим выбора ✓.
4. **Safe-area ВСЁ ЕЩЁ ЧЁРНАЯ на устройстве** после theme-color #0a0420
   (читался чёрным) → усилен до #150933 (билд -6). Если снова чёрная —
   это, вероятно, скрим/чин самого Chrome, НЕ theme-color; копать
   edge-to-edge Chrome Android (а PWA standalone у юзера норм).
5. Найдено попутно, НЕ чинилось: шапка на small (360) без эмблем-гробов
   (вероятно F1-правило узких ширин — спросить юзера, ок ли).

**Уроки/харнесс:** гримуар-лист = `.grim-leaf` (не .grim-note-item); таб
гримуара = `#nav-notes`; select-mode пробовать НАСТОЯЩИМ touchscreen.tap
(js .click() маскирует поведение); эмуляция Auto Dark = CDP
setAutoDarkModeOverride + контрольная светлая страница как дискриминатор.

Связь: [[f1-mobile-rework-done]], [[strategy-2026-07-16-fable-abundant]],
[[skills-toolkit-2026-07]].
