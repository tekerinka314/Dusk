---
name: f1-mobile-rework-done
description: Мобильная адаптация НЕ ЗАВЕРШЕНА — слайсы 1-6 + само-аудит задеплоены (билды -1…-8), но юзер недоволен качеством; двойной ревью-долг юзера; следующий заход = Ф2-мобайл целостный редизайн (Fable xhigh + adversarial workflow по «го») ЛИБО итерации с Опусом
metadata: 
  node_type: memory
  type: project
  originSessionId: 20078c52-ab20-4d6b-b966-8aefb9830cfb
---

**СТАТУС 2026-07-17 (вечер): РАБОТА НЕ ЗАВЕРШЕНА.** Юзер посмотрел результат
само-аудита: проблемы, которые он увидел сразу, НЕ закрыты — разочарован.
Диагноз Fable (признан): аудит оптимизировал МЕТРИКИ (ряды/оверфлоу/таргеты),
а не дизайн-качество; статичные скрины вместо интеракций; латание вместо
целостной композиции. Подход «скажи какие проблемы — пофикшу» юзер отверг:
итеративить он может с Опусом, от Fable ждёт автономного решения ВСЕХ проблем.

**Ревью-долг юзера (отложен им, возможно завтра+):** (1) слайсы 1-6 из прошлой
сессии + (2) само-аудит батчи -7/-8 из этой. Чек-лист и развилки —
audit-v2/REVIEW-QUEUE.md. НЕ давить с чек-листом, у него нет времени.

**Открытые поверхностные проблемы (Fable видит сам, кандидаты на то, что
заметил юзер):** карточки рыхлые — иконки-призраки без границ/аффорданса,
рассыпаны по ширине; подзадача в одно слово ~150px высоты, дыры между рядами;
тулбар трёхэтажный шкаф с лейблами и обрубками разделителей в начале wrapped-
рядов; шапка ~120px до табов; разнобой выравниваний (экшены задач влево,
подзадач вправо, Закрепить-центр/Сохранить-право); фокус-кольцо на первом
пункте action-шита (programmatic focus, светится и на устройстве); staggered-
вход ~2.8с тянется; dashed «+ГРУППА/ШАБЛОНЫ» выглядят черновыми.

**ПУТЬ A УТВЕРЖДЁН ЮЗЕРОМ (2026-07-17, обсуждён и уточнён). Binding-план =
`audit-v2/F2-MOBILE-PLAN.md` в репо (закоммичен 9ad43a6) — при старте новой
сессии ЧИТАТЬ ЕГО ПЕРВЫМ, там всё:** гибрид-автономия (превью спорного не
блокирует), скоуп = весь мобильный слой, тулбарный блок ДАННЫЕ переносится но
функционал остаётся доступным на 100%, workflow дёшево (sonnet-5 medium ~5-7
агентов + судья opus-4.8 high, 1-2 раунда, БЕЗ fable-агентов, запуск одобрен),
обязательный список скиллов (impeccable, refactoring-ui, emil-design-eng,
microinteractions, ux-heuristics, web-design-guidelines и др.), effort XHIGH.
Путь B (итерации с Опусом) — запасной, если A снова не примет.

Ф1 (MOBILE-REWORK-PLAN, B1-P0) выполнен Fable за один автономный лимит 2026-07-16.
Тег отката: `v2.3-pre-mobile-rework`. Коммиты: слайс 1 `e75cd79` (3-строчная
карточка, IS_COARSE в 01-core, ⋯-меню +Редактировать/snooze/архив/удалить),
слайс 2 `f6feed4` (2-строчный групп-хедер, группа-⋯ с armed-удалением через
`.fm-group-del`, пилюля 45vw, групп-хэндл виден на таче), слайс 3 `284b901`
(bottom sheet `.action-sheet` + `.fm-scrim` в `_openFloatMenu`, Esc-close,
body-scroll-lock, focus-return), слайс 4 `829b1e9` (.modal max-height dvh +
overflow — и фикс-follow-up `_pickerOpenUp` в 01-core: open-up пикеров ограничен
верхом модалки, иначе negative-overflow клип), слайс 5 `1bbd4fa` (viewport-fit=
cover, все нижние FAB + env(safe-area-inset-bottom), гуттер 76px на coarse),
слайс 6 `c466f1f` (fallbackOnBody; pen-звук через beforeinput на coarse;
enterkeyhint/autocapitalize-off; сегменты→showPicker() на тач; qa-тайпахед
клампится в visualViewport и следует за скроллом; грим-бар icon-only;
bulk-бар 2 ряда; таблицы заметок display:block+overflow-x).

**Ревью юзера — по `audit-v2/REVIEW-QUEUE.md`** (вкусовые развилки: право/лево
выравнивание экшенов, автокапс новой задачи, орнамент кромки шита и пр.).
Реальное устройство обязательно для слайса 5 (эмулятор без инсетов) и B1-20/21.

**Уроки харнесса:** fullPage-скриншоты Playwright при mobile-эмуляции ЛОМАЮТСЯ
(текст «рассыпается» вертикально; capture-артефакт, НЕ баг приложения) —
только viewport-скрины + evaluate-метрики; staggered-вход карточек требует
settle ~2.8s; десктоп-дифф всегда содержит 2 шумовые полосы (typewriter-
подзаголовок бренда + тикающий каунтдаун) — bands [~90-145] и [~1730-1750].
Пробы слайсов: `D:\tmp\pw\b1\rw_s1..s4.mjs`.

**Не сделано намеренно (по плану):** B1-10/18/16/25, B1-23 (перепроверить на
устройстве после ревью — вероятный драйвер устранён), sort-picker поповер
(отдельный движок, B4-02). Дальше по [[strategy-2026-07-16-fable-abundant]]:
Ф2 дизайн-программа (effort xhigh — ЗАПРОСИТЬ переключение у юзера!).

**САМО-АУДИТ ВЫПОЛНЕН (2026-07-17, Fable, скиллы impeccable+web-design-guidelines).**
Билды `2026-07-16-7` (d974718) и `-8` (3afcb03), оба задеплоены. Починено:
(1) ряд экшенов карточки = ОДНА строка во всю ширину карты (full-bleed
`margin-left:-48px` под колонку гроба; 40px кнопки, 36-38px @≤380 — групповые
карты теряют ещё 14px на group-body padding); (2) групп-хедер с длинным именем
= 2 строки (`.group-title{flex:1 1 0%}` — при flex-wrap элемент переносится по
БАЗОВОМУ размеру до сжатия, basis 0 лечит); (3) тулбар 42px таргеты / 3 ряда
(38px @≤380 — раньше на 360 h-overflow тулбара РАСТЯГИВАЛ layout-вьюпорт до
378px, вся страница рендерилась отъехавшей); (4) FAB шорткатов скрыт на coarse,
перо-звук на bottom 68px (справа 2 кружка вместо 3); (5) шапка-брендинг −30px
вертикали; (6) режим расписания: dl-side-panel скрыт на coarse (54px
stretch-колонка = пустой тоннель на карте с подзадачами + снова ломала ряд
экшенов 7/14) — вместо неё обычная пилюля, `_dlSideOn = showDlSide &&
!IS_COARSE` в 04-tasks; (7) грим-сниппеты: пункты списков через « · » (было
textContent-склеивание). Вкус-развилки дописаны в REVIEW-QUEUE.md (саб-экшены
вправо vs задачи влево; 3 ряда тулбара; вернуть ли dl-колонку верхней полоской).
НЕ тронуто: разделитель в начале wrapped-ряда тулбара (микро-косметика);
focus-кольцо первого пункта шита (вероятен harness-артефакт evaluate-клика).
**Урок харнесса:** navigации через `localStorage.currentPage`+reload НЕ работают
(IDB-first boot переживает подмену LS) — rw_sweep v2 кликает реальные табы,
empty-state сеет свежий контекст; rw_measure.mjs / rw_probe2.mjs / rw_desktop.mjs
добавлены рядом.
