---
name: audit-v2-fable-2026-07
description: "⭐⭐⭐ ТЕКУЩАЯ РАБОТА: аудит v2 — спека rev4.1, план S1-S7. Статус: S1+S2+S3+S4-дизайн+**S4/B6 ПОЛНОСТЬЮ ЗАКРЫТ** (Opus xhigh, `6bf5a35`). Итог B6: 6 finding (V2-B6-01 boot-гонка ФЛАГМАН DL3 + 02 multitab + 03 quota + 04 карантин-превью + 05 undo-design + 06 shiftDeadline-tz) + 2 промоушена (B0-01/02→B) + design-вердикт. ДАЛЬШЕ: следующий B-модуль плана S5+ (B6 закрыт); `[RATIFY-FABLE]` (B6-01/02/03/05+B0-02) ждут Fable"
metadata:
  node_type: memory
  type: project
  originSessionId: 92c99071-f67f-4d4b-a329-ddb224196104
---

# Аудит v2 — статус на 2026-07-11 (спека rev4)

**Операционная спека = `AUDIT-SPEC-V2.md` rev 4** (корень репо). Резюм новой
сессии: прочитать спеку rev4 (особенно §3 ROI-доктрина, §4a/4b, §5 таблица
S1-S7) + `audit-v2/FINDINGS.md` + отчёты + этот файл → выполнять следующую
сессию по таблице. Историю диалогов НЕ поднимать — всё перенесено в файлы.

## ⭐ S4/B6 Tier-0 ЗАКРЫТ (Opus 4.8 xhigh, 2026-07-12, коммит `6606351`)
Отчёт `audit-v2/B6-functional.md`; probe-скрипты `D:\tmp\pw\b1\s4_*.mjs` +
`fakecloud.mjs`; raw `audit-v2/shots/s4/*.json`. Docs-only, тесты 9/9 зелёные.
**Находки:** V2-B6-01 (ФЛАГМАН, DL3, `[RATIFY-FABLE]`) boot-гонка: on-open-sync
(11:661/676 `_exchangePromise.then`) читает `getSyncSubset(state)` на ПУСТОМ
state до того как async `loadState` приземлился → `applySyncSubset` затирает
загруженное. Детерминировано: пустой Drive→wipe 5/5; staler→потеря несинканной
правки 3/3. **Дискриминатор доказал: гонит ТОЛЬКО slow IDB-absent путь**
(IDB-present reload безопасен 14/14) — приват-режим/эвикция/first-boot, НЕ
обычный reload. Коммент 11:713 «init done» ложный (init async/не-awaited 08:1524).
· V2-B6-02 (DL2,`[RF]`,+E) multi-tab LWW (нет storage/BroadcastChannel; спросить
юзера про 2 вкладки) · V2-B6-03 (DL3,`[RF]`) saveState глотает QuotaExceededError
молча (нет try/catch на 01:1038) · V2-B6-04 (owed B4-07) карантин «пусто» для
body-only заметок/note-only подзадач (`_entryLoserPreview` 11:586 не читает
.body/.note); инъекция инертна · V2-B6-05 (design,`[RF]`) undo тянет через
приземлившийся мерж, self-healing (без потери). **Промоушены:** V2-B0-02 D→B
(IDB-stale clobber LS15→14, `[RF]`), V2-B0-01 A→B (grimEmptyCrypt воскрешение).
**ЧИСТО (без finding):** syncNow-цикл P6a-f (все 6 сценариев верны — сам движок
исправен, риск только в boot-гонке ДО цикла), merge-gaps P7a-d, backups-ring P3.
**B6 ЗАКРЫТ (Tier-1 финал `6bf5a35`; Tier-1-partial `182c277`).** **P11** sibling-sweep —
`\b`-дефект ИЗОЛИРОВАН в priority-regex (B1-11); date `%`(\S+) и tag `*`([\wа-яё]+u)
кириллице-безопасны → новых нет. **P9** repeats-clock — monthly-31 overflow-repair /
leap Feb-29 / weektime / cycle return-no-double-fire ВСЕ ВЕРНЫ, НО **V2-B6-06**:
`shiftDeadline` (04:1658) чеканит дату через `toISOString()`(UTC) при локальном приложении
(`_ymd`) → date-mode повтор-дедлайн едет на день короче восточнее UTC (UTC+3: daily
заморожен, weekly +6). Snooze+reset-тайминг верны, фикс 1 строка (`_ymd`). **Tier-1 остаток
статикой:** P12 undo-census ЧИСТ (все 30 `{undo:true}` пушат ДО мутации; import/merge 3
ветки верны), P15 callout round-trip ЧИСТ (санитайзер whitelist 5 blessed grim-co классов
→ закрывает callout-долг B4-07; дефект был только в карантин-превью B6-04), P10 статусы
display-only DL0, P16 notif guarded DL0. **P13/14/17/18 DEFERRED** (рантайм-тяжёлые
UI-матрицы, DL0-1, покрыты B4/B5, ROI отриц. — мандат юзера «пропускай опциональное»).
**E-резолвы юзера 2026-07-12:** B6-02 multi-tab — «обычно 1 вкладка, бывает больше» →
occasional, DL2 стоит но exposure LOW; B6-06 — «дедлайнами редко пользуюсь, не замечал» →
exposure LOW, баг детерминир. **ИТОГ B6: 6 finding (V2-B6-01..06) + 2 промоушена (B0-01/02)
+ 1 design-вердикт.** `[RATIFY-FABLE]`-теги (B6-01/02/03/05 + B0-02) ждут дешёвого Fable.
Уроки: fake Date через window.__fixedMs + FakeDate-обёртка; harness-артефакт vs реальность —
всегда дискриминатор (racetiming для B6-01); undo/callout/notif-остаток дешевле статикой чем
рантаймом.
**Углубление deferred (`ce2c603`, статик-deep):** P14 DnD + P18 reconcile — 3 highest-DL/IC
гипотезы все correct-by-construction, новых нет. Ключевая: **int-id коллизия reconcile×sync
СНЯТА** — оба устройства `nextId=1` (int id не глоб-уникальны, `_liCache`/`find(t=>t.id===)`
по int id), НО `mergeStates` мержит по uid + `_reindex` (09:393-448, вызван 09:492 каждый
мерж) ремапит int id (локальные uid держат id, remote-only свежий, groupId из `_groupUid`)
→ уникальны. Subtask-DnD split: 2 раздельных Sortable, cross-zone запрещён (pull/put:false,
done disabled) → `onSubDragEnd` не тоглит done КОРРЕКТНО. Card-sig (04:215)=cls+prio+dl-flag+
ink+mainHTML+subsHtml ловит любой контент.
**P13+P17 отрантаймлены ПОЛНОСТЬЮ (`e2dfbce`, юзер: «полностью значит рантайм»).** P13:
23-combo матрица (normal/split/schedule/schedule+split × sort × focus × color × today × 2
форс-пустых) — 0 JS-ошибок, структурные СОХРАНЯЮТ все 14 задач, empty-state виден IFF 0,
фильтры только сужают; V-1/V-2 фиксы подтверждены. P17 (coarse-emul Pixel-7): matchMedia
coarse+hover-none matched→6g активен; реальные touch-тапы работают end-to-end (check on/off,
subtask, note→инлайн-редактор, noteOpen=false = add-flow не баг); tap-hit action 30×30 /
sub 26×26 ≥AA, но check-col 21px / sub-check 16×20 <AA = ПОДМНОЖЕСТВО V2-B1-09 census
(кросс-реф, не новый). Новых finding 0. **Tier 1 закрыт без остатка.** Урок: emulation
media (coarse/hover-none) реально matched в headless → POSITIVE-тапы доверяемы; глиф≠hit-зона
(мерить ancestor-колонку); noteOpen флипается лишь при сохранении контента.

## Хронология моделей
1. **Fable 5** — Фаза 0 (спека) + Фаза 1 + начало B0; лимит кончился.
2. **Opus 4.8** (branch-сессия `ab34843c`, 2026-07-07..08) — rev3, добил B0,
   провёл B1 (25 находок, device-round, фикс PWA `686ddd3` — единственная
   правка кода, одобрена юзером).
3. **Fable 5 вернулся 2026-07-11** — провёл ревизию ветки Opus (работа ПРИНЯТА
   по существу; ARCHITECTURE.md спот-верифицирован; методология доказательств
   образцовая), написал **rev 4** по новому мандату юзера: **макс качество при
   макс эффективности контекста** (бюджет снова ограничение).

## Решения юзера 2026-07-11 (AskUserQuestion, все 4 = Recommended) — НЕ переспрашивать
1. **Слияние батчей в 7 сессий S1-S7** (порядок приоритетов v1 сохранён,
   сливается только оверхед): S1=B1-topup · S2=B2+B3+B4 · S3=B5 · S4=B6 ·
   S5=B7+B8 · S6=B9+B10+B11 · S7=B12+B13+B14. Отчёты — по-батчево, как раньше.
2. **Сплит моделей:** Fable = S1,S2,S3 + дизайн матрицы проб B6 + ратификация
   data-loss/high-sev находок B6 + финальный синтез B14. Opus = исполнение B6,
   S5, S6, S7(B12/B13). Обоснование: премиум там где вкус/data-safety-суждения,
   ревью в ~10× дешевле исполнения.
3. **B1-topup ТАРГЕТНЫЙ** (не exhaustive crawl): только НОВЫЕ классы корней —
   тач-виджеты (SegmentedInput+6 готик-пикеров), модалки с реальной клавиатурой,
   rotate-mid-edit (data safety), карантин-оверлей (засеять журнал), typeahead
   над клавиатурой. Новые ЭКЗЕМПЛЯРЫ известных 4 тем (crush action-row /
   nowrap-полосы / no-landscape / fixed-px) — одной строкой к теме, не
   переизмерять: mobile-rework снесёт эти поверхности. Кросс-батчевые лиды из
   «Leads for an EXHAUSTIVE B1» уходят батчам-владельцам; S2-S7 отныне аудитят
   desktop И мобильный вьюпорт.
4. **S1 — в НОВОЙ сессии** (после rev4+commit+Compact), не на хвосте этой.

## Ключевые правила rev4 (выжимка)
- **ROI-доктрина §3:** глубина по РИСКУ и НОВИЗНЕ; data-safety/вкус/новые классы
  корней = полная глубина; экземпляры известных тем = одна строка; проба если
  дешёвая, airtight-инференс с записанным опровержением если проба дорогая;
  сессия всегда ЗАКРЫВАЕТ свой батч; отчёт фиксирует что НЕ покрыто и почему.
- **Workflows ОТКЛЮЧЕНЫ** (§4a; факт: 20-агентная панель B1 сожгла ~2 лимита,
  6/20 дошло; серийный main-thread добор дал то же за ~⅓). Состязательная
  верификация — серийно; data-loss = независимый второй проход.
- Порядок = приоритеты v1; commit+push после каждого батча/сессии; дедуп против
  июньского аудита; аудит = только доки, НИКАКИХ фиксов кода без одобрения.
- Chrome-DevTools MCP ставить на S5.

## Статус
- [x] Фаза 0+1, [x] B0 (ARCHITECTURE.md + 4 находки; V2-B0-02 = тихая потеря
  правки IDB-first boot — ПЕРВАЯ проба в S4!), [x] **B1 ЗАКРЫТ (28 находок,
  эмуляция + 2 device-раунда).** Флагманы: B1-01 карточка, B1-13 заголовок
  группы, B1-26 редактирование (см. ниже); роадмап P0 = переделка action-model
  + тач-аффорданс редактирования; B1-19 PWA починен. S1-topup (Fable,
  2026-07-11): B1-26 dblclick-only редактирование — НА DEVICE double-tap
  РАБОТАЕТ (эмуляция врала: 0 событий) → даунгрейд до discoverability UI 2;
  B1-27 typeahead clip (портрет на device ХОРОШ, landscape клип стоит);
  B1-28 typeahead прибит к вьюпорту, отвязывается при скролле — И НА ПК, все
  типы (юзер) + код (нет scroll-обработки; B4 просканит сиблинг-поповеры);
  B1-22→A+device: нативные пикеры работают (= рабочий тач-путь), сегменты
  мертвы для VK (div без inputmode). Здоровые: пикеры 45px, модалки с
  клавиатурой, rotate-mid-edit БЕЗОПАСЕН, карантин ок (тач-restore).
  Остаточные лиды все розданы владельцам — ownership-таблица в конце
  B1-mobile.md. Пробы: `D:\tmp\pw\b1\s1_probes.mjs`, скрины
  `audit-v2/shots/s1/`. Урок: эмуляция double-tap НЕ отражает реальный
  Android Chrome (жест-зум глушит dblclick в эмуляции, на device нет).
- [x] **S2 ЗАКРЫТ (2026-07-11, Fable): B2 (5 находок) + B3 (5) + B4 (7).**
  Отчёты `audit-v2/B2-gothic.md`/`B3-icons.md`/`B4-ui.md`, коммиты `b5527d9`
  (инвентарь ~50 скринов s2/ + иконный лист) → B2 → B3 → B4. Флагманы:
  B2-01 два языка (identity-слой vs generic chrome), B2-02 полоска приоритета
  (AI-паттерн, лечить ember-идиомой `--sprio`), B3-01 систем. редизайн иконок
  (реестр→редизайн; tier-триаж, метрика элементов), B3-02 ~160 дублей при
  2 <symbol>, B4-01 нет desktop-тира (колонка ≤770px на 2560), B4-02 ЕДИНЫЙ
  корень поповеров: 4 движка, вся float-семья detach при скролле (live-proof
  s2_fix2: top 725→725, Esc не закрывает) — один utility закроет B1-27/28/17.
  Харнесс: `D:\tmp\pw\b1\s2_shots.mjs` (секции tasks|modals|grim|sync|empty|
  second|hover|icons — icons = переиспользуемый превью-лист для фикс-стадии),
  s2_fix1/2.mjs. Хвосты закрыты 2026-07-11: реальная заметка получена
  (grimuar_note.png, вердикты подтверждены, addendum в B2-gothic.md);
  **DESIGN-PLAN.md написан (`6a69c9a`)** — ультимативный дизайн-план по мандату
  юзера, ВСЕ скиллы применены (таблица §0: impeccable/emil/taste/redesign/
  foundations/web-typo/ui-ux-pro-max; serif-ban и LILA-rule taste-скиллов
  переопределены их же override-гейтами — бренд = manuscript+violet);
  структура: токены → готик-рамка поповеров → ember-приоритет → икон-реестр →
  desktop-тиры → мошн-система (emil); фазы D1-D8 с маппингом находок; 6 юзер-
  развилок в §7. Остался только V2-B3-05 (пустые глифы) — при следующих пробах.
- [x] **S3 ЗАКРЫТ (2026-07-11, Opus 4.8 xhigh): B5 UX+a11y+RU = 12 находок
  V2-B5-01..12.** Отчёт `audit-v2/B5-ux.md` (таблицы: контраст ground-truth,
  хоткеи, icon-aria census, discoverability-матрица, toast-census, терминология,
  карта humane-лейблов карантина = spec V2-B4-07, workflow-cost). Пробы
  `D:\tmp\pw\b1\s3_*.mjs`, JSON `audit-v2/shots/s3/_*.json`. **ГЛАВНОЕ: a11y-пол
  НАМНОГО выше ожиданий директивы** — live-region+toast announce, 13 модалок с
  focus-trap+return (`openModalWithFocus`), labelled dialogs, listbox
  activedescendant, 387/389 icon-кнопок с RU-именем УЖЕ есть и работают; все
  рекламируемые хоткеи fire-verified; `_matchKey` на `e.code` → кириллица-раскладка
  ОК. Реальные дыры (узкие): B5-01 overdue-чип 2.53:1 (единств. AA-fail; тёмно-
  красный #A01525 9.5px bold — увести на #E03060); B5-02 свёрнутая «Параметры»
  держит 40 фокусируемых контролов в tab-order (max-height:0 не inert); B5-03
  инпуты без focus-ring (кнопки есть); B5-04 карантин-оверлей мимо
  openModalWithFocus (нет label/trap/focus-in, **Esc не закрывает** — нет id →
  dismissModalById(undefined) no-op); B5-05 нет `<main>`/заголовков ниже h1; B5-06
  хоткей-доки неполны (нет Ctrl+F/F3/Esc/Backspace) + карта Гримуара нигде; B5-07
  toast анимируется под reduced-motion (единств. слип из 50 RM-блоков); B5-08 два
  «повтора» (задача vs авто-повтор дедлайна) без пояснения [RATIFY-FABLE]; B5-09
  notes search-empty нем (задачи показывают «Ничего не найдено»); B5-10 quick-add
  hint aria-hidden+клавой перекрыт; B5-11 терминология Архив/Склеп/Начертать
  [RATIFY-FABLE]; B5-12 dim-фиолет токен ~#9068C0 маргинал 4.7 (падает над ярким
  bg). **Переоценка V2-B2-05: архив-заголовок = 5.82:1 ПРОХОДИТ AA → «нечитаемость»
  = strike+hue, НЕ контраст** (аппендж в B5/B2, без нового ID). delete-ladder
  ПРАВИЛЬНО градуирован (архив нейтр, skull=danger) — позитив.
- [x] **S4-СТАРТ Fable ЗАКРЫТ (2026-07-12, коммит `bdd35f2`, запушено):** дизайн-
  половина S4 сделана. (1) Написан **`audit-v2/S4-B6-DIRECTIVE.md`** — матрица проб
  B6 для Opus (xhigh), ordered-by-risk: **Tier 0 data-safety** (P0 IDB-stale-boot
  clobber = промоут V2-B0-02 D→B — ГЛАВНАЯ проба; P1 grimEmptyCrypt-воскрешение
  промоут B0-01 A→B через чистый mergeStates; P2 multi-tab LWW; P3 backups-ring;
  P4 quota-честность; P5 undo-поверх-мержа = design-verdict [RATIFY-FABLE]; P6
  fake-cloud syncNow-петля a–f = НЕТЕСТИРОВАННЫЙ цикл 11; P7 merge-gap/identity
  translation; P8 карантин-integrity+«пусто»+инъекция) → **Tier 1** движки (P9
  repeats/cycle-clock, P10 дедлайны, P11 quick-add sibling-regex sweep — `\b`-после-
  кириллицы наверняка и в `*`/`%`, P12 undo-дисциплина, P13 view-mode комбо, P14
  DnD, P15 Гримуар, P16 notif, P17 mobile-taps, P18 reconcile-fuzz). Есть anti-scope/
  дедуп, judgment-frames, ratification-interface, traps, boundary-insurance. **Коммит
  после Tier 0 обязателен.** (2) **Ратифицированы оба [RATIFY-FABLE]** (тег →
  `RATIFIED-FABLE 2026-07-12` в FINDINGS): **B5-08** два-повтора — gap CONFIRMED,
  CF 2→3, фикс лексический (резерв «повтор»+ouroboros за задачей, срок = «Повторять
  срок»/«Ритм срока» — слово за юзером на D7); **B5-11** терминология — «заметка»/
  «запись» разводить безусловно; Архив/Склеп+Начертать = ЛЕГИТИМНЫЙ per-app флейвор
  (Гримуар-регистр богаче, не унифицировать — глоссарий подписывает ЮЗЕР на D7);
  permanent-delete глаголы согласовать ВНУТРИ каждого приложения. **ДАЛЬШЕ: S4-
  ИСПОЛНЕНИЕ = Opus 4.8 xhigh в НОВОЙ сессии исполняет `S4-B6-DIRECTIVE.md`
  дословно с §0 boot** (директива сама требует clean session; данная сессия
  компактится ИМЕННО на этой границе Fable→Opus).
  · [x] S4-дизайн (Fable) ·
  [ ] **S4-исполнение (Opus xhigh) — СЛЕДУЮЩЕЕ** ·
  [ ] S5 Opus high · [ ] S6 Opus xhigh · [ ] S7 Opus high, B14-драфт xhigh по
  `audit-v2/S7-B14-TEMPLATE.md` → Fable-ратификация. **Перераспределение
  rev4.1 утверждено юзером 2026-07-11** (зафиксировано в AUDIT-SPEC-V2.md §4b):
  Fable ~15% (директивы+ратификации, B6-матрица не отдаётся никогда), Opus
  ~85%; фикс-стадия — свой анализ пропорций при старте (ориентир: D1/D4
  механика→Opus; D2/D3/D5 вкус→Fable превью+Opus вживление; B1-P0→Fable дизайн
  +Opus исполнение). DESIGN-PLAN.md = операционная цель дизайн-фиксов.
- **S2-калибровка (ответы юзера 2026-07-11, НЕ переспрашивать):**
  1) основной рендер **2560×1440 @100% (DPR 1)**; другие разрешения аудировать
  вторично; 2) ground truth: (а) БОЛЬШИНСТВО иконок вне Гримуара не проходят
  трёхосевой бар — все три оси (готика × детализация × интуитивность) должны
  быть на максимуме одновременно, сейчас часто прокачана одна; (б) полоска
  приоритета задач = узнаваемый AI-паттерн; (в) юзер не дизайнер — находить
  всё самому, планка = абсолютный максимум качества/глубины/объёма, не
  экономить; 3) масштаб рекомендаций B2 = вариант (б): крупные ре-дизайн
  предложения допустимы, решает юзер по каждому; 4) реальную заметку Гримуара
  юзер скинет в папку проекта ПО ЗАПРОСУ (запросить перед Grimuar-скринами).
  Метод-предложения ратифицированы: иконочный лист (scratchpad HTML со всеми
  глифами 16/20/24/32px), курируемый скрин-сет (~30-40 desktop + 15-20 моб.
  из B1), CDP-форсинг hover/focus.
- **Дизайн-скиллы установлены (2026-07-11, ~/.claude/skills, по мандату юзера):**
  impeccable (основная критик-линза B2/B4), redesign-existing-projects
  (каталог AI-паттернов), emil-design-eng, review-animations (на S5/B7),
  design-taste-frontend, high-end-visual-design (⚠️ оба топят Apple/Linear-
  минимализм — брать крафт-принципы, эстетические директивы игнорировать:
  готика locked), ui-ux-pro-max (справочник), visual-design-foundations,
  web-typography, web-design-reviewer (резерв), superpowers весь сет (фикс-
  стадия). Использовать read-only как линзы: аудит пишет только доки.
- Харнесс: `D:\tmp\pw\b1\` (seed+пробы, переиспользовать); ~150 скринов в
  `audit-v2/shots/`; 6 фото устройства в `audit-v2/`. `npm test` зелёный
  (6 файлов/9 блоков). Юзер доступен для телефон-проверок.
- Fable-vs-Opus сравнительный анализ Opus'а — в хвосте branch-транскрипта
  (A[1504], A[1512]); его вывод совпал с принятым сплитом.

## Уроки (не повторять ошибок)
- Большие multi-agent панели жгут лимиты непропорционально ценности.
- Восстановление контекста из 18МБ транскрипта: извлекать только user-сообщения
  + финальные assistant-тексты node-скриптом (~60k вместо ~500k+ токенов).
- Mobile-эмуляция (isMobile+hasTouch) воспроизводит медиа-состояние реального
  телефона; но ≤360px-баги не воспроизводятся на 393-412px устройстве юзера.
- Эмуляция double-tap (2×touchscreen.tap) даёт 0 событий (зум-жест); реальный
  Android Chrome СИНТЕЗИРУЕТ dblclick на mobile-optimized вьюпорте — негативы
  тач-жестов из эмуляции подтверждать на device перед высокой severity.
- Провал тапа в пробе чаще артефакт порядка (недозакрытая анимация модалки,
  остаточный оверлей) — всегда чистый повтор в свежем контексте до записи.
- Компакт-дисциплина (просьба юзера 2026-07-11): когда аспект закрыт и его
  история не нужна — сверить что всё в файлах и ЗАПРОСИТЬ компакт у юзера.
