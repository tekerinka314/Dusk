---
name: dusk-polish-2026-06
description: "DUSK polishing pass — 12-item plan, what's done/committed and what's left (П5/П12/П11)"
metadata: 
  node_type: memory
  type: project
  originSessionId: cc25111e-5f8c-4f85-9e5c-87b53b1d4fed
---

Polishing pass over the EXISTING DUSK app (NOT the sync/refactor stage — that's still
forbidden without a separate go). User-driven 12-item list, done in waves; user reviews every
visual change in the running app and says when to commit. Branch `fix/ui-repeat-meta-subtasks`,
origin = github.com/tekerinka314/Dusk. Communicate in Russian. Commits are in Russian with the
`Co-Authored-By: Claude Opus 4.8` trailer. `bg-gothic.jpg` stays uncommitted (M from before).

## The 12 items
1 ярче подписи приоритетов в форме · 2 скрыть поиск в пустом архиве · 3 редизайн счётчика +
двойной прогресс-бар · 4 редизайн тогла «Параметры» (плита-реликварий) · 5 в форме: тогл
«закрепить новую задачу» + кнопка «сохранить как шаблон» (только сохранить) · 6 цвет группы =
10 пресетов + RGB-спектр · 7 удаление группы в параметрах (та же иконка/анимация/two-step) ·
8 готический дропдаун выбора группы · 9 тост удаления группы «…со всеми задачами» только если
были задачи · 10 заменить колхозную иконку удаления заметки (кинжал) · 11 ЗАМЕТКИ отдельной
вкладкой (огромный, поэтапно) · 12 полный паритет подпунктов в форме.

## DONE & pushed (commits on the branch)
- `7cb4087` Волна 1: п1 (ярче приоритеты), п2 (скрытие поиска в пустом архиве), п9 (тост группы).
- `88214e7` Волна 2 визуал: п10 (кинжал удаления заметки/сабноута), п3 (двойной прогресс-бар +
  счётчик), п4 (плита-реликварий тогла «Параметры»).
- `a840f25` Волна 2 финал: п6 (полный RGB-спектр ВСТРОЕН прямо в модалку группы — движок
  спектра параметризован `_grgbScope` task/group, элементы по классам, без дублей id; убраны
  openGroupColorModal/groupColorActive), п7+п8 (чипы → готический дропдаун `#grp-picker` по
  паттерну dl-month-picker: `renderGroupChips` рисует триггер+список, `selectGroupChip`,
  `initGroupPicker`; кнопка удаления `.grp-dd-del` с two-step, `deleteGroup` подсвечивает обе
  кнопки через querySelectorAll), фиксы (повтор по центру; симметрия отступов плиты;
  триггер группы фиолет по умолчанию/белый hover), иконки «Нет»/«без группы» = парные
  КОНТУРНЫЕ полумесяцы (fill none, stroke 1.5, путь `M14.5 3.35A9 9 0 1 0 14.5 20.65A9 9 0 0 1
  14.5 3.35Z`, правый зеркальный `.none-moon-r`), «Создать группу» = `IC.crossAdd`.

Иконки в реестре: `IC.noneMoonL/noneMoonR` (полумесяцы), `IC.crossAdd` (крест). Полумесяц
эволюционировал через 4 неудачных варианта (блоб→наклон→тонкая скобка→толстый) — финал
выверен по headless-скриншоту. Уроки: серп = ДВА арка равного радиуса (offset центров =
толщина), контур а не заливка; визуальные правки проверять скриншотом, см. [[dusk-headless-screenshot]].

## LEFT TO DO (next fresh session)
- **Волна 3 — паритет формы:** п5 (тогл «закрепить новую задачу» + «сохранить как шаблон»
  только сохраняет, размещение на моё усмотрение — у нас уже есть `state.templates`,
  Promote/Demote, шаблоны); п12 (полный функционал подпунктов в форме как в задачах, кроме
  бессмысленного при создании: чекбокс/promote/split пропустить).
- **Волна 4 — п11 заметки:** отдельная вкладка «Гримуар/Манускрипт», contenteditable,
  markdown-экспорт сразу; ТАБЛИЦЫ на этапе 2–3 (НЕ на 4-м); картинки+IndexedDB — последний
  этап, ТОЛЬКО с отдельного «go». Заметки независимы от задач (связь — потом). Этапы:
  0 каркас → 1 форматирование+md-экспорт → 2 организация+таблицы → 3 продвинутое → 4 картинки.

## Продуктовые решения (держать)
Quick-add символы: `!`=приоритет, `*`=тег, `%`=дата. Удаление группы удаляет её задачи+сабтаски
(тост предупреждает). «Никогда не терять данные» — правило №1. Готик-эстетика обязательна для
ВСЕГО (только hand-drawn SVG, без emoji/Material). Related: [[audit-2026-06-fix-progress]],
[[audit-2026-06-plan]].
