---
name: plan-2026-07-17-final-limits
description: "ДЕЙСТВУЮЩИЙ план (ред.9, 2026-07-18): **Ю7 ЗАКРЫТ вкл. фикс-3.** Обе ошибки фикса-2 были МОИ (этой же сессии), не потерянный вердикт фейбл-сессии до fb3 — fb2-строка проверена, цела везде. Фикс-3: coverage-инструкция Opus'у была узко ограничена IC/GIC/FIC+index.html — расширена на shared-<symbol>+<use>, голые <svg> во ВСЕХ dusk/*.ts, сверку с B3-census на предмет роста после F1/F2-ревворка. Финальный вердикт: fb1 A2·B1·C1·D1·E2·F2 + fb2 G1·H2·I2·J2·K1·L1·M3·N3·O2·P3·Q1·R2·S2·T1·U2·V1·W1·X1·Y1·Z2·AA1·AB1·AC1·AD1·AF1·AI1·AN1·AO1·AP1·AQ2·AR1·AS1·AT1 + fb3 AU1·AV1·AW1·AY1·BA0·BB1·BC1·BD1·BE1·BF1·BG1·BH1·BI1·BJ1·BK1·BM1·BN1·BO1·BQ1·BR1·BU1 + AX0·AZ0·BL0·BP0 (keep current, AX/BL доказано Tier-A-реконфирмом, AZ/BP — default) + BS/BT folded в AI1. СЛЕДУЮЩЕЕ: Opus O-FINAL — промпт в OPUS-QUEUE §O-FINAL-Ю7-ADDENDUM v3."
metadata: 
  node_type: memory
  type: project
  originSessionId: c493f430-f072-4023-89f6-41b47c43e49d
  modified: 2026-07-19T14:06:51.460Z
---

**O-FINAL В РАБОТЕ (Opus, 2026-07-19) — чекпоинт:**
- **BATCH 1 (fb1 A–F) ЗАДЕПЛОЕН** (`8cac639`, build `2026-07-19-2`, vitest
  35/35 + build ✓): A2 кован.крест→IC.crossAdd/crossSm+#btn-add+#btn-add-group;
  B1 свеча-окно→IC.window; C1 серп→IC.moon (noneMoonL/R НЕ тронуты=derive
  Batch4); D1 новолуние→повтор-модалка `data-repeat="none"`; E2 солнце-шпили→
  #btn-today; F2 руны→#btn-main-select. Стейдж поимённо (не -A).
- **ИНСТРУМЕНТ для след. окон (в scratchpad сессии, НЕ в репо):**
  `extract.mjs` парсит fb1/2/3-preview → `picks.txt` (все 163 pick, sz-16
  минифицир. однострочно, геометрия=byte-for-byte); `filter.mjs` → `verdict.txt`
  (58 вживляемых пиков). ПЕРЕСОЗДАТЬ в новом окне (scratchpad session-specific):
  скрипты тривиальны, логика = grep `data-pick="XXX"` → span `.sz-16` svg.
- **⚠ МУЛЬТИ-ГЛИФ роли (экстрактор берёт ТОЛЬКО первый svg — читать карту
  целиком):** K1 (selectEmpty+Checked пара), M3 (sortPriority+Order+Alpha ×3 —
  проверить, даёт ли карточка 3 варианта или 1 стиль), O2 (soundOn+soundOff
  «выключен»-ряд), P3 (gothEye ВКЛ полуприкрытое / ВЫКЛ открытое — two-state,
  ОБЕ кнопки: тулбар «только невыполненные» И subNotesAlwaysOpen 04:109/05:271).
- **BATCH-2a ЗАДЕПЛОЕН** (`e72dbbc`, build `2026-07-19-3`, 35/35+build ✓):
  13 ОДИНОЧНЫХ IC-билдеров fb2 (авто-пропагация): J2→dagger (=удалить
  заметку, кинжал-сквозь-свиток, семантика сверена по usage), L1→focusMode,
  I2→pin (pinSpike Tier-A НЕ тронут), V1→repeatMonthly, W1→snooze, Y1→template,
  Z2→tombstone, AB1→restore, AD1→quill, AN1→twinCoffin, AQ2→more, AS1→
  crossedSwords, AT1→skull (viewBox 20×22→24×24, весь svg). Стейдж поимённо.
- **BATCH-2b ЗАДЕПЛОЕН** (`e5c25a6`, build `2026-07-19-4`, vitest 35/35+build ✓):
  fb2 мульти-глиф/мульти-состоян. билдеры (авто-пропагация, геометрия
  байт-в-байт): G1→addNote/editNote (манускрипт на кованом валике),
  M3→sortPriority/sortOrder/sortAlpha (три башни / цепь-плашка / книга-А),
  O2→soundOn/soundOff (гравир. колокол + кованый перечёрк-OFF), K1→selectEmpty/
  selectChecked (рунный круг F2-язык; checked сохраняет rgba-подсветку по
  конвенции), AO1→g4/g4any/g4all (витражная роза — **fill-прогрессия DERIVE
  по задокумент. семантике: fb-превью дал ОДИН базовый глиф; ⚠ юзеру на
  визуальный ревью — единственное место интерпретации в батче**),
  AP1→promote/demote (стрела к парапету), H2→taskColorGlyph 04 (алх. фиал,
  ink→налитое зелье). Стейдж поимённо (dusk/01,04 + version).
- **ЮЗЕР ОТСМОТРЕЛ Batch-2b на проде (2026-07-19) — «всё правильно».** Только
  AO-роза наследование объяснено (g4=«как везде»/наследует глобал subAnyMode =
  контур без заливки; any=1 доля+ядро; all=все доли), derive принят.
  ДОП-ЗАПРОС юзера ВЫПОЛНЕН (`a262bd3`, build `2026-07-19-5`): кнопка приоритета
  ЗАДАЧИ (IC.spires, 04:183 btn + 04:2300 ⋯-меню) переведена на тот же
  трёхбашенный M3-глиф, что sortPriority → консистентность приоритет-кнопка ↔
  сортировка-по-приоритету.
- **BATCH-3 ЗАДЕПЛОЕН** (`fa26eac`, build `2026-07-19-6`, vitest 35/35+build ✓):
  fb2 кластер ВИД + унификация поиска, byte-for-byte: Q1→#btn-collapse-all
  (складень-триптих), R2→#btn-split-groups (бифорий со свечами), S2→все 3
  инлайн-лупы (поиск задач index.html:359 + архив 632 + гримуар 750) + синк
  IC.search builder (01:392, был unused — обновлён для консистентности реестра;
  живые = 3 инлайн-копии). Стейдж поимённо (index.html + 01 + version).
- **BATCH-5 ЗАДЕПЛОЕН** (`fe0f929`, build `2026-07-19-8`, vitest 35/35+build ✓):
  (a) **Тулбар-рельса раскладка = вариант А** (юзер выбрал): все `.side-rail
  .toolbar-group` → колонка, надпись сверху-слева, кнопки wrap flex-start;
  убран right-hug (flex-end) + `:has(nth-child(6))` column-switch (style.css
  ~8689). Чинит асимметрию «мало кнопок=вправо / много=влево» (репорт юзера).
  (b) **bulk-бар derives:** цвет→H2 (фиал-контур), дедлайн→B1 (свеча-окно),
  удалить→AT1 (череп); archive-select-bar restore→AB1 (восставший гроб).
  (c) **склеп-чекбокс** runeCircle/runeCircleChecked (01:375/383) → K1
  рунный-круг (был гекс-руна; гримуар-заметки 02:1045 уже были K1).
- **BATCH-6 ЗАДЕПЛОЕН** (`e3af4be`, build `2026-07-19-9`, vitest 35/35+build ✓):
  (a) **«В группу» НАРИСОВАН** (юзер: «нарисуй сейчас, gothic+detailed+intuitive»):
  стрельчатая арка-ниша (=группа)+замковый финиал + нисходящая стрела внутрь +
  ledger-строки на полу = собранные задачи (`openBulkGroupModal`, была двойная
  арка). Юзеру на визуальный ревью. (b) **3 старых черепа 20×22 → AT1** (clearAll
  тулбар, clearArchive, grim-bulk-delete). (c) **grim цвет → H2 фиал** (bulk-color
  + per-note 02:1173) + **task colorClear «Без цвета» → перечёркнутый H2**
  (был hex, рассинхрон с Batch-2b). (d) **archive «Отметить» inline гекс-руна
  → K1**. 
- **BATCH-7 ЗАДЕПЛОЕН** (`ce9959f`, build `2026-07-19-10`, vitest 35/35+build ✓):
  **AA1 нав-табы ×3** (index.html:71-92, byte-for-byte fb2-preview AA1): Задачи→
  гроб с кован.крестом+заклёпками+швами; **Архив→САРКОФАГ на постаменте с крестом
  (был generic-ящик) = снимает рассинхрон таб↔действие U2-крипта**; Гримуар→том
  с буквицами-ромбами+лента-закладка. Единый stroke-width 1.6 на тройку (был
  рассинхрон 1.7/1.8/1.7). Юзеру на визуальный ревью прода. AA-роль ЗАКРЫТА.
- **BATCH-8 ЗАДЕПЛОЕН** (`ee66395`, build `2026-07-19-11`, vitest 35/35+build ✓):
  **AF1 + AI1** (dusk/02-grimoire.ts, 13 глифов byte-for-byte, закрывает роли
  **AF·AI·BS·BT**). AF1: GIC.toc (196) → фигурная скоба писца (была прямая
  скрепка); tocClose/tocArch НЕ тронуты. AI1: 12 FIC-микрознаков (bold/italic/
  underline/strike/ul/ol/task/quote/code/hr/link/table) → скрипторий-ряд.
  **НЕ тронуты по ноте роли AI:** callout, align L/C/R, codeBlock, md, tbl*
  (add/del/grip/sigil) — «на планке». BS/BT = подмножество AI1, закрыты этим
  же вживлением (отдельного шага нет, как и планировалось). Юзеру на ревью прода.
  ⚠ X (дедлайн-модалка) ОТЛОЖЕН: время-пикер завязан на N3 (не готов) — развилка.
- **BATCH-9 ЗАДЕПЛОЕН** (`3eda643`, build `2026-07-19-12`, vitest 35/35+build ✓):
  derive-свип (dusk/01-core.ts). (1) **ФИКС РЕГРЕССА Batch-7:** 01-core:1008-1014
  клонировал SVG из `#nav-archive` таба в кнопку «Архивировать всё» → после
  AA1-саркофага таб тянул саркофаг вместо U2-крипты. Хак УБРАН, archive-all держит
  inline `<use href="#icon-archive"/>` (=U2, index.html:1615 симв.). Декаплинг:
  таб=навигация, действие=крипта. Закрывает derive «archive U2 во все точки»
  (04:186/2308 уже были `${IC.archive}`=U2). (2) **deadlineClear** (01:513):
  старый двойной-арк+✕ → B1(IC.window) приглушённое 0.5 + диаг.перечёрк.
  ⚠ ЮЗЕРУ НА РЕВЬЮ: перечёрк ОДИНАРНЫЙ (по «перечёрк»); был ✕ — если хочешь X,
  тривиальный флип. **Сепараторы уже ок** (sundial/moon=C1/pin=I). **ОТЛОЖЕНЫ
  развилки (НЕ выдумывал):** noneMoonL/R←C1 (03:2014, визуальный вес фланкеров
  «без группы»); цвет-фильтр-кнопка (index.html:497, огранённый кристалл уже
  готичен+семантика призмы=фильтр лучше H-фиала). **ЮЗЕР РЕШИЛ 2026-07-19 (все
  keep-current):** deadlineClear перечёрк = ОДИНАРНЫЙ (оставить); noneMoonL/R =
  плоские серпы (НЕ применять C1, детальный перегрузит фланкеры); цвет-фильтр =
  кристалл (НЕ H-фиал, призма семантически точнее для фильтра). Эти 3 derive-
  развилки ЗАКРЫТЫ — не переспрашивать.
- **BATCH-10a ЗАДЕПЛОЕН** (`95d782b`, build `2026-07-19-13`, vitest 35/35+build ✓):
  5 ролей byte-for-byte. **AV1** IC.sundial→часовая башня (пропаг. в сорт-по-
  дедлайну + сепаратор «С дедлайном» — AV1 перекрыл старый Tier-A sundial по
  вердикту, как и планировалось). **AW1** IC.spires→три готич.башни ОТДЕЛЬНЫМ
  глифом. **AY1** IC.restore + GIC.restore + grim-bulk-restore(inline index 889)
  →«отваленная крышка», единый глиф на задачи+гримуар. **BO1** GIC.tocClose→
  меч-в-кромку (AU-язык). **BU1** FIC.code/codeBlock/md/callout→ковка+розетка.
- **⚠⚠ РАЗВИЛКИ РЕШЕНЫ ЮЗЕРОМ 2026-07-19 (не переспрашивать):** (1) **AW = СПЛИТ**:
  приоритет-кнопка задачи (IC.spires) = AW1, а сорт-по-приоритету (sortPriority)
  ОСТАЁТСЯ M3 (декаплинг, сделан в 10a). (2) **AY = AY1** (перекрыл AB1 «восставший
  гроб» из Batch-2a; сделан в 10a). (3) **BU-инлайн-код = BU1** (перекрыл AI1-версию
  FIC.code из Batch-8; сделан в 10a).
- **⚠ BN = ГЭП (НЕ вживлён, спросить юзера):** `GIC.focus` (02:216) определён, но
  похоже МЁРТВ — фокус-тогл рендерит `GIC.focusLvl[grimFocus]` (699/701/1122), а
  сам GIC.focus нигде не используется (grep). BN-нота говорит «focusLvl не трогать».
  Т.е. свап GIC.focus невидим. Развилка: свапнуть мёртвый const / оставить / другой
  таргет? Реестр fb3:1302 маппит BN→GIC.focus(215) — расходится с реалом. По clause 4
  вынести юзеру.
- **BATCH-10b ОСТАТОК (РАЗВЕДАН ПОШТУЧНО, готов к быстрому вживлению) — picks
  BB1·BC1·BE1·BF1·BG1·BH1·BI1·BJ1·BK1·BM1·BQ1·BR1 + BD (parametric):**
  · **BB** ключ гор.клавиш: index.html 1574 (single). BB1 fb3:502.
  · **BC** рептилоид-глаз синка: index.html 1537 (СОХРАНИТЬ .brow/.lids/.ball +
    стейт-классы lid-up/lid-lo/tear/iris/pupil/glint, свап только внутренностей).
    BC1 fb3:557.
  · **BE** coffin-emblem: index.html 1625 `<symbol>` (АДАПТ: BE1-пути fb3:641,
    но БЕЗ inline `<defs>` — держать `url(#emblemFillShared)`/`#emblemStrokeShared`).
  · **BF** be-fil: index.html 363 + 369 (×2 идентичн., оба). BF1 fb3:680.
  · **BG** сплит-заголовки: `_SPLIT_ACTIVE_ICON`(03:269)+`_SPLIT_DONE_ICON`(03:270)
    + inline-дубли 07:397(свеча)/428(гроб) — свапать ТОЛЬКО ведущий свеча/гроб svg,
    НЕ хвостовой `.split-chevron`(=AU-меч). BG1 fb3:712(свеча)/723(гроб).
  · **BH** «Свой цвет-спектр»: index.html 189 (single; реал.current=hex-кристалл,
    BH0-превью=«приближение»). BH1 fb3:755.
  · **BI** dl-dur: стрелки ×4 index.html 1247/1251/1265/1269→BI1-стрелка fb3:791;
    углы ×4 index.html 1202/1203/1204/1205→BI1-угол fb3:800. (dl-dur-sep 1255 ромб
    = НЕ BI, не трогать.)
  · **BJ** grim-io крылатый свиток: index.html 819 + 897 (×2). BJ1 fb3:899.
  · **BK** тома fold: GIC.foldOpen(02:192)+foldClosed(02:193) + inline-дубли
    index.html 834(gt-closed=закр)/835(gt-open=откр) [grim-tpl-trigger].
    BK1 откр fb3:941 / закр fb3:953.
  · **BM** dividerFleur: 02:213 + CSS-дубли style.css 371/8134 (fleur-раздел.,
    mask/content — синкать). BM1 fb3:990 (держать КОД-враппер width=40 h=12 aria).
  · **BQ** grim-seg гроб: index.html 807 + 886 (×2). BQ1 fb3:1102.
  · **BR** grim-danger «Опустошить»: index.html 825 (single). BR1 fb3:1136
    (опрокинутая урна).
  · **BD** coffinSVG (01-core:137) = ПАРАМЕТРИЧ. билдер `coffinSVG(w,filled,cycle)`
    — переписать внутренности (кайма+гвозди+pattée-крест) СОХРАНИВ w-параметр и
    seal/cycle-анимации. BD1 fb3:606(пусто)/609(выполн). Самый заметный глиф →
    ОТДЕЛЬНЫЙ проход + отдельный ревью юзера на проде.
  keep-current (НЕ трогать): AX0·AZ0·BA0·BL0·BP0. BS/BT folded в AI1 (Batch-8).
- **⚠ Grim bulk ОСТАТОК = fb3-гримуар роли, очередь Batch-3 (fb3 AV-BU):**
  grim-bulk-archive урн (BQ грим-гроб), grim-bulk-restore, grim-bulk-export
  крылатый свиток (BJ). Юзер знает (напоминание), это НЕ derive — ждут fb3.
- **Цвет-гекс ОСТАТОК (H-семья, отдельный проход):** index.html 164/472/843/1633
  — часть = цвет-ФИЛЬТР (BP0 keep!), проверять по-штучно, НЕ трогать скопом.
- **ОСТАЛОСЬ (Batch-2b-хвост + статики-хвост + 3-fb3 + 4):**
  · **fb2 отложенное/не-01core:** P→gothEye (01, two-state ВКЛ/ВЫКЛ + ОБЕ
    кнопки: тулбар + subNotesAlwaysOpen 04:~109/05:~271 — ТРЕБУЕТ ЛОГИКИ
    two-state-swap на 2 кнопки, отложено отдельным аккуратным шагом),
    N→_qaRenderMenu (08, сигилы в строки, глифов там нет — добавить).
    · **fb2 статики-хвост (Q/R/S Batch-3; T/U ГОТОВЫ Batch-4 `6073607`
    build -7):** T1→тулбар ДАННЫЕ export/import/backup + 08 _EXPORT_TASKS_IC/
    _TOOLS_EXPORT/IMPORT/BACKUP_IC (⚠ _EXPORT_MD_IC ОСТАВЛЕН — отдельный
    markdown-глиф вне триады, юзеру на заметку); U2 крипта→symbol #icon-archive
    (archive-all + bulk) + builder IC.archive (per-task/⋯-шит/archive-all/
    archive-page-title). ⚠ Нав-таб «Архив» (index.html:78, `nav-archive`) =
    роль AA, НЕ тронут → пока рассинхрон (таб=старый ящик, действие=крипта) до
    AA1. ОСТАЛОСЬ: X1=арки времени дедлайн-модалка, AA1=нав-табы ×3 глифа. · **fb2→grimoire:** AF1→GIC toc-скоба, AI1→FIC-ряд
    (02:~3146, закрывает и BS/BT). · **Batch 3 = fb3 AV–BU** (19, grimoire/
    sync/misc, AU готов): AV/AW/AY/BB/BC/BD/BE/BF/BG/BH/BI/BJ/BK/BM/BN/BO/BQ/
    BR/BU. AX0/AZ0/BL0/BP0 keep-current — НЕ трогать. · **Batch 4 derives:**
    noneMoonL/R←C1, deadlineClear←B1+перечёрк, сепараторы, runeCircle/Checked←K,
    цвет-фильтр-кристалл←H, IC.archive U2 во все точки (08:292/04:186/04:2308).
  SVG байт-в-байт из verdict.txt (scratchpad, пересоздать) / превью. Карта
  «роль→куда» = OPUS-QUEUE §O-FINAL шаг 2-3.
- **РАЗВИЛКА РЕШЕНА (юзер 2026-07-19): консолидация = СИНК-ОБНОВЛЕНИЕ**, НЕ
  символизация. Каждый index.html/CSS-дубль рендерит идентичный ратифицированный
  SVG байт-в-байт («один глиф на роль» визуально, физически copy-paste остаётся),
  ноль регресс-риска, load-поведение не трогаем. НЕ переводить статики на
  shared `<symbol>`/JS-inject. Дубли (из O-FINAL-COVERAGE §CSS-слой): меч-шеврон
  style.css:6147 (AU), select-сосуд 6130 (K), уроборос 3136 (AZ keep), fleur
  371/8134 (BM); index.html hex-кристалл ×5 (H), меч ×5 (AU), череп ×4 (AT),
  полумесяц ×4 (C); + generic-chevron `.group-select` 789 + 3 native select +
  ☰ sync-log 8516 → AU-меч.
- **Шаг 0 coverage-сверка СДЕЛАНА** (`audit-v2/O-FINAL-COVERAGE.md`): IC 47 /
  GIC 20 / FIC 22, вся строка A-BU маппится без сирот. ГЛАВНЫЙ УРОК: первый
  проход искал только `<svg>` в JS/HTML и **пропустил CSS-слой иконок**
  (`background-image`/`mask`/`::before content`) — юзер поймал. Дочёс style.css
  обязателен в любом icon-аудите. Находки CSS-слоя: generic-chevron
  `.group-select` (мёртвый, hidden backing), ☰ sync-log, CSS-дубли меча/сосуда/
  уробороса/флёра. **Все 4 native `<select>` = `display:none` backing** →
  стрелки не видны, не трогать.
- **Гэпы (вердикта нет нигде): callout-типы GRIM_CO ×3 + note-шаблоны
  GRIM_TPL_IC ×4 → юзер решил ОСТАВИТЬ КАК ЕСТЬ** (2026-07-19). В REVIEW-QUEUE.
- **AU1 «Полуторный меч» ВЖИВЛЁН во все 15 JS/HTML дропдаун-точек + CSS**
  (коммит `d264b94`, version 2026-07-19-1, vitest 35/35 + build ✓). Юзер
  подтвердил: брать НОВЫЙ fb3-AU1, не старый. **Юзер ОТСМОТРЕЛ AU1 на девайсе
  — «всё ок»** → байт-в-байт подход + детальный fb-глиф ВАЛИДЕН для остальных
  ролей (даже на микро-размерах читается, отдельный упрощённый меч НЕ нужен).
- **ОСТАЛОСЬ (после Batch-1/2a/2b/AU):** P (two-state+2 кнопки), N (qa-сигилы),
  fb2-статики Q/R/S/T/U/X/AA+08-экспорты, AF/AI (grimoire GIC/FIC), Batch-3
  fb3 AV-BU (кроме AU готов + AX0/AZ0/BL0/BP0 keep), Batch-4 derives (noneMoon←C,
  deadlineClear←B, runeCircle склепа←K, цвет-фильтр-кристалл←H, archive U2 во все
  точки, сепараторы), + CSS-доп-проход (native-select стрелки→AU+appearance:none,
  ☰ sync-log, .group-select generic-chevron, CSS-дубли меч/сосуд/уроборос/флёр).
  Порядок и SVG байт-в-байт — `audit-v2/OPUS-QUEUE-2026-07-17.md` §O-FINAL +
  `O-FINAL-COVERAGE.md`.
- Грабли: `git add -A` затянул `.codex_tmp/` + 190 audit-PNG — чинено
  soft-reset+выборочный add+force-with-lease; `.codex_tmp/` теперь в .gitignore.
  ВПРЕДЬ стейджить иконочные файлы поимённо, НЕ `-A`.

---

**Ю7 ЗАКРЫТ (2026-07-18, эта сессия). Разбор жалобы юзера на Ю6/fb3 + фикс
процесса — читай ПЕРЕД любым будущим icon-проходом:**

**Жалоба юзера:** Ю6 (fb3-icons-preview.html, 27 ролей AU–BU) НЕ должен был
редизайнить иконки, по которым вердикт уже был. Юзер дал вердикт только на
21 роль, демонстративно пропустив 6: **AX (цепь-хэндл), AZ (уроборос-повтор),
BL (кинжалы закрытия), BP (галка цвета), BS/BT (FIC-наборы буквоформы/списки)**
— по его словам, эти уже были решены раньше. Плюс подозрение, что ещё
какие-то иконки в живом приложении вообще пропущены мимо всех трёх партий
(привёл пример: «Показать все заметки подпунктов»).

**Разбор — ПОПРАВЛЕН дважды в этой сессии, оба раза юзер поймал МОЮ ошибку
(не какой-то прошлой fable-сессии — та часть цела, см. п.8 ниже). Первая
версия ниже была неточной: «0» я подставил САМ, не из его слов буквально —
он лишь промолчал по 6 ролям + указал, что решения по этим же ролям УЖЕ есть
в предыдущих fb; попросил применить именно ИХ, а не голый «текущий код».
Второй, проверенный текстом прогон:**

1. **AX (`IC.drag`, 01:397, «хэндл DnD карточек/подзадач») — Tier-A,
   ПОВТОРНО подтверждён «НЕ трогать» в списке правок-2** (OPUS-QUEUE §O-FINAL,
   строка «Tier-A подтверждён вторым проходом правок-2: sundial, **drag-цепь**,
   ...» — дословно). Это и есть «уже решено раньше», буквально. Рифма с
   AQ2 (цепь) — только язык, AQ = другая роль (⋯-меню «ещё действия» на
   карточках/тулбаре-Данные/группах, `IC.more`), НЕ то же самое место. →
   **AX = keep current (Tier-A-решение подтверждено, ссылка есть)**.
2. **BL (`GIC.dismiss` 02:211 + search-x×3, «закрытие модалок Летописи +
   крестики очистки поиска») — тот же список правок-2 называет «dismiss»
   явно в Tier-A «НЕ трогать».** → **BL = keep current (тоже подтверждённое
   раньше решение, не голая догадка)**.
3. **BS (буквоформы B/I/U/S̶) и BT (списки ul/ol/task) — это НЕ отдельные
   роли вообще, это ПОДМНОЖЕСТВО уже полностью провердикченной роли AI
   (fb2, «AI · FIC — готизация микрознаков редактора», финальный пик AI1,
   «выбирается ряд целиком»).** Текст самого AI1-кандидата в fb2-превью
   дословно перечисляет: «B с ромб-серифами · курсив с флагами-засечками ·
   подчёрк с каплями · клинок-зачёрк с помелем (=S) · лощёные ромбы (=ul) ·
   РИМСКИЕ i·ii·iii (=ol) · арки-чекбоксы (=task, «язык K») · ...». Это
   BUKVALNO то же самое, что fb3 попыталось предложить заново под BS/BT —
   стопроцентный дубль уже принятого AI1. **Правильное решение: BS/BT
   НЕ вживлять как отдельный пункт — они уже покрыты вживлением AI1 (те же
   FIC-константы), никакого отдельного шага не нужно.**
4. **AZ (`IC.ouroboros`+`cycleReturn`, 01:492/503) и BP (цвет-фильтр-галка
   03:1440) — здесь честно: явного прошлого ГОЛОСОВАНИЯ (в отличие от AX/BL)
   НЕ нашлось, только исходная B3-icons.md пометка «мотив залочен» (AZ,
   концепция, не артворк) и полное отсутствие любого упоминания (BP,
   похоже реально впервые всплыла на Ю6-тотальном-проходе).** Тут «уже
   решённого варианта» подставить НЕЧЕГО — по инструкции юзера («там где
   решения нет — на нет и суда нет») default = **keep current**, без
   выдумки замены. Юзер может поправить точечно позже, если не так.
5. **Урок процесса (главный):** «эта роль без явного пика юзера» и «эта роль
   уже решена в прошлой партии под ДРУГИМ именем» — РАЗНЫЕ состояния,
   нельзя схлопывать в один ответ «0/текущий». Перед тем как писать
   Opus'у/памяти «оставить как есть» — сначала grep'ом искать, не решена ли
   та же роль уже под другим кодовым именем/буквой (derive-map, Tier-A
   confirmed-list, «язык X» пометки в самом же превью — они ПРЯМАЯ улика,
   не просто стилистическая рифма). Только если такого следа нет — тогда
   действительно «keep current, вопрос открыт».
6. **«Показать все заметки подпунктов» (04-tasks.ts:109, 05-edit-notes-
   groups.ts:271) — ПРОВЕРЕНО, это НЕ пропущенная иконка.** Кнопка использует
   `IC.gothEye` — БАЙТ-В-БАЙТ тот же constant, что и роль **P (Око фильтра,
   вердикт P3 = пара глифов вкл/выкл)**. Это значит редизайн P автоматически
   меняет и эту кнопку — раз constant общий. **НО derive-карта O-FINAL (см.
   OPUS-QUEUE §O-FINAL, строка "P→IC.gothEye...") называла только ОДНОГО
   потребителя (тулбар «только невыполненные»)** — второй потребитель
   (subNotesAlwaysOpen, 2 файла) не был явно вписан. Для P3 (два состояния,
   глиф меняется не только классом `active`) это важно: **обе кнопки нужно
   подключить к одинаковой two-state-swap логике**, не только тулбарную.
   Дописано в аддендуме для Opus ниже.
7. **Урок на будущее (сохранить в процессе):** «покрытие» (какая роль реестра
   покрывает какой(ие) реальный(ые) DOM-узел(ы)) надо явно валидировать
   grep'ом «конструктор иконки → все места использования», а не полагаться на
   память при рисовании. Это МЕХАНИЧЕСКАЯ, не творческая работа — годится для
   Opus, не для дорогого творческого Fable-времени.
8. **ФИКС-3 (юзер поймал ДВЕ ВЕЩИ отдельно, обе разобраны честно):**
   - Юзер заподозрил, что путаница «нет пика ≠ нет решения» означает потерю
     ЕГО прошлого fb2-вердикта прошлой fable-сессией. **Проверено: НЕ так.**
     Строка `G1·H2·I2·J2·K1·L1·M3·N3·O2·P3·Q1·R2·S2·T1·U2·V1·W1·X1·Y1·Z2·
     AA1·AB1·AC1·AD1·AF1·AI1·AN1·AO1·AP1·AQ2·AR1·AS1·AT1` идентична и в
     `OPUS-QUEUE-2026-07-17.md` (записана сессией правок-3, коммит `593861e`,
     ДО этой сессии) и в памяти до и после моих правок сегодня — ничего не
     терялось и не переписывалось. Путаница была моя, этой сессии, только
     про 6 fb3-ролей, где ОН сам не дал пика (сознательно, полагая их уже
     решёнными) — не про то, что его пики где-то пропали.
   - **Coverage-инструкция Opus'у (шаг 0 аддендума) была слишком узкой**:
     только `IC.*`/`GIC.*`/`FIC.*` + известные 75 index.html-статик. Не
     покрывала: 2 shared `<symbol>` (icon-archive, coffin-emblem) и все их
     `<use>`-точки; голые `<svg>`, вписанные прямо в `dusk/*.ts` вне
     IC/GIC/FIC; и возможность, что census B3-icons.md (2026-07-11) устарел
     после мобильного F1/F2-ревворка (снуз/шаблоны/promote-demote/quick-add
     typeahead/rail-digest могли добавить глифы, которых census не видел).
     **Расширено в ADDENDUM v3** (см. OPUS-QUEUE): доп. grep `<symbol`/`<use`,
     голый `<svg` по всем dusk/*.ts и всем .html в репо, сверка счётчиков с
     census (рост — ожидаем, не баг; но новый глиф идёт в список находок,
     не остаётся молча непокрытым).

**РЕШЕНИЕ ПО ДАЛЬНЕЙШЕМУ ХОДУ (принято в Ю7, не изменилось фиксом-2/3):** отдать
**весь остаток на Opus**, не тратить ещё лимит Fable. Причина: творческой
работы по иконкам больше не осталось — 63 роли уже нарисованы и
провердиктованы (fb1 6 + fb2 30 + fb3 27, минус 4 keep-current + 2
folded-в-AI1 из Ю7); то, что осталось (свод финальной строки, 4 keep-current,
BS/BT-фолд, coverage-сверка constant→usage-сайты, регистри-консолидация,
вживление) — механическая проверка и код, ровно то, в чём Opus не хуже и
дешевле. Промпт для запуска — в `audit-v2/OPUS-QUEUE-2026-07-17.md`
§O-FINAL-Ю7-ADDENDUM версия 2 (переписан после фикса-2) — вставить юзеру
целиком в новое окно Opus.

**ФИНАЛЬНАЯ СТРОКА ВЕРДИКТА (вся, для O-FINAL, всё выше — производное от
неё; ничего дальше не спрашивать юзера по иконкам, кроме реального нового
гэпа от coverage-сверки):**
`A2·B1·C1·D1·E2·F2 | G1·H2·I2·J2·K1·L1·M3·N3·O2·P3·Q1·R2·S2·T1·U2·V1·W1·X1·
Y1·Z2·AA1·AB1·AC1·AD1·AF1·AI1·AN1·AO1·AP1·AQ2·AR1·AS1·AT1 | AU1·AV1·AW1·AX0·
AY1·AZ0·BA0·BB1·BC1·BD1·BE1·BF1·BG1·BH1·BI1·BJ1·BK1·BL0·BM1·BN1·BO1·BP0·BQ1·
BR1·BU1`

**BS и BT ВЫЧЕРКНУТЫ из строки вердикта — это НЕ отдельные пункты.** Они
подмножество роли **AI** (уже AI1 в основной строке, «G1·...·AI1·...»).
Opus не создаёт для них отдельного шага вживления — просто вживляет AI1
(FIC-ряд, 02:3146+) целиком, это автоматически закрывает и B/I/U/S̶ (=BS),
и ul/ol/task (=BT). **AX0/AZ0/BL0/BP0 = keep current**, но с РАЗНОЙ
доказательной базой (см. разбор п.1-4 выше): AX/BL подтверждены явным
Tier-A-переподтверждением из правок-2 (текстовая улика есть), AZ/BP — просто
не найдено прошлого решения (default по недостатку данных, юзер может
поправить позже).

СЛЕДУЮЩЕЕ (после Opus O-FINAL): вернуться к плану — TWA → W2 → ... по
STRATEGY.md. Иконочная программа Fable закрыта целиком, буфер кредитов
свободен под drawer-nav (Ф-C) / календарь, если юзер даст добро отдельно.

---

**Ю6 ВЫПОЛНЕН (2026-07-18, сессия Ю6, для истории):** `audit-v2/previews/
fb3-icons-preview.html` — 27 ролей AU–BU одним файлом, обвязка = клон fb2
(клик-выбор, sz-clone, localStorage `fb3picks`, копи-строка). Состав: AU
меч-шеврон · AV sundial · AW spires↔M3 · AX drag-цепь↔AQ2 (ОТМЕНЕНО Ю7,
см. выше) · AY restore-унификация · AZ уроборос-змей+cycleReturn (ОТМЕНЕНО
Ю7) · BA penSound · BB ключ · BC рептилоид · BD coffinSVG-билдер · BE эмблема
· BF be-fil · BG сплит-заголовки · BH спектр-кристалл · BI dur-стрелки · BJ
крылатый ИО-свиток · BK фолд-тома · BL dismiss=J2-язык (ОТМЕНЕНО Ю7) · BM
флёр · BN grim-focus · BO tocClose=AU · BP галка-росчерк (ОТМЕНЕНО Ю7) · BQ
грим-гроб · BR emptyCrypt · BS/BT/BU FIC-наборы (BS/BT ОТМЕНЕНЫ Ю7, BU
остался). Рендер НЕ гонялся (запрет) — coverage-сверка теперь на Opus.

2026-07-17: недельный ресет; план пересобран, юзер утвердил. Operating doc =
**STRATEGY.md в корне** (таблица лимитов, Opus-порядок, План Б при продлении
Fable).

Ключевые решения юзера (история, ещё в силе): Adversarial-воркфлоу слит с
B11+B12 → Opus после 19.07. TWA — Opus сейчас, последовательно. xhigh только
Ф-B1 (уже потрачен). O-2 у Fable (движок его).

**Ф-А ВЫПОЛНЕНА (билды -13..-15):** long-press хинт — 3 механизма найдены и
закрыты (slop-отмена, B4-02 асинхронный render, Android contextmenu ~500мс).
Сорт-дропдаун клампы, Гримуар tpl-pop клампы. fa_bugs.mjs 15/15, vitest 35/35.
Ждёт финальной проверки юзера на девайсе.

**Ф-B1 партия №1 (коммит `d89c2c9`):** вердикт A2·B1·C1·D1·E2·F2.
**Ю2-2/Ю5 партия №2-4+правки (коммиты `1023f07`,`8e59c0f`,`aae0504`,`593861e`):**
финальный вердикт G1..AT1 (строка выше), мега-бриф O-FINAL дописан в
OPUS-QUEUE.

**СКИЛЛЫ этой (Ю7) сессии:** find-skills пропущен — тот же класс задачи
(icon-audit/process-fix), уже искали в сессиях Ю2-Ю6 этого плана, повтор
дал бы тот же результат. Загружен только код-навигационный инструментарий
(Grep/Read) — задача была верификация фактов + память + промпт, не дизайн/
код/бренсторм; icon-designer/impeccable/frontend-design не грузились
осознанно (нет нового творческого решения по иконкам в этой сессии — только
разбор+реверт+хэндофф).

Дальше после иконок: Opus по STRATEGY (O-FINAL → TWA → W2 → O-1 → O-5/B11-B12).
