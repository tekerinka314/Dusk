---
name: opus5-parity-replan-2026-07-24
description: "⭐⭐⭐ 2026-07-24 СМЕНА РАМКИ: Opus 5 = паритет с Fable, сплит моделей ОТМЕНЁН, $100 кредитов тратит Opus, порядок свободен. ПЕРЕСТРОЙКА ПЛАНА ВЫПОЛНЕНА 2026-07-24 → новая очередь живёт в STRATEGY.md (блоки 0-6); опционал переведён в очередь; аудит+харнесс ДО флагмана пуш+TWA."
metadata: 
  node_type: memory
  type: project
  originSessionId: a57aaf4a-6a7b-4d2f-aad0-32526d3f06d2
  modified: 2026-07-28T13:24:40.991Z
---

**Слово юзера 2026-07-24 (вечер, после закрытия иконочного хвоста O-FINAL):**

1. **Opus = 5-я версия.** Ничем не уступает Fable, кое в чём превосходит.
2. **Сплит моделей ОТМЕНЁН.** Всё, что план отводил Fable (дизайн-фазы,
   вкус-критичное, превью-циклы, Ф1/Ф2/Ф3/Ф4 по `STRATEGY.md`), теперь делает
   Opus. [[strategy-2026-07-16-fable-abundant]] в части «кто исполняет» —
   ИСТОРИЯ; фазы/классификация/definition-of-done оттуда остаются валидны.
3. **$100 кредитов сохранены**, доступны в любой момент, тратятся **именно
   Opus'ом, не Fable**.
4. **Порядок выполнения свободен.** Раньше очередь была искажена доступностью
   Fable (что-то ждало его лимита, что-то отдавалось Opus'у «пока дёшево»).
   Этого ограничения нет → выбирать порядок **по приоритету и уместности**, а не
   по модели. Если что-то было отложено только «потому что это к Fable» — можно
   брать сейчас.
5. **Следующий шаг = НЕ автоматом W2**, а **перестройка дальнейшего плана** под
   новую рамку. Пересобрать очередь из `remaining-scope-map` + `STRATEGY.md` +
   `FIX-PLAN.md`, снять из неё модельные оговорки, предложить юзеру порядок.

**Свежие входы того же сообщения (в очередь плана):**
- **БАГ (визуальный, Гримуар):** кнопка **TOC заходит на границы тулбара**, и
  **тогл тулбара тоже на грани**. Нужен systematic-debugging + фикс.
- **Отложенный творческий хвост O-FINAL — ГРИНЛАЙТ, можно закрывать сейчас:**
  archive-крипта де-алиасинг (`#icon-archive`) · N-сигилы уведомлений
  (`_qaRenderMenu`, 08) · X1 арки-времени дедлайн-модалки (был завязан на N3).
  Плюс возможный «CSS-доп-проход» — сперва проверить, не сделан ли Batch-9.

---

## ✅ ПЕРЕСТРОЙКА ВЫПОЛНЕНА 2026-07-24 — итог (источник истины = `STRATEGY.md`)

Юзер добавил: **бюджет больше не режет** — опционал (блок 5 старого расклада)
попадал в бэклог в основном из-за дефицита Fable-лимитов; теперь «не
обязательное, но очень желательное» → переведено в очередь. Развилки A/B отданы
мне со словами «скорость APK не приоритетна, выбирай глубже/качественнее».
Оценки лимитов юзер считает консервативными — ориентир, не ограничитель.

**Новый порядок (STRATEGY.md, блоки 0-6):** 0 баг тулбара Гримуара + творческий
хвост O-FINAL → 1 W2 (кириллица первой) → 2 Этап 5 Playwright (ПОДНЯТ: харнесс
как страховка для всего дальнейшего) → 3 B11+B12 adversarial + O-1 мотион →
4 W3 polish → 5 🎯 ФЛАГМАН Push Ф1→Ф2→Ф4→TWA APK→Ф3→Ф5 → 6 бывший опционал
(календарь · Гримуар Этап 4 · master-detail · стрик · Tauri · грим-фичи).

**Принцип, определивший порядок:** аудит + смоук-харнесс ДО крупнейшей новой
поверхности (пуш+TWA = крипта RFC 8291 + worker-cron + WebView-OAuth), а не
после. Полировать законченную базу дешевле, чем полировать под новым слоем.

**Факт разведки 2026-07-24 (грепом):** «CSS-доп-проход» из иконочного хвоста
почти мёртв — generic-chevron `polyline 6 9 12 15 18 9` не встречается нигде,
`style.css:8516` теперь sync-glyph (☰ sync-log нет). Осталась микро-проверка
native-select. Цели блока 0.2: `#icon-archive` шарится `index.html:610`
(archiveAll) и `:721` (склеп), symbol `:1671`; `_qaRenderMenu` =
`08-quickadd-export-init.ts:157`.

## ✅ БЛОК 0.1 ЗАКРЫТ 2026-07-24 (`81e5382`, build `2026-07-24-7`, юзер отсмотрел
«с первого взгляда норм»)
Баг «TOC/тогл заходят на границы тулбара Гримуара». Корень — **незавершённая
миграция отступов** из `afa7c36`: когда угловые сигилы стали вертикальной колонкой
(`right 12…42px`, вертикаль 7…109px), правый отступ подняли до 42px ТОЛЬКО у
заголовка; divider/fmt-bar/body остались на 22/18/20px → TOC лежал на тулбаре
24×27px. Фикс — жёлоб **52px на ≥641px** для title/divider/body/fmt-bar,
зеркальный левому (руна 12…42 + 10px воздуха).
**Уроки (переносимые):**
- ⚠ **Каскад:** override обязан лежать ПОСЛЕ базовых деклараций — при равной
  специфичности поздние шорткаты `margin`/`padding` бьют ранний `@media`.
  Первая попытка не изменила НИЧЕГО именно поэтому (цифры замера не сдвинулись).
- Замер-харнесс с ВЕРДИКТОМ (PASS/FAIL по перекрытиям+зазору) сильнее скриншота:
  `D:/tmp/pw/_grimbar.mjs` (4 вьюпорта, playwright-core + системный Chrome,
  `switchPage('notes')` — ключ страницы 'notes', НЕ 'grimoire').
- Сравнивать надо **контентный край** (right − paddingRight), иначе textarea даёт
  ложный FAIL.
**Оба побочных хвоста ЗАКРЫТЫ** (`987ef3c`, build `2026-07-24-8`, юзер сказал «мне
норм, но если считаешь желательным — фикси»): (1) касание заголовка на мобиле —
спецслучай убран целиком, базовый отступ заголовка 52px симметрично левому,
мобильный override 42px удалён → зазор 10px на ВСЕХ тирах одним числом;
(2) третья строка тулбара в полосе ~735-849 — ширина выкуплена из ритма между
группами (`.fmt-cluster` gap 9→6, padding 8→6) ТОЛЬКО в этой полосе, жёлоб не
тронут. Граница переноса: между 349 и 357px ширины кластера. При 700px тулбар
трёхстрочен и БЫЛ таким до фикса. Замер строк: `D:/tmp/pw/_grimrows.mjs`.
⚠ **Грабля каскада повторилась второй раз** на `.fmt-cluster` — оно объявлено НИЖЕ
блока жёлоба, `@media` выше не применялся вовсе (замер не сдвинулся ни на пиксель).
Правило: перед правкой override'ом СВЕРИТЬ номер строки базовой декларации.

## ✅ БЛОК 0.2 ЗАКРЫТ 2026-07-25 (кроме развилки chronicle) — 6 билдов на проде
Полное состояние — в `audit-v2/O-FINAL-XVOST-HANDOFF.md`, секция «Отложенный
творческий блок» → «ОСТАЛОСЬ». Здесь только рамка.
**Вердикты юзера 2026-07-25:** приоритет = **PA «башня-ранг»**; часы = **T1
«Орлой»** (T2 роза отклонена, AV1 был отклонён ранее как «те же часы»); сигил
даты в квик-эдде **тоже переведён на T1-lean** — вскрылась коллизия «две башни в
одном меню», старый fb2-вердикт по N3_DATE переопределён.
**Задеплоено И ОТСМОТРЕНО ЮЗЕРОМ НА ПРОДЕ 2026-07-25:** `e7527b9` build -1 свип
часов T1 (три породы часов → одна) · `d9e52a2` build -2 сигилы квик-эдда
(`.qa-dot` → `.qa-sig` 15px) · `48192cf` build -3 X1 (степперы ×8 + ланцетная
скрижаль в пикере даты). Вердикт: «прошли, хоть 2 и 3 почти никак не отличаются
от оригинала, но да ладно». По -3 это ожидаемо (X1 был тюнингом подачи, не
редизайном); по -2 есть гипотеза реальной потери — цвет приоритета переехал с
8px ЗАЛИВКИ на 15px ТОНКУЮ ЛИНИЮ, площадь цвета упала в разы. Лечение описано в
хэндофф-доке, делать ТОЛЬКО по слову юзера.
**✅ ВЖИВЛЕНИЯ ЗАКРЫТЫ 2026-07-25 (билды -4/-5/-6, ждут прод-ревью):**
1. `a2db1e2` build -4 — **нав-табы ×3 на пиксельной сетке** + иконка у заголовка
   «Архив». Болели все три (не только архив): рендер 13×13 при viewBox 24/20×26
   клал каждый штрих между пикселями. Лечение — `viewBox 0 0 13 13` (1 единица =
   1 пиксель), линии на полупиксельных центрах, штрих ≥1px, иерархия с толщины на
   opacity, субпиксельные заклёпки → метки 1×1px, детали разведены на шаг 2px.
   ⭐ **Инструмент на будущее:** `D:/tmp/pw/_navpx.mjs` растеризует глиф ровно в
   13×13 и печатает альфу ASCII — видно, сплошной штрих или каша. Дёшево, точнее
   скриншота, переиспользовать при любой правке мелких иконок.
2. `daaee51` build -5 — **свип песочных часов на T1**: пикер времени `08:1058` →
   `IC.sundialLean` (ссылкой, не копией) · очистка времени `08:1112` → погашенный
   циферблат под × · `GIC.hourglass` (штамп «начертано») снят, потребитель
   `02:1186` → `IC.sundialLean`.
3. `7bfe1a9` build -6 — **партия 4, де-алиасинг архива**: `#icon-archive` снят,
   вместо него `icon-archive-all` (A2 оссуарий) и `icon-archive-sel` (B1 рунный
   круг); `IC.archiveAll` — не копия арта, а такая же обёртка `<use>`, поэтому у
   глифа одна копия. `IC.archive` (U2) остался одиночному «В архив».
`d701d71` — хэндофф приведён к факту.
4. **ДОБИВКА `build -7`** — нав-таб «Архив» всё равно читался кривым. Причина не в
   сетке, а в САМОЙ структуре: косые грани с уклоном 1 к 5 дают лестницу полутонов.
   Арт переработан по слову юзера в **ортогональный саркофаг-ларь** (плита 8×2 /
   корпус 6×6 с греческим крестом / цоколь 10×2, square+miter). Роль `AA1b`.
   ⭐ **Правило:** на 13px запрещены не только тонкие штрихи, но и ПОЛОГИЕ
   ДИАГОНАЛИ — либо уклон ≥45°, либо ортогональный силуэт.

## ✅ ПАРТИЯ 7 ЗАКРЫТА 2026-07-25 — заказ вечера выполнен целиком (билды -10, -11)
Превью `86d3770` → вердикты юзера: приоритет = **два регистра** (контур остаётся
в квик-эдде, заливка идёт в маркеры), нав-таб = **путь (б), 15px, все три**.
- **7а `b3c000f` build -10:** кружки приоритета → **залитые башни 15px во всех
  пяти точках**, арт раздаётся **МАСКОЙ** (`--tw-*` в `:root`), поэтому НИ ОДНОЙ
  правки в разметке и JS — цвет остался обычным `background`.
  ⚠⚠ **Грабля:** SVG в `mask` грузится как отдельный документ и **без `xmlns` не
  парсится** — элемент исчезает молча, computed style при этом корректен. Ловится
  только `new Image()` по URI из computed style.
  Ещё: маска обрезает `box-shadow` → свечения меток сняты, ореол массовых кнопок
  переехал на кнопку через `[data-prio]`.
- **⚠ 7б ОТКАЧЕН ЮЗЕРОМ. Финал = `96e1b29` build -12:** вердикт «иконка для задач
  полное говно, давай (а) орто». Подъём трио на 15 отменён целиком (revert), все
  вернулись на сетку 13; гроб «Задачи» перерисован ОРТОГОНАЛЬНО (роль `AA1d`:
  плечи и стопы ступенькой, косых граней нет). ⭐ Плюс **руина «без приоритета»
  переработана** (первая была примитивна и не опознавалась): теперь ЦИТИРУЕТ
  башню — те же двойные стены со щелью и цоколь, но верх срезан, перемычка
  сорвана и свисает влево, справа рухнувший блок, у подножия щебень. Руина
  поставлена ВО ВСЕ точки «без приоритета»: полумесяцы C1 в сетке формы и пилюле
  модалки СНЯТЫ, прочерк в панели массовых действий тоже (цвет берётся из
  `currentColor` кнопки).
- **Руина переработана ВТОРОЙ раз — `0db2aa3` build -13 (финал):** «не узнаётся
  рядом с тремя рангами». Обе прежние версии ломали НЕ ТО: узнавание держится на
  цоколе, перемычке-архитраве шириной 5 и двойных стенах со щелью — их и ломали.
  Теперь КОРПУС ЦЕЛ и совпадает с рангами символ в символ, срезан по диагонали
  только ШПИЛЬ. ⭐ **Переносимое правило: «разрушенный/пустой» вариант знака ломает
  ОТЛИЧИТЕЛЬНУЮ деталь, а общую часть силуэта сохраняет нетронутой.**
- **⚠ Третья итерация руины ОТКАЧЕНА (`6c433ff` build -15). ФИНАЛ = вариант -13,
  ровный срез.** Ниже — что пробовали и почему отвергнуто. `4065ff1` build -14: «не вижу ни обломков
  шпиля, ни его надломанной части». Ровный срез читался как ЦЕЛАЯ деталь. Лечение:
  верх обрывается ЗУБЦАМИ разной высоты через выщербину (силуэт слома, а не среза),
  у подножия лежит отвалившийся КЛИН шпиля — та же треугольная форма, что у целых
  башен, только опрокинутая, плюс мелкий скол слева. ⭐ **Правило-2: слом на мелком
  кегле показывают зубчатый край + узнаваемый обломок рядом; ровная диагональ
  читается как замысел, а не как повреждение.**
- **⚠ УРОК ТЕМПА (юзер, жёстко):** на эти два пункта ушло поллимита — «непозволительно
  много». Меньше превью-циклов и растровых итераций, больше решений с первого
  захода. Не разворачивать полный brainstorming-ритуал на точечной иконочной правке.
- ~~7б `f579f55` build -11~~ (откачен): нав-трио переобводилось на **нечётную канву 15**
  (центр 7.5 = центр пикселя → осевой штрих не расщепляется; 16 не годится
  именно поэтому). Гроб `AA1c` = 45°-грани + шов крышки + крест pattée с
  заклёпками + гвозди. Переехали **все пять** потребителей `<symbol>` разом
  (нав ×2, заголовок «Архив», сегменты Гримуара ×2) + три правила размера в CSS.
  Замер вёрстки на 360/414/768/1280 — переполнения нет.
- ⭐ **Инструмент:** `D:/tmp/pw/_pxprobe.mjs` — обобщение `_navpx.mjs` на любой
  глиф и любой размер (`--in json --keys a,b --sizes 7,13,15`); метрика «пикселей
  полного покрытия» = дешёвый детектор «глиф умрёт в этом размере». Именно она
  доказала, что контурная PA не переносится в маркеры (0-1 px даже на 15).
## ✅ ПРОД-РЕВЬЮ БИЛДОВ -4…-16 ПРОЙДЕНО 2026-07-25 (вечер)
Вердикт юзера: **принято ВСЁ**, кроме руины «без приоритета» («проработка и
детализация не на высшем уровне, хочу читаемее сломанной, с обломками, но строго
в семье трёх рангов»). Иконочный хвост O-FINAL закрыт целиком — переоткрывать
нечего.
**Руина ред.4 — `0b9e462` build -17, вердикт «не идеально, но сойдёт, ресурсов
больше не тратить» → ТЕМА ЗАКРЫТА, не возвращаться.**
⭐ **Диагноз, ради которого стоило три итерации:** ред.3 (ровный срез) читалась
как ЦЕЛАЯ деталь, потому что силуэт оставался СИММЕТРИЧНЫМ. Сигнал разрушения
даёт асимметрия + мусор у подножия, а не линия среза. Ред.4: левая стена уцелела
на 3 клетки выше архитрава, правая обрушена вровень с ним (разновысокий обрыв),
справа два обломка разной плотности, слева щебень; в контурном регистре —
рухнувший клин кровли в проёме между стенами. Корпус (стены 8.4/15.6, перемычка
y12, цоколь, плинтус) цитирует ранги символ в символ.
⭐ **Метод, окупившийся:** три кандидата → растровая проба `_pxprobe.mjs` в 15px →
выбор по числу пикселей полного покрытия (35 против 27 у ред.3, medium = 32).
Кандидат «пролом в стене» отпал — дробил стену на полутона. Рисовать на глаз в
мелком кегле дороже, чем прогнать пробу.
⚠ Правка руины = ДВА регистра синхронно: маска `--tw-none` в `style.css:69` (5
точек) + контур `IC.prioRankNone` (`01-core.ts:704`, квик-эдд) + ОБА в
`gen-fb4-v3.mjs` (иначе превью разъедется с приложением).

## ⏳ БЛОК 1 (W2) НАЧАТ 2026-07-25 — пункт 1 из 9 закрыт
**W2-1 V2-B1-11 кириллица квик-эдда — `58f6d36` build -16.** Корень: `\b` БЕЗ
флага `u` определяется через ASCII-`\w`, поэтому справа от русской буквы границы
слова НЕТ вовсе → `!высокий` не матчился ни в конце строки, ни в середине (не
«частично работало» — не работало никак). Лечение: `(?![\p{L}\p{N}_])` + флаг
`u`, `i` снят (\p{L} покрывает регистр). Заодно тело тега `[\wа-яёА-ЯЁ]` →
`[\p{L}\p{N}_]` («*café» резалось до «*caf»), подсветка и `extractTags` сведены
на ОДИН литерал `_TAG_BODY_RE`. `%date` (`\S+`) багу не подвержен.
⭐ **Переносимое:** в JS `\w`/`\b` остаются ASCII даже с флагом `u` — юникодная
граница делается только lookahead'ом по `\p{L}`.
⭐ **Харнесс:** `tests/quickadd-cyrillic.test.mjs` — первый тест, поднимающий
модули 01-08. Они трогают разметку на верхнем уровне (05 падает на
`getElementById('note-modal-input')`), поэтому: сперва `document.body.innerHTML`
из настоящего `index.html` (со срезанными `<script>` — happy-dom пытается их
грузить и сыплет NotSupportedError), затем `await import()` цепочкой + обяз.
`src/sortable-global.js` (иначе unhandled `Sortable is not defined` из 07).
Базлайн `tsc --noEmit` на этом репо = **27 ошибок и до правки** (проверено
worktree'ом на HEAD) — «зелёный tsc» тут значит «счётчик не вырос».
**W2-2 контраст AA (V2-B5-01 + V2-B5-12) — `7ab5945` build -18, ЖДЁТ ПРОД-РЕВЬЮ.**
⭐ **Главный урок: прежний зонд врал.** `s3_contrast.mjs` читал только
`getComputedStyle().color` и игнорировал `opacity` элемента — а `.dl-absolute`
красится при 0.80. Замер по реальным пикселям с учётом каскада opacity дал
**СЕМЬ фейлов вместо двух заявленных в аудите**: просрочено 1.90/2.29,
критично 3.05/4.03 (аудит его вообще не видел), срочно 4.45, скоро 3.96,
подсказка квик-эдда 3.72 (её глушат 0.85). Течь была не в оттенке, а в том, что
гашение накладывалось ПОСЛЕ настройки токена.
Правки — только светлота, оттенки удержаны: `--deadline-over` #a01525→#ea5d6d,
`--deadline-critical` #e03060→#e76186, `--text-muted` #9068c0→#a07ec9 (4.7→6.1,
запас на светлые области фона), `.dl-absolute` opacity .80→.92 + 9.5→10px.
Итог 0 фейлов, иерархия primary 17.2 > secondary 7.0 > muted 6.1 цела.
⭐ **Инструменты:** `D:/tmp/pw/b1/s3_contrast3.mjs` — opacity-aware зонд (свой сид
на ВСЕ статусы дедлайна: over/critical/urgent/warn/ok/live; селектор чипа =
`.deadline-tag`, НЕ `.task-deadline`); `tests/contrast-tokens.test.mjs` — вечный
замок, считает ratio из `style.css` на ЗАМЕРЕННЫХ фонах чипов, а не на чёрном.
⚠ Красные статусы после подъёма сблизились по светлоте — так и задумано в самом
аудите («unify overdue on the brighter red»); различают их подложка, жирность и
слово. Если юзер скажет «одинаковые» — разводить подложкой, не оттенком.
**W2-3 disabled-скин плавающих меню (V2-B4-05) — `076247b` build `2026-07-26-1`,
✅ ПРОД-РЕВЬЮ ПРОЙДЕНО 2026-07-26 («теперь читается погашенной»).** У общей обшивки `.snooze-menu` не было НИ ОДНОГО правила для
`:disabled` → мёртвый пункт панели синка выглядел живым и глотал клик. Правки:
`opacity .38` + `--text-muted` + `cursor:not-allowed` + приглушённый svg;
pointer-events НЕ снят (иначе `not-allowed` не рисуется — в отличие от идиомы
`.repeat-btn`); все hover/active обшивки закрыты `:not(:disabled)` (голый
`:hover` матчит и disabled-кнопку). Разметка: единый литерал `DEAD_ITEM =
' disabled aria-disabled="true"'` в `11-sync-ui`. ⭐ Решение: нативный `disabled`
ОСТАВЛЕН (гарантирует блок клика + `_apMenuKeyNav` `03-render:1165` пропускает
строку); размен — строка недостижима с клавиатуры, для меню без roving-tabindex
дешевле, чем гасить клик по aria-disabled в диспетчере.
⭐ **Замок/зонд:** `tests/floatmenu-disabled.test.mjs` (контракт по исходникам:
CSS-правило + отсутствие незакрытых hover + парность атрибутов);
`D:/tmp/pw/b1/w2_b405_disabled.mjs` — вердиктный зонд по реальным пикселям dist
(desktop-девайс задаётся объектом, в `DEVICES` только мобильные), проверяет
hover-перекраску и парковку фокуса после ArrowDown ×3.
⚠ **Билд -18 (контраст AA) явного вердикта юзера ТАК И НЕ ПОЛУЧИЛ** — спросить при
случае; если скажет «два красных статуса одинаковые», разводить ПОДЛОЖКОЙ, не оттенком.
**W2-4 a11y карантина (V2-B5-04) — `b4be4bc` build `2026-07-26-2`. ПРОД-РЕВЬЮ НЕ
БУДЕТ: юзер 2026-07-26 «не хочу это чекать» (нужен реальный конфликт синка) →
принято на основании зонда 6/6 PASS. Тема закрыта, не переспрашивать.**
Оверлей конфликтов строился мимо `openModalWithFocus`: без id → Esc звал
`dismissModalById(undefined)` (no-op), без `aria-labelledby` → безымянный диалог,
фокус не входил, Tab уходил за оверлей. Лечение: `overlay.id='quar-overlay'` +
labelledby на заголовок + открытие через хелпер + регистрация в `MODAL_CLOSERS`
ИЗ 11 (рантаймовый оверлей в статический реестр 05 не попадает).
⭐ **Три неочевидности:** (1) триггер надо запомнить ДО `closeFloatMenu()` —
десктопное меню (не action-sheet) фокус анкеру НЕ возвращает, `activeElement`
становится `<body>`; фоллбэк = `_glyph()`; (2) `closeQuarantine` анимирует через
`closeModalWithAnim` и сразу СНИМАЕТ id — иначе переоткрытие внутри ~250 мс
анимации оставит второй `#quar-overlay` и `getElementById` вернёт умирающий;
(3) `_refreshQuarOverlay` перестраивает список НА МЕСТЕ вместо close+open — иначе
каждое «Восстановить» рвало ловушку фокуса и гоняло анимацию.
⭐ **Замки:** `tests/quarantine-modal-a11y.test.mjs` (контракт по исходникам, 7
кейсов) + `D:/tmp/pw/b1/w2_b504_quar.mjs` — зонд по dist (имя диалога, фокус
внутрь, Tab не сбегает за 14 нажатий, 3→2 строки без потери фокуса, Esc,
возврат на `sync-glyph-btn`): PASS. Тесты 65/65, tsc 27 (базлайн).
**W2-5 focus-visible полей (V2-B5-03) — `37e55ec` build `2026-07-26-3`, ЖДЁТ
ПРОД-РЕВЬЮ.** ⚠⚠ **АУДИТ СОВРАЛ ВТОРОЙ РАЗ ТЕМ ЖЕ СПОСОБОМ.** Заявлено «кольца
нет, border-color не меняется» — замер опроверг: индикатор есть у КАЖДОГО поля,
но на ОБЁРТКЕ (`.row` / `.search-wrap` / `.archive-search-wrap`
`:focus-within`), а у самого `<input>` `border:none`. Аудит читал не тот
элемент — ровно как контрастный зонд игнорировал `opacity`.
⭐ **Правило: перед фиксом a11y-находки СНАЧАЛА замер, потом код; проверять не
только элемент из находки, но и ближайшего предка, который реально красит.**
Настоящий дефект: 2.08:1 у быстрого ввода и `.form-sub-input` при 3:1 (WCAG 2.2
§1.4.11) + шесть разных фиолетовых литералов на одну роль. Лечение — один токен
`--focus-ring: rgba(180,100,255,.85)` (= цвет существующего кнопочного кольца,
поэтому кнопки и поля сошлись в одну идиому), 10 правил переведены В СЕБЕ, не
override-блоком (каскадная грабля этого файла). Итог 2.08 → 4.55/4.63:1.
⭐ **Замки:** `tests/focus-ring.test.mjs` (11 кейсов; ⚠ в нём две тонкости —
`rule()` берёт ПОСЛЕДНЕЕ вхождение = победителя каскада и имеет lookahead
`(?![\w-])`, иначе `.modal-input:focus` матчит блок-глушилку
`:focus-visible`), `D:/tmp/pw/b1/w2_b503_probe.mjs` (зонд по dist: композит
фонов вверх по дереву с учётом opacity; ⚠ читать стиль ТОЛЬКО после ~350 мс —
`transition: border-color` отдаёт старое значение и зонд врёт «фокус ничего не
меняет»). Порядок табов проверен отдельно, здоров.
**Вердикт юзера по -3: «слишком ярко» → `7070f52` build `2026-07-26-4`:
`--focus-ring` = `rgba(172,92,250,.75)`, замер 4.55 → 3.48:1.** Порог 3:1 держим
с запасом 0.48; кандидат 3.05 отвергнут как впритык (округление рендера +
светлые участки фоновой картинки). **Билд -4 ПРИНЯТ юзером 2026-07-26 («ок»)
→ кольцо фокуса закрыто, разводка по вводу не понадобилась.**
**Билд -18 (контраст AA) ПРИНЯТ юзером 2026-07-26** — вопрос закрыт, красные
статусы разводить подложкой не понадобилось.
**W2-6 inert Параметров (V2-B5-02) — `b721771` build `2026-07-26-5`, ЖДЁТ ПРОД-
РЕВЬЮ (визуально ничего не меняет — проверять клавиатурой).** ⭐ Здесь аудит
ПРАВ, замер подтвердил: `#extra-fields` при `max-height:0` держал 38
фокусируемых / **31 таб-стоп, все ДО кнопки «Параметры» (стоп #37)**. Лечение:
`inert` в разметке (дефолт свёрнут) + снятие в `toggleExpand` до замера
scrollHeight + возврат при закрытии + снятие в `loadUiState` при восстановлении
открытой. ⚠ Неочевидность: фокус, оказавшийся ВНУТРИ гасимого поддерева, при
`inert` улетает на `<body>` → паркуем на `#btn-expand` ДО установки атрибута.
Фоллбэк без нативного inert — `visibility:hidden`, но только по окончании
анимации (мгновенное скрытие сорвало бы схлопывание).
⭐ **Замки:** `tests/extra-fields-inert.test.mjs` (5 кейсов, red-green проверен
стэшем — 4/5 краснеют без фикса; поднимает модули 01-08 по рецепту
quickadd-cyrillic и реально дёргает `toggleExpand`), зонд
`D:/tmp/pw/b1/w2_b502_inert.mjs` (реальный Tab-walk по dist, 6/6 PASS: свёрнута
0 стопов · развёрнута 31 · повторное закрытие возвращает inert и паркует фокус ·
бут `expandOpen=1` открыт и не inert). Тесты 81/81, tsc 27.
🔻 **Хвост из самой находки (НЕ сделан):** тот же паттерн «max-height-схлопывание
с фокусируемыми внутри» возможен у групп, секции подпунктов и месяцев архива —
свипнуть тем же зондом.
**W2-7 reduced-motion тоста (V2-B5-07) — `c67c582` build `2026-07-26-6`.** Аудит
прав, замер подтвердил: под `reduce` тост играл `toastAppear 0.3s` с
translateY+scale. Лечение — RM-блок с opacity-only кейфреймами
(`toastFadeIn`/`toastFadeOut`, `--dur-quick`). ⚠ **transform НЕ обнулять:**
`translateX(-50%)` у тоста = центрирование, `transform:none` увёл бы его вправо
на полширины. Замки: `tests/toast-reduced-motion.test.mjs` (4 кейса, red-green
3/4; ⚠ кейфрейм в одну строку → регекс с одним уровнем вложенности, а не
`\n\s*\}`), зонд `D:/tmp/pw/b1/w2_b507_toast.mjs` (сэмплит computed transform во
времени в обеих фазах и под обеими настройками; 3/3 PASS).
**W2-8 RU-копирайт карантина (V2-B4-07) — `686ef2f` build `2026-07-26-7`.** Карта
(recType → поле → RU) с общим разделом `*`, незнакомое поле деградирует в «поле
«X» задачи»; формулировки видов конфликта переписаны; ЗНАЧЕНИЯ тоже переведены
(приоритет/повтор словами, булево «да/нет», дедлайн датой, группа названием) —
это ВТОРАЯ течь, которой в находке не было, нашлась зондом.
⚠⚠ **Грабля: `formatDeadlineAbsolute` на кривой форме НЕ бросает, а возвращает
«NaN undefined NaN»** (у `mode:'date'` value = строка `'YYYY-MM-DD'`, не
timestamp). Проверять РЕЗУЛЬТАТ форматтера, а не только исключение.
⚠ Пришлось намеренно переписать пин V2-B6-04 в `tests/quarantine-preview.test.mjs`
(булево «нет» вместо «false»); деградация неизвестного объекта в JSON сохранена —
терять содержимое проигравшей стороны нельзя, его снесёт GC.
⭐ Замки: `tests/quarantine-copy.test.mjs` (9 кейсов; **поднимает цепочку 01-11**
в happy-dom — 10/11 грузятся тем же рецептом, что 01-08), зонд
`D:/tmp/pw/b1/w2_b407_copy.mjs` (12 строк журнала всех видов, 7 детекторов течи).
Тесты 94/94, tsc 27.
## ✅ W2-9 ЗАКРЫТ 2026-07-26 → **ВЕСЬ БЛОК 1 (W2, 9/9) ЗАКРЫТ**
`fb76059` build `2026-07-26-8`, ЖДЁТ ПРОД-РЕВЬЮ. Все четыре пустых состояния +
поиск Гримуара переведены на утверждённые строки; два глифа нарисованы (EM1
незажжённая свеча в подсвечнике с дымком, EM2 запечатанный гроб = силуэт
`GIC.coffin` + перевязь + восковая печать с крестом), два переиспользованы
(`#icon-scrying`, AA1b), CTA получила перо. Арт пера вынесен в
`<symbol id="icon-quill">`, `GIC.quill` переведён на `<use>` (копии нет).
⭐ **Переносимое:** `updateVisibility` перекидывает заголовок + подстроку + `href`
глифа ОДНИМ куском — иначе глиф врёт о причине пустоты (свеча «задач нет» против
шара «ничего не нашлось»). Руна пустого состояния была `font-size`-символом:
став глифом, она требует поправки ФИНАЛЬНОГО кадра `emptyRuneIn` (кончался на
opacity 0.20 — глиф тонул). `ornamentPulse` в проекте НЕ ОПРЕДЕЛЁН (мёртвое имя
анимации, no-op) — не считать его живым.
⚠ Правка АРТА внутри `<symbol>` меняет хэш всего блока `defs` в реестре иконок →
`icon-inventory.mjs --update` ПОСЛЕ последней правки арта, иначе ратчет красный
уже после коммита.
⭐ Замки: `tests/empty-state-voice.test.mjs` (5 кейсов по исходникам), зонд
`D:/tmp/pw/b1/w2_b204_empty.mjs` (5 состояний по dist, 22/22, скриншоты в
`audit-v2/shots/w2-9`). ⚠ В зонде две грабли окружения: поиск Гримуара =
`#notes-search-box` (не grim-*), а плита «Все выполнены» требует
`localStorage.isFiltered='1'` (иначе выполненные видимы → не noVisible).
Дальше по `STRATEGY.md`: блок 2 (Playwright в репо) или блок 3.5 (язык на
плотную готику) — порядок за юзером.

## ⏭ (ИСТОРИЯ) W2-9 — вердикты и точки правки, по которым делали
Юзер отвердиктил 2026-07-26 (три развилки, выбор **2 / 1 / 1**):
1. **Голос = ПЛОТНАЯ ГОТИКА** (не «в тон Гримуару»). Утверждённые строки:
   · нет задач — «Алтарь пуст» / курсив «Ни одного обета ещё не дано» / кнопка
     «Дать первый обет»
   · все выполнены — «Все обеты исполнены» / «Ночь может забрать своё»
   · ничего не найдено — «Тишина в ответ» / «Ни одна задача не отозвалась на зов»
   · архив пуст — «Архив пуст» / «Ни одно деяние ещё не погребено»
   (формулировки согласованы как ВАРИАНТ; при вживлении можно уточнять в том же
   регистре, менять регистр — нельзя.)
2. **Глифы: свой мотив каждому** + перо вместо generic-плюса на кнопке.
   Раскладка: нет задач = незажжённая свеча/пустой подсвечник · все выполнены =
   запечатанный гроб с печатью · ничего не найдено = скрайинг-зеркало (ГОТОВОЕ
   `#icon-scrying`) · архив пуст = пустой саркофаг (ГОТОВОЕ `AA1b`) · кнопка =
   перо (как `GIC.quill` в Гримуаре). Два глифа рисовать, два переиспользовать.
3. **Гримуар, поиск без результата = ПОЛНАЯ ПЛИТА** как у задач (глиф-зеркало +
   заголовок + подстрока) вместо нынешней голой `.grim-list-none`.
**Точки правки:** `index.html:720` (#empty-state, ✦ + кнопка с плюсом),
`index.html:731` (#all-done, ✦), `index.html:795` (#archive-empty, ◈),
`07-dnd-filter-progress.ts:806-816` (переключение текста + показ кнопки),
`02-grimoire.ts:950` (`.grim-list-none`), `_grimEmptyHTML` `02:824` = эталон
структуры. ⚠ Подстрок у задач сейчас НЕТ вовсе — их надо добавлять в разметку и
стилизовать под `.grim-empty-sub`.
⚠ Ратчет иконок: новые глифы → `node scripts/icon-inventory.mjs --update` + роли
тем же коммитом.

## ⏳ БЛОК 3.5 (ЯЗЫК НА ПЛОТНУЮ ГОТИКУ) ИДЁТ С 2026-07-26 — партии A и B закрыты
Юзер выбрал блок 3.5 сразу после W2 (не блок 2 Playwright).
**Источник истины по содержанию — `audit-v2/GOTHIC-LEXICON.md` в репо** (карта:
принципы, зоны-исключения, ядро словаря, 8 партий построчно, §5 вердикты).
Память дублировать карту НЕ должна — здесь только состояние и уроки.

**✅ Вердикты юзера по 7 развилкам (2026-07-26, вжиты в §5 карты):** Склеп вместо
Архива ВЕЗДЕ вкл. нав-таб · дедлайн → **«исход»** (юзер выбрал плотнее моего
«срока») · обет = проза + подписи кнопок, нав-таб «Задачи» остаётся якорем ·
группа и фильтр НЕ трогаются · шаблон → «образец» · круговорот в меню, частота
в пилюле («Ежедневно») без изменений.

**✅ РЕВИЗИЯ A/B ПО ВЕРДИКТАМ ЮЗЕРА 2026-07-26 — `b67439d` build `-11`.** Юзер
отсмотрел таблицы в чате (не на проде): «исход» принят; отклонены «Прочие
деяния» → **«Иное»**, «Пригвоздить/Снять с гвоздя» → **«Приковать к вершине/
Расковать»** (тост D станет «Обет прикован/раскован»), «Условия обета» →
**«Условия»**, «Вернуть в неисполненные» → **«Отречься»** (сведён и парный
aria-label подпункта). «примечание» юзер оставил.
⭐ **Урок регистра:** пафосные составные («Прочие деяния») и буквальные телесные
метафоры («пригвоздить») юзер читает как кринж — плотная готика для НЕГО = сухо
и коротко («Иное», «Условия», «Отречься»), атмосферу несут существительные.
Показывать таблицу партии ДО вживления дешевле, чем откатывать.

**Сделано:** `fe4398a` карта · `c5bd668` вердикты · `c25419a` build `-9`
партия A (строка обета, меню, подпункты, примечания; 62 замены) · `ec0c569`
build `-10` партия B (форма, «Условия обета», модалки исхода/ранга/круговорота/
образцов). **ЖДУТ ПРОД-РЕВЬЮ ЮЗЕРА** — пауза взята намеренно: если регистр не
тот, перекраивать две партии дешевле, чем восемь.

**✅ ПАРТИЯ C ЗАКРЫТА 2026-07-26 — `bb77e93` build `-12`.** Тулбар/фильтры/
порядок/массовые/хранилище, 44 замены скриптом. Юзер выбрал ПЛОТНЫЙ вариант
карты (не смягчённый мной): «Явлены все обеты», «Предать склепу всё»,
«Замкнуться на этой группе», свитки в хранилище. Зонд
`D:/tmp/pw/b1/lex_c_toolbar.mjs` 3/3 PASS (360/412/1280; ⚠ desktop-девайс в
`openApp` = плоский объект `{width,height,...}`, НЕ `{viewport:{}}`).
**🔻 РЕЖИМ ОСТАЛЬНЫХ ПАРТИЙ (вердикт юзера): таблица В ЧАТЕ ДО вживления,
коммит только после вердикта.**

**✅ ПАРТИЯ D ЗАКРЫТА 2026-07-27 — `4b5d27a` build `-27-1`.** 45 замен в 7 файлах.
Вердикты юзера по развилкам: цвет → **ВИТРАЖ** (отклонены «печать» и «масть») ·
копия свода «отлит заново» · переполнение хранилища готическое · «Отменять нечего».

**✅ СВИП «ГРУППА» → «СВОД» 2026-07-27 — `31192e0` build `-27-2`.** Юзер САМ ОТМЕНИЛ
вердикт Р-2 карты («оставить группу») → перевели везде, 33 строки. «Круг» отклонён
мной из-за омонимии с «Круг завершён»/«Круговорот» — юзер согласился. Побочно
найден и починен баг рода в карантине («Шаблон удалена») → `_QUAR_REC_DEL`;
тест `tests/quarantine-copy.test.mjs` пинил старый копирайт — при копи-партиях
ГРЕПАТЬ tests/ на заменяемые строки ДО прогона.

**✅ ПАРТИЯ E ЗАКРЫТА 2026-07-27 — `1ea4616` build `-27-3`.** Архив → СКЛЕП везде
(нав-таб, страница, поиск «Взывать к склепу…», воскрешение, легенда клавиш, сегмент
Гримуара). Закрыт вердикт Р-1 и долг V2-B5-11. Слово «архив» осталось лишь в комментариях.

**✅ ПАРТИЯ F ЗАКРЫТА 2026-07-27 — `d67a4b9` build `-27-4`.** Гримуар + ⚠ РЕВИЗИЯ УЖЕ
ОТГРУЖЕННОЙ ПАРТИИ C: юзер забраковал пару «отдать/принять» как ПРИМИТИВНУЮ →
**вывоз = «Запечатать свиток», ввоз = «Прочесть свиток»** (тосты «Запечатано в свиток: X»,
«Прочтено обетов: N»). Летопись: «Снимки» → **«Слепки»** («Начертания» юзер отклонил),
«Летопись ещё не начата». Шаблоны → образцы, цвет → **витраж** (вкл. массовое Гримуара,
тост записи, поле карантина).
⭐ **Урок:** пары глаголов юзер оценивает ОТДЕЛЬНО от существительных — «свиток» принял,
а глаголы к нему забраковал через две партии. Предлагая пару, сразу давать 3 варианта
с образом, а не один «рабочий»; ревизия отгруженного дешевле рассинхрона языка.

**✅ ПАРТИЯ G ЗАКРЫТА 2026-07-27 — `17f1de9` build `-27-5`.** Карантин: «Разночтения
устройств», «Разобрать разночтения», «Воскресить» / «Предать забвению», имена полей
и падежи видов записей сведены со словарём. ⚠ «Предать забвению» шире прежнего
«Отклонить»: колонка кнопок `flex:0 0 auto` забирала 122 из 331px строки → на ≤560px
строка стала колонкой (кнопки под текстом), доля текста 141px→93%. Зонд
`D:/tmp/pw/b1/lex_g_quar.mjs` 3/3. ⚠ В зонде: у КОРОТКОЙ строки info ужимается до
текста — долю мерить только у строк >40 символов.

**✅ ПАРТИЯ H ЗАКРЫТА 2026-07-27 — `dedc2b2` build `-27-6`.** Легенда клавиш теми же
словами, что кнопки (`Ctrl+Y` = «возврат», снята омонимия с круговоротом) + сверка
скрытых имён (витраж ×9, «Вести об исходе», свод) + ХВОСТ партий A-F, которого не
было в картах (зоны «С дедлайном», объявления скринридеру, склонения облака тегов,
markdown-экспорт). ⚠ Зонд вскрыл СТАРЫЙ баг: `.shortcuts-hint` с `nowrap`+`left:50%`
была 1521px при вьюпорте 1280. Лечение: перенос (пары склеены `&nbsp;`) + `max-width`
+ **обязательный `width:max-content`** — у fixed с `left:50%` shrink-to-fit даёт
`vw − left`, т.е. ПОЛОВИНУ экрана (иначе 4 строки). Зонд `lex_h_hint.mjs` 6/6.
⚠ На ≤560px легенда скрыта совсем — мобильные прогоны вердиктны лишь по факту.

**✅ РАТЧЕТ + БЛОК 3.5 ЦЕЛИКОМ ЗАКРЫТ 2026-07-27 — `3294d10` build `-27-7`.**
`tests/gothic-lexicon-ratchet.test.mjs`: скан `index.html`+`dusk/*.ts` СО СРЕЗАННЫМИ
комментариями (они по-русски и говорят языком реализации) по 11 старым понятиям,
ошибка называет замену. Страховки: `ALLOW` (§4), `ALLOW_EXACT` по ЦЕЛОЙ строке
(иначе «Задачи» пропускало бы любую строку с этим словом), `ALLOW.length ≤ 10` и
тест «новый словарь реально присутствует» (иначе зелёный ратчет = вычищенный текст).
⭐ **Ратчет окупился мгновенно: вскрыл 10 пропусков партий A-F** (подпись счётчика,
поле и кнопка свода, «Приоритет»/«Цвет»/«Сохранить как шаблон»/«Дублировать задачу»
в ⋯-меню, «Подпункты задачи…», «Сделать задачей», подсказка звука пера) — грепом их
не находили, потому что искали по картам, а не по всему видимому тексту.
Тесты 102/102, tsc 27. Все три билда (-5/-6/-7) ЖДУТ ПРОД-РЕВЬЮ.
**✅ РЕВИЗИЯ 2026-07-27 — `40d303a` build `-27-8`: НАВ-ТАБ «ОБЕТЫ».** Юзер отменил
вердикт Р-7«б» (якорь навигации не нужен) → «Задачи» в нав-табе, ссылка на страницу
из тоста Гримуара и «Ни одна задача не отозвалась» переведены. ⭐ **Метод: снял
исключения из ратчета — тест САМ назвал ровно три оставшихся места.** Ратчет = не
только замок, но и поисковик по остаткам голоса; снять исключение дешевле грепа.
Исключений осталось 3 (только §4).

🔻 **ОТКРЫТАЯ РАЗВИЛКА (ждёт вердикта юзера): «подпункт» → другое слово.** Юзер
2026-07-27: «стоит перевести подпункты на что-то другое». Карта раньше говорила
«замен не нашёл» — вердикт отменён. Масштаб: **38 вхождений в 6 файлах**
(index.html 4 · 04-tasks 26 · 05 2 · 08 2 · 11 3 · 01 1), слово склоняется во всех
падежах и во мн.ч. Предложены три: **Ступень** (лестница к исполнению, 🟢
интуитивно, ж.р.) · **Звено** (цепь обета, рифмуется с «приковать/расковать»,
короче всех, ср.р.) · **Зарок** (малый обет, семейно, но рядом с «обетом»
слипается). Внутренние ключи (`subtasks`, `subId`, классы `.sub-*`) НЕ трогать.


**✅ СВИП №3 ЗАКРЫТ 2026-07-28 — `eac5d9f` build `2026-07-28-2`, ЖДЁТ ПРОД-РЕВЬЮ.**
Прикованные · звенья · Замкнуто/Разомкнуто · своды (Возвести/Наречь/Наложить) ·
Вести · склеп Гримуара (Воскресить всех, Отметить, стёрта, Свитки пусты, К записям,
Снять время/дату) · починена порча «Новый своппу» от свипа «группа→свод». Полная
таблица — `audit-v2/COPY-FALLBACK-PRE-3.5.md`, раздел «свип №3».
⭐ **Корень повторяющихся всплытий назван и вылечен:** ратчет — СПИСОК, а не радар;
после каждой партии его надо пополнять словами этой партии. Теперь 23 понятия
(было 16), все 7 новых правил проверены red-green по одному.
⚠ Срезалка комментариев ратчета НЕ берёт `//`-хвост, внутри которого есть кавычки
(`[^'"\`]*$`) — такой комментарий даёт ложный лик; лечится переписыванием комментария.
⚠ Регекс `вернуть` обязателен с lookbehind `(?<![А-Яа-яЁё])`, иначе ловит
«свернуть/развернуть».
**Иконки «Условий» (заказ юзера того же дня):** кнопки «Приковать обет» и «Сохранить
как образец» несли старый арт. Лечение — вынос в `<symbol id="icon-pin">` (PN1) и
`<symbol id="icon-template">` (TP1), `IC.pin`/`IC.template` переведены на `<use>`:
одна копия арта на 4 потребителя. Реестр обновлён + роли, ратчет 197 глифов чист.
Зонд `D:/tmp/pw/b1/sw3_icons.mjs` 3/3 (проверяет, что `<use>` резолвится: bbox svg,
а не только наличие узла — молчаливый несработавший `<use>` иначе не видно).

**✅ УЛЬТИМАТИВНЫЙ СВИП ИКОНОК 2026-07-28 — билды -3 (`b2c3d1b`) и -4 (`4ebdbf5`),
ЖДУТ ПРОД-РЕВЬЮ.** Юзер: «раз за разом нахожу старые иконки — значит их ещё много».
⭐ **Инструмент, которого не хватало (в репо, `50f8ba1`):** ратчет `icon-inventory`
ловит «глиф не зарегистрирован», НЕ «глиф нарисован generic». Добавлены
`scripts/icon-suspects.mjs` (эвристика: нет `<path>` · нет полутонов · мало узлов ·
одна толщина — срабатывала на обеих находках юзера) и `scripts/icon-sheet.mjs` —
**контактный лист всех 177 глифов одной страницей** со складом `<symbol>`, якорем и
ролью; скриншот `D:/tmp/pw/b1/icon_sheet_shot.mjs`. Один взгляд вместо 177 — именно
им находится «старое, но не примитивное» (эвристика такое пропускает).
**Вердикты юзера:** панель редактора Гримуара (~22 глифа B/I/U/S, списки, таблица,
выравнивания) — **ОСТАВИТЬ** (тот же размен, что в языке §4); служебные состояния
Гримуара — **ОСТАВИТЬ**; «Дать обет» уже новый; **превью не показывать, сразу прод**.
**Сделано:** свотч «Без витража» → канонические мечи X1 · feather-галочка
`polyline 20 6 9 17 4 12` ×2 (отметка витража) → MK1 кованая отметка · «Добавить
звено» → крест-паттэ на сетке 13 (плотный регистр: крупный крест на 13px умирает).
⚠ **Три настоящих алиаса вскрыты по ходу:** мечи жили 4 копиями; `IC.crossSm` и
`IC.crossAdd` байт-в-байт равны + ещё 2 инлайна в html. Все сведены в `<symbol>`.
⚠ Радар МЯГКИЙ: `sortOrder`/`sortAlpha`/`promote`/`demote` он пометил, а глазами
видно — они уже готические (иллюминированная книга, зубчатый парапет). Счёт
эвристики — повод посмотреть, не приговор.
Проба нового арта в реальном кегле: `D:/tmp/pw/b1/newglyph_probe.mjs` (1× и 8×).
⚠ **ВЕРДИКТ ЮЗЕРА ПО КРЕСТУ — крест-паттэ ОТКАЧЕН (`ce24bac`, build -5).** «Знак
добавления должен быть ТОЧНО таким же, как у "Дать обет" и у живой кнопки под
звеньями». Мой «плотный регистр под 13px» здесь не нужен: живая
`.btn-subtask-confirm` уже рисует крупный крест на **12px** и юзера это устраивает.
Кнопка «Условий» переведена на `#icon-cross-add`, кегль 13→12. Symbol
`icon-cross-pattee` удалён. Зонд `D:/tmp/pw/b1/cross_parity.mjs` 3/3.
⭐ **Правило: перед вводом «второго регистра» знака СНАЧАЛА посмотреть, в каком
кегле тот же знак уже живёт в проекте** — если он там выживает, второй регистр
не нужен, а расхождение знаков юзер читает как баг.
⚠ **MK1 «кованая отметка» ОТКАЧЕНА ЮЗЕРОМ (`e941404`, build -6): «галочка плохая,
верни старую и удали оттуда, где взял, чтобы больше не внедрял».** Прежний глиф
отметки витража вернулся в обе точки, `<symbol id="icon-mark-chosen">` снят из
склада. **ТЕМУ НЕ ОТКРЫВАТЬ, отметку витража не трогать.**
⚠ **Грабля удаления `<symbol>`:** срез «от комментария до следующего комментария»
снёс ТРИ соседних символа (swords/pin/template) — символы лежат подряд и мой якорь
был не тот. Резать надо от комментария до ЗАКРЫВАЮЩЕГО `</symbol>` своего символа,
и сразу грепать, что соседи на месте.
⚠ **Грабля:** `sed -i` по строке `width: 13px; height: 13px; pointer-events: none;`
задел ЕЩЁ ДВА правила (`.grp-dd-del`, `.btn-note-delete`) — в style.css такие
хвосты не уникальны. Правки CSS — только скриптом с обязательным числом попаданий.

⭐ **Метод, который окупился (переиспользовать):**
1. **Инвентарь ДО карты.** Разовый harvest-скрипт (кириллица из `index.html`
   `title`/`aria-label`/`placeholder`/текста + литералы `dusk/*.ts`, комментарии
   отброшены) дал 810 уникальных строк — карта строилась по факту, а не по памяти.
2. **Партия = скрипт замен с минимальным ожидаемым числом попаданий**; любое
   несовпадение → файлы не тронуты вовсе. ⚠ **Грабля: порядок замен.** Общая
   замена (`'Добавить заметку'`) съедает частную (`s.note ? … : 'Добавить
   заметку'`) → частные пары ставить ПЕРЕД общими либо матчить пост-состояние.
3. **Зонды на длину:** `D:/tmp/pw/b1/lex_a_row.mjs` (переполнение мета-ряда на
   360/412) и `lex_b_form.mjs` (кнопка «Условия обета» + модалка исхода).
   Готические слова длиннее канцелярских — это главный техрисок свипа.

## 🔻 ДИРЕКТИВА ЮЗЕРА 2026-07-26 — ЯЗЫК ВСЕГО ПРИЛОЖЕНИЯ НА ПЛОТНУЮ ГОТИКУ (исходная формулировка)
Слово юзера: «неплохо бы перевести язык (текст) полностью на плотную готику;
сейчас это прослеживается в Гримуаре (но возможно не полностью), а хотелось бы
ВЕЗДЕ. Только нужно тщательно продумывать тексты, чтобы они оставались
интуитивны и пользователь хотя бы примерно понимал суть — хотя жертва
интуитивности здесь неизбежна и это нормально».
Это ОТДЕЛЬНАЯ программная работа, НЕ часть W2: сплошной свип копирайта (кнопки,
меню, тосты, модалки, подсказки, ошибки, пустые состояния, панель синка,
карантин). Логически поглощает `V2-B5-11 terminology map` из W3 и опирается на
готовую таблицу терминологии в `audit-v2/B5-ux.md`. Порядок: сперва
СЛОВАРЬ-КАРТА (нынешнее → готическое, с пометкой «где жертвуем интуитивностью»)
→ вердикт юзера по карте → потом сплошное вживление партиями по экранам.
W2-9 (empty-state, плотная готика) = ПИЛОТ этого перехода; его словарь («обет»,
«алтарь», «деяние», «зов») становится затравкой общей карты.
Место в очереди — за юзером; по умолчанию после W2/W3, до флагмана пуш+TWA.

## 🔻 ЗАКАЗ ЮЗЕРА 2026-07-25 (вечер, после ревью партий 5-6) — ✅ ВЫПОЛНЕН, см. выше
Вердикт по всем отсмотренным билдам: **«всё ок»**. Плюс два задания:

**1. Кружки приоритета → БАШНИ КВИК-ЭДДА.** Уточнение юзера: имеются в виду
**PA · rank towers из типахеда квик-эдда** — `IC.prioRankHigh/Medium/Low/None`
(`dusk/01-core.ts` ~695-705, диспетчер `qaSigil()` ~:749, рендер `.qa-sig` 15px
`style.css:1384-1388`, цвет едет через `currentColor` + классы `.prio-*`).
НЕ `IC.sortPriority`/`IC.spires` (M3/AW1) — те остались кнопке сортировки и
кнопке приоритета задачи. Уровень читается высотой башни + навершием
(крест-ромб → ромб → шар), «нет приоритета» = руина без шпиля.
⚠ **Главный конструктивный риск:** кружки сейчас 6/7/9/11px, а башни рисовались
под 15px. Контурная башня в 7px не выживет — либо поднимать размер маркеров до
13-15px (и пересматривать вёрстку строк селектора/меню), либо делать плотный
«залитый» вариант башен под мелкий размер. Решать растровой пробой ДО вживления;
если оба пути имеют цену — показать превью и спросить юзера. Есть также гипотеза
с билда -2: перевод приоритета с 8px ЗАЛИВКИ на 15px тонкую линию уронил площадь
цвета — тут та же ловушка, цвет должен остаться заметным.
Карта точек
(проверена грепом 2026-07-25, всё равно перепроверить перед правкой):
- `.prio-dot` — `style.css:821` база 6px; `:890` в `.prio-grid-btn` 7px;
  `:6680` в `.prio-bulk-btn` 9px + свечение; `.fm-prio-dot` `:1280` 11px
  (плавающие меню);
- разметка: `index.html:147-149` (селектор в форме), `:1308-1310` (модалка
  «Изменить приоритет»), `:644-646` (select-bar, массовый приоритет);
- динамика: `04-tasks.ts` `fm-q-prio` (~:2515, ⋯-меню подпункта; у задачи свой
  аналог), `.btn-sub-action.sub-prio-btn` `style.css:3083` — круглая кнопка
  «чтобы совпадать с мотивом кружка», после смены мотива скругление пересмотреть;
- ⚠ НЕ трогать «уголёк» подпункта (`::before`, `--sprio`) — это отдельное решение.
- Развилка на подумать: башни цветные (заливка `--prio-*`) или контурные с
  цветным свечением; и как показывать «нет приоритета» (сейчас парные полумесяцы
  C1 — вероятно остаются).

**2. Нав-таб «Задачи» — ПОЛНАЯ переработка арта.** Юзер: «сильно не хватает
детализации и эстетичности». Сейчас `index.html:71` = AA1 гроб с кованым крестом,
`viewBox 0 0 13 13`, штрих 1, заклёпки 1×1px — аскетизм продиктован пиксельной
сеткой 13px. ⚠ Конфликт с правилом «на 13px запрещены тонкие штрихи и пологие
диагонали»: детализацию честно даёт либо (а) более выразительный ОРТОГОНАЛЬНЫЙ
силуэт в тех же 13px, либо (б) подъём размера нав-иконок (13 → 15-16px, `.nav-tab
svg` `style.css:454`) — тогда бюджет на детали появляется, но переносить придётся
ВСЕ ТРИ таба (том и саркофаг уже сидят на сетке 13 и живут в `<symbol
id="icon-tome">`/`icon-tomb`). Выбор (а)/(б) — вкус юзера, СПРОСИТЬ, показав
превью-таблицу вариантов растром.

## ✅ ХВОСТ «ГРИМУАРНАЯ ПОЛОВИНА» ЗАКРЫТ 2026-07-25 — партии 5 и 6
Перепись алиасинга проведена (реестр как ДЕТЕКТОР, не только ратчет): 40 хэшей с
≥2 локациями → 13 настоящих алиасов. Полный отчёт+вердикты — в
`audit-v2/O-FINAL-XVOST-HANDOFF.md`, секция «✅ ПЕРЕПИСЬ ПРОВЕДЕНА».
- **`f66781d` build -8 (партия 5):** нав-таб «Склеп» и «Выбрать» Гримуара
  перестали быть действиями; grim-bulk-archive/restore = те же глифы, что у
  задач; «Очистить архив» с ЧЕРЕПА (== одиночное удаление в строках!) на BR1
  урну; **новый квантор «ВСЁ» = три убывающие плиты постаментом** —
  `icon-purge-all` (череп) и `icon-restore-all` (урна); `.grim-seg` 14→13px,
  `.btn-archive-action` 11→13px.
- **`941dfb8` build -9 (партия 6):** поиск — три копии → один `#icon-scrying`;
  триггер дедлайна нёс мёртвую арку, совпадавшую с «По будням» → арт `IC.window`
  статически; снята мёртвая `IC.search`.
- **Метод (переиспользовать):** `extractAll()` → группировка по хэшу → ближайший
  назад якорь (`id`/`title`/`data-act`/имя константы) → grep потребителей
  `IC.*`/`GIC.*`. Реестр видит только инлайн-литералы: константа = одна локация,
  хотя потребителей десять. ⭐ Растровая проба перед выбором композиции
  (`_p5cand*.mjs`): badge-кванторы (печать/кольцо/удвоение) на 15px — каша,
  выживает только «постамент из плит».
- Оба билда ЖДУТ ПРОД-РЕВЬЮ юзера (вместе с -4…-7).

🔻 **История находки (юзер, 2026-07-25): ГРИМУАРНАЯ ПОЛОВИНА ИКОНОК.**
O-FINAL шёл по задачной стороне; у Гримуара свой параллельный набор массовых
действий (`#grim-select-bar`, index.html:945-962), и де-алиасинг партии 4 его не
касался: `grim-bulk-archive` = BQ1-гроб, общий с одиночной записью; `grim-bulk-restore`
= урна AY1, общая с одиночным restore. Слово юзера: «раз есть это, значит наверняка
упустили ещё». ⭐ **Следующий заход начинать с ПЕРЕПИСИ, а не с правки:**
`icon-inventory.mjs` + выбрать хэши с ≥2 локациями РАЗНЫХ семантик — реестр умеет
это, но как детектор алиасинга им ни разу не пользовались (ратчет ловит только
незарегистрированные глифы, не склейку смыслов).

✅ **Развилка `GIC.chronicle` ЗАКРЫТА вердиктом юзера 2026-07-25: ОСТАВИТЬ.**
Крылатые песочные часы «Летописи» в Гримуаре — орнаментальные, memento mori, своя
семантика «история версий», после свипа ни с чем не конкурируют. Единственные
песочные часы в приложении, это НАМЕРЕННО. **Блок 0.2 закрыт целиком; дальше по
`STRATEGY.md` блок 1 = W2, кириллица квик-эдда первой.**

**Уроки:**
- ⭐ Доки соврали ЕЩЁ ДВАЖДЫ 2026-07-25: `IC.deadlineClear` числился песочными
  часами — грепом это ОКНО СО СВЕЧОЙ (derive B1 от `IC.window`, парное постановке
  дедлайна, отдельный вердикт keep-current), НЕ тронуто; зато в списке свипа не
  было `GIC.hourglass`. Правило: свип «породы» вести грепом по мотиву
  (`hourglass`, фрагмент пути), а не по списку из дока.
- Старые доки соврали ТРИЖДЫ: `#icon-archive` = три семантики (не две), у X1
  живых целей две (не три), у «Архивировать всё» точек ДВЕ (`index.html:610`
  через `<use>` И `08:295` через `IC.archive`). Grep перед каждой правкой.
- ⭐ **Арт брать машинно:** `node audit-v2/previews/gen-fb4-v3.mjs --dump f.json`
  выгружает утверждённые глифы в JSON → вживление копирует оттуда, превью и
  приложение совпадают символ в символ.
- ⚠ **CRLF-грабля:** рабочая копия чекаутится в CRLF (`core.autocrlf=true`).
  Многострочная вставка с LF рождает одиночные `\r`, и `git diff` показывает ВЕСЬ
  файл изменённым. Детектор: `--stat` тысячи строк vs `--ignore-all-space`
  десятки. Лечение: `s.replace(/\r(?!\n)/g,'\r\n')`. `git stash`/`pop` тоже
  перечекаутит файлы LF→CRLF.

**Скилл impeccable обновлён до v4.0.2** (юзер разрешил 2026-07-24).

## ✅ ПРОД-РЕВЬЮ 11 БИЛДОВ ПРОЙДЕНО ЗА ЮЗЕРА 2026-07-28
Юзер: «посмотрел поверхностно, лень всё чекать — сделай за меня». Закрыты билды
`-27-5…-9` и `2026-07-28-1…-6`.
**Метод (переиспользовать вместо ручного обхода):** зонды по dist, не глаза —
`D:/tmp/pw/b1/rev_sweep.mjs` (30 состояний × 412/1280: скриншот + детекторы +
дамп `innerText`), `rev_sweep2.mjs` (добор 10 поверхностей, которых первый не
открыл: снуз, хранилище, три панели массовых, пустой зов, тосты),
`rev_attrs.mjs` (**188 строк `title`/`aria-label`/`placeholder`** — копирайт,
невидимый в innerText; ратчет их видит в исходниках, но не в факте DOM).
**Детекторы дали 0:** гор. скролла нет, за вьюпорт ничего не вылезает, все
`<use>` резолвятся, ошибок консоли нет. Значит длинные готические слова вёрстку
НЕ сломали ни на 412, ни на 1280.
⭐ **Урок: дамп текста дешевле картинок в разы** — из ~40 скриншотов реально
понадобилось 5. Картинки нужны только под иконки/композицию.
⚠ **Дедуп строк между состояниями ВРЁТ:** «Высокий» в карантине выглядел
потерянным только потому, что уже встречался в форме. Вердикт — по СЫРОМУ тексту
состояния.
⚠ **Ложняк детектора мёртвых `<use>`:** у потомка `display:none` предка
`getComputedStyle` отдаёт СВОЙ display → нулевой bbox на скрытых страницах.
Фильтровать `el.checkVisibility({checkOpacity:true, checkVisibilityCSS:true})`.
**Найдено 8 расхождений, одно снял сам** (двоеточие в тосте «Порядок: по рангу» —
родная идиома, ср. «Режим: неисполненные / исполненные»).
**Вердикты юзера 2026-07-28:** глиф «Без витража» → **сосуд везде** · панель
синка **НЕ переводить** · «Горячие клавиши» **оставить** · остальное чинить.
- **`6f338ed` build `-7`** — `<symbol id="icon-vial-none">` (VN1): свотч формы
  обета и кнопка «Убрать витраж» модалки на `<use>`, одна копия арта. Мечи X1
  остались знаком «оборвать зов / отклонить» (4 потребителя).
  ⚠⚠ **Грабля, стоившая отката:** регекс поймал НЕ ТОТ сосуд — массовый «Витраж»
  (H2 алхимический фиал, без косой) лежит выше по файлу и почти совпадает.
  Проверка «строка встречается ровно 1 раз» это НЕ ловит: уникальным был чужой
  захват. **Якорь замены брать по УНИКАЛЬНОЙ ДЕТАЛИ арта** (здесь — косая
  `x1="5.2" y1="19.4"`), а не по началу тега, и после правки грепать соседа.
- **`b34ff5f` build `-8`** — «Родитель чекается по…» → «Обет исполняется по
  любому звену / по всем звеньям»; плейсхолдер «Новое звено» жил двумя литералами
  (`...` и `…`) → сведён; aria «Раздел гримуара» → «Створы Гримуара»; последняя
  в приложении кнопка **«ОК»** (своя отсрочка) → «Отсрочить», зонд
  `snooze_fit.mjs` 3/3. **§4 карты пополнен: «панель синхронизации целиком» —
  зона-исключение** (служебный контур; готическим там остаётся только карантин).
**⚠⚠ ЮЗЕР НАШЁЛ ПРОПУСК ТОГО ЖЕ ДНЯ: «Чек родителя по звеньям» / «Чек по
звеньям» → `da52c84` build `-9`.** Две причины промаха, обе переносимые:
1. ⚠⚠ **`grep -i` В ЭТОЙ СРЕДЕ НЕ СВОРАЧИВАЕТ РЕГИСТР КИРИЛЛИЦЫ.** `grep -niE
   "чек"` не нашёл «Чек» с заглавной — искать ТОЛЬКО явными классами `[Чч]`.
   Это же делает ратчет словаря, поэтому он был зелёным и не спорил.
2. Меню режима чека в свипе не открывалось (лежало в списке пропусков) —
   **пропущенная поверхность = пропущенный копирайт**; список пропусков надо
   вычитывать отдельно, а не считать «мелочью».
Замены: заголовки обоих меню + title кнопки → «Исполнение обета по звеньям»,
пункт ⋯-меню → «Исполнение по звеньям». Сплошной скан 30 понятий явными
классами вскрыл ещё три рассогласования: title сортировки «Режим: по рангу»
против тоста «Порядок: по рангу» (сведены на «Порядок»), голое title
«Добавить» у живой кнопки звеньев (→ «Добавить звено»), тост Гримуара «чтобы
удалить» (→ «уничтожить»). Ратчет = **24 понятия**; правило «чек» с lookbehind
`(?<![А-Яа-яЁё])` (иначе ловит «Точек») и lookahead `(?!-?лист)` (щадит
«Чек-лист» редактора и «markdown-чеклист» — §4). Red-green проверен.
Зонд `D:/tmp/pw/b1/submode_copy.mjs` 6/6. ⚠ В зонде три ложных FAIL, стоивших
итерации: динамический title перебивает статический из разметки
(`updateSubAnyModeBtn`), заголовки меню рисуются КАПСЛОКОМ через CSS (сверять
без учёта регистра), а пункт «Исполнение по звеньям» есть только у обета СО
ЗВЕНЬЯМИ (`hasSubs`) — выбирать строку через `.find(t => t.querySelector('.subtask-item'))`.
🔻 **ОСТАЛОСЬ:** билды `-7`/`-8`/`-9` ждут прод-ревью (косметика, риск низкий).
Дальше по `STRATEGY.md`: **блок 2** (Этап 5, Playwright в репо — мой дефолт,
харнесс страхует всё дальнейшее) **или блок 3** (B11/B12 adversarial + O-1
мотион-замер). Порядок за юзером.

## ✅ БЛОК 2 (ЭТАП 5, PLAYWRIGHT В РЕПО) ЗАКРЫТ 2026-07-28 — 5 коммитов, 31 кейс
Юзер выбрал блок 2 и утвердил рамку: глубина «средний», **без пиксельных
базлайнов**, CI не заводим, `D:\tmp\pw` ОСТАЁТСЯ песочницей одноразовых зондов.
Спека и состав — **`E2E-SPEC.md`** в корне (там же §8 «грабли»); память карту не
дублирует. Коммиты `42a6577` каркас+бут · `d3fad1c` навигация/обеты/звенья/
квик-эдд · `d687ddb` исходы/склеп/Гримуар · `d3d0bd1` хвост последовательности+
PWA · `800bc21` геометрия/панель синка/доки. Гейты: e2e 31/31 (~42 с),
vitest 102/102, tsc 27, build, ратчет иконок чист. **`version.json` НЕ бампался
— коммиты тестовые, приложение не менялось.**
**Что закрыто попутно:** долг W0 из FIX-PLAN (seq-aware сиды вместо
`s4_p0_idbboot`) — теперь кейсы бута в ОБЕ стороны.
⭐ **Аргумент против базлайнов (переиспользовать, если тема всплывёт):** Chrome
системный и автообновляется, шрифты системные, DPR 2.625, фон — JPEG с
артефактами, дедлайны пульсируют по таймеру → каждое обновление браузера даёт
массовый ложный красный; глушится порогом `maxDiffPixels`, а порог убивает ту
самую чувствительность. Замена — вердиктные замеры геометрии.
⚠ **Грабли, стоившие времени (все в §8 спеки):** `addInitScript` НЕ ждёт промис
(сев IDB проигрывал буту → сеять отдельной навигацией на route-заглушку) ·
заглушка обязана нести `<link rel="icon">`, иначе Chrome сам просит
`/favicon.ico` и 404 выглядит багом приложения · сетевые события service
worker'а приходят на КОНТЕКСТ, не на страницу · `msg.location().url` — без него
«Failed to load resource» не называет виновника · позиции строк сравнимы только
ВНУТРИ одной секции (свод рисуется своим блоком) · **любой зонд-обходчик обязан
возвращать число осмотренных узлов, а тест — его проверять** (иначе «всё
отфильтровалось» = зелёный).
⭐ **Именование `*.e2e.mjs`, НЕ `.spec.`** — дефолтный include vitest ловит
`.spec.` и утащил бы e2e в `npm test`. Развязка именем не теряется при правке
`vite.config.js`.
✅ **БИЛДЫ `-7`/`-8`/`-9` ПРОШЛИ ПРОД-РЕВЬЮ ЮЗЕРА 2026-07-28** («успешно») →
хвост косметики закрыт, невыверенных билдов на проде НЕТ.
🔻 **ДАЛЬШЕ:** блок 3 (B11/B12 adversarial + O-1 мотион-замер) → блок 4 (W3) →
блок 5 флагман Push+TWA.

Связи: [[remaining-scope-map]], [[strategy-2026-07-16-fable-abundant]],
[[drawer-w4-session]], [[model-tier-checkins]], [[context-hygiene-rule]].

## ✅ БЛОК 3 ЗАКРЫТ 2026-07-28 — B11/B12 (`88a5a1e`, build `-10`) + O-1 (`d382e55`, build `-11`)
Юзер: делать «оба одним заходом», линзы — «как качественнее» → прошёл ИНЛАЙН без
субагентов (весь контекст проекта уже при мне; холодный субагент переспрашивает).

**B11/B12.** Содержание — `audit-v2/FINDINGS.md`, батчи B11/B12; память карту не
дублирует. Флагман **B11-01**: `id` записи из недоверенного источника (свиток от
человека ИЛИ подменённый файл Drive) ехал в `data-id="…"` СЫРЫМ. Зонд до фикса:
1 инъекция в листе Гримуара, 2 в меню образцов, 10 в строке обета.
⭐ **Корень, который стоит помнить: границ доверия ТРИ** (импорт свитка · посадка
мержа синка · чтение LS/IDB), а форму проверял только импорт. Общий пост-шлюз
`normalizeState()` (зовётся после КАЖДОЙ подмены state) идентичность лишь
БЭКФИЛЛИЛ. Лечение — там же: `_sanitizeIdentity()` в `01-core.ts`.
⭐ **B11-02 — переносимый урок:** шлюз NA-2 чистил `state.notes`, а летопись живёт в
ОТДЕЛЬНОМ ключе LS → мимо шлюза. «Шлюз на границе» перечислять ПО ХРАНИЛИЩАМ.
⚠ Усиление, из-за которого класс не косметический: `refresh_token` в localStorage →
цена инъекции = бессрочный доступ к Drive.
Не трогал: `worker/` (вне пуш-блока, B11-04 записан), семантику мержа (B11-06).
⭐ Опровергнуто по ходу: «Bubblewrap не умеет SVG-иконки» — ЛОЖЬ (`ImageHelper.ts`
гонит svg через `svg2img`). Зато B12-01 реален: арт иконок вне 80%-й безопасной
зоны maskable (углы (24,168) → r=101.8 при пороге 76.8) — маска Android срежет.

**O-1.** Полный отчёт — `audit-v2/MOTION-TRACE.md`. Главный факт: медиана кадра
скролла росла ЛИНЕЙНО с числом обетов (20→6 мс, 60→42, 100→76, 200→137). Лечение —
`content-visibility: auto` + `contain-intrinsic-size: auto 104px` на строки списка
и склепа, со снятием пропуска на `body.is-dragging` (Sortable меряет соседей).
Итог: 100 обетов 76→37 мс, 200 → 52 (p90 195→79).
⭐⭐ **Три гипотезы замер ОПРОВЕРГ — не тратить на них стадию:** (а) пульс
`.app-glow` (слой 5.8 Мпикс!) — снятие will-change/пульса/элемента даёт НОЛЬ;
(б) пункт 2 плана (батчить `scrollHeight`/`offset*` в 02/07) — принудительного
layout при скролле/сворачивании 0 мс, а реализованный батч раскладки ручек дал 12%
при 60 строках (шум) и −1% при 200, событий `Layout` поровну (114/114) → ОТКАЧЕН;
(в) пункт 5 (`transition:all`) — живых 0, свип W3 чист.
⚠⚠ **Грабли замера, стоившие итераций:**
1. Census `transition-property: all` насчитал 5355 элементов — это ДЕФОЛТНОЕ
   вычисленное значение; считать только с `transition-duration > 0`.
2. Микро-A/B по МЕДИАНЕ без чередования порядка показал «9× выигрыш», которого нет:
   первый прогон серии платит за грязную вёрстку. Правило: чередовать участников
   И порядок, каждый замер стартовать с инвалидированного layout.
3. Прогон-к-прогону трейс гуляет ±30% — одиночным прогоном ничего не доказывается;
   решают либо A/B внутри одного процесса, либо счётчики событий.
4. headless vs headed (реальный GPU) дают ОДИНАКОВЫЕ числа — SwiftShader сигнал не
   искажает, отдельный headed-прогон не нужен.
⭐ **Инструменты (песочница `D:/tmp/pw/b1/`):** `o1_trace.mjs` (6 сценариев × фазы
кадра + счётчики событий), `o1_layers.mjs` (перепись слоёв CDP LayerTree + census
стилей), `o1_ab.mjs` (A/B гипотез CSS-накладкой, вариант `--only`), `o1_loaf.mjs`
(LoAF: `forcedStyleAndLayoutDuration` с атрибуцией по функции — единственный
честный ответ «нужен ли батчинг»), `o1_handles_bench.mjs`, `o1_cv_func.mjs` (6/6:
DnD, доскролл, звенья, склеп).

🔻 **ОТКРЫТО, ЖДЁТ ВЕРДИКТА ЮЗЕРА (4 пункта):** B11-06 потолок входящего
`updatedAt` (кривые часы устройства выигрывают мерж навсегда) · B11-04 хардненинг
воркера (только внутри пуш-блока) · B12-01 иконки maskable (вкус по арту) ·
O-1-x уход обета в склеп (полный `render()` в `onanimationend`, 16 кадров >33 мс).
**ДАЛЬШЕ по STRATEGY.md: блок 4 (W3, ~14 polish) → блок 5 флагман Push+TWA.**

### ✅ ХВОСТ БЛОКА 3 ЗАКРЫТ 2026-07-28 (`ca705eb`, build `-12`; `23b65b5`, build `-13`)
Юзер: «B11-06 фикси · B12-01 выбери лучший по качеству/надёжности · O-1-x фикси ·
B11-04 фикси» → все четыре сделаны, открытых вердиктов по блоку 3 НЕТ.

- **B11-06** — кламп входящего `updatedAt` на ГРАНИЦЕ (тот же `_sanitizeIdentity`),
  потолок `Date.now()+24ч`, по всем видам записей + надгробия + журнал (по их
  штампам работает GC). ⚠ Потолок от `Date.now()`, НЕ от `nowTs()` — монотонные часы
  поползли бы за отравленным штампом. Оговорка: первый мерж отравленный штамп ещё
  выигрывает, со следующей локальной правки — нет.
- **O-1-x** ⭐⭐ **виновник оказался НЕ там, где искали.** Инструментировал `render()`
  по фазам → `updateProgress` стоил 36 из 155 мс при 200 обетах. Причина — идиома
  перезапуска анимации счётчика `remove → void el.offsetWidth → add`: синхронный
  layout ВСЕГО документа на КАЖДОМ изменении счётчика. Контроль: под
  `prefers-reduced-motion` (перезапуска нет) та же фаза = 1 мс.
  Лечение — чередование двух классов-близнецов с РАЗНЫМИ именами кейфреймов
  (`counterPop` ↔ `counterPopAlt`): смена имени = «начать заново», без чтения.
  ⚠⚠ **WAAPI (`getAnimations()`+cancel/play) ОТВЕРГНУТ ЗАМЕРОМ** — форсирует
  пересчёт стиля, стоит столько же (39 против 36). Итог: рендер ухода обета
  60 обетов 63→45 мс, 200 обетов 176→126 мс.
  ⭐ Правило: перезапуск анимации в ГОРЯЧЕМ пути — только чередованием классов;
  `void offsetWidth` допустим лишь в редких переходах (в проекте таких ~10, не
  трогал — замер не требует).
- **B11-04** — воркер: серверная проверка Origin (403 до похода в Google) на
  `/exchange`, `/refresh`, `/ws`; пустой ALLOWED_ORIGIN = «не настроено» → пропуск
  (кривой деплой не должен убивать синк); потолок тела 8 КБ через `request.text()`
  (Content-Length подделывается); лимит 30/мин по IP через ШТАТНЫЙ биндинг
  `[[ratelimits]]` (своего состояния не заводим — модульные переменные в воркере
  протекают между запросами; нет биндинга → тихо пропускаем); длина имени комнаты
  урезана; ретрансляция нуджа >256 байт запрещена. `compatibility_date` → 2026-07-28,
  `observability` включена. Замки: `tests/worker-hardening.test.mjs` (10 кейсов).
  ✅ **ЗАДЕПЛОЕНО ЮЗЕРОМ 2026-07-28** (`npx wrangler@latest deploy`, wrangler 4.114, версия `196cdb29`; биндинг SYNC_LIMITER поднялся). Дымовая проверка по ЖИВОМУ воркеру прошла: чужой Origin 403 · curl БЕЗ Origin 403 · свой Origin доходит до Google · тело 9020 Б → 413 · /ws со своим Origin 101 (будилка жива) · /ws с чужим 403. ⚠ Лимит намеренно НЕ простреливали 31 запросом: он по IP и на минуту заблокировал бы собственный синк юзера.
- **B12-01** — выбран вариант с ОТДЕЛЬНЫМ `public/icon-maskable.svg`: нынешние иконки
  остаются `purpose:"any"` (десктоп/браузер видят детальный арт), Android получает
  вариант, который не режет ни одна маска. Силуэт НЕ перерисован — та же группа,
  только масштаб 0.74, подобранный по крайним точкам арта (дальняя 188.7 при пороге
  204.8), проверено рендером под круглой и squircle-маской. Прекеш SW пополнен,
  **CACHE `dusk-shell-v9` → `v10`** (иначе новый файл не попал бы в офлайн-оболочку).

⚠ **Грабля сессии:** `git checkout <файл>` для отката экспериментальной правки унёс
и НЕЗАКОММИЧЕННЫЙ соседний фикс в том же файле. Перед откатом файла — проверять, что
в нём нет другой живой работы (или коммитить её раньше).
**Гейты на конец блока: vitest 121/121, e2e 31/31, tsc 27 (базлайн), build, ратчет
иконок чист. ДАЛЬШЕ: блок 4 (W3, ~14 polish) → блок 5 флагман Push+TWA.**

## ✅ БЛОК 4 (W3, polish) ЗАКРЫТ 2026-07-28 — 5 коммитов, билды `-14`…`-18`

Все живые пункты `FIX-PLAN §W3` закрыты. Коммиты: `90bd93e` (пачка 1 — последний
инлайновый хендлер, локальная дата свитка, глобальное выделение), `e757b51`
(V2-B6-02 многовкладочный реконсайл), `d5923ad` (a11y-пачка: ориентиры и
заголовки, подсказка синтаксиса, честная генерируемая легенда клавиш, развод двух
повторений), `5844712` (визуальная пачка: читаемость склепа, запечатанная плита
свода, привязка плавающих полос к карточке), `f30a90c` (створка оглавления +
липкий тулбар). Гейты на конец блока: **vitest 182/182** (было 121), e2e 31/31,
tsc 27 (базлайн), build, ратчет иконок чист.

**Вердикты юзера, по которым делали:** V2-B5-06 → легенда клавиш ГЕНЕРИРУЕМАЯ
(не новый оверлей «?»); V2-B5-08 → развести словами + пояснитель; V2-B1-16 →
нижняя СТВОРКА оглавления; V2-B1-25 → липкий тулбар «и на ПК тоже».

⭐ **Уроки, переносимые дальше:**
- ⚠ **Значение анимации с `forwards` бьёт обычный каскад.** Кандидаты на контраст
  склепа «не двигались» не из-за селекторов: цвет держал кейфрейм `strikeColorIn`.
  Лечение — `animation: none` в области склепа. Тот же класс капкана, что
  `void offsetWidth` в O-1: сперва замер, потом гипотеза.
- ⚠ **`var()` внутри кастомного свойства подставляется там, где свойство
  ВЫЧИСЛЯЕТСЯ** (`:root`), поэтому потирные переопределения `--app-card-w` до
  второй переменной-кромки не доходили → формулу `max(22px, calc(...))` пишем по
  месту у каждой плавающей кнопки.
- ⚠ **`position: sticky; bottom:` работает только для элемента в КОНЦЕ потока.**
  У тулбара в начале записи он мнимый — элемент просто уезжает вверх (замерено на
  живом). Лечение: колонка → флекс, тулбару `order: 9`.
- ⚠ **Layout viewport под клавиатурой не сжимается** (Chrome отдаёт
  `interactive-widget=resizes-visual`), поэтому высоту клавиатуры считаем сами:
  `_kbInsetSync()` по `visualViewport` → `--kb-inset`, а правило берёт
  `max(обход плавающих кнопок, --kb-inset)` — ветвления в CSS не нужно.
  `position: fixed` в Гримуаре НЕЛЬЗЯ: у `.grim-detail` есть `transform`, он
  становится содержащим блоком.
- ⚠ **Зонд ловил чужую страницу**: переход страниц переживает фиксированные
  ожидания, а раздельные `page.evaluate` ложатся по разные стороны перехода →
  один снимок за один evaluate + ручной опрос (playwright `waitForFunction` с
  объектным аргументом здесь таймаутил).
- ⚠ **Находка аудита V2-B5-08 была фактически НЕВЕРНА** (утверждала, что тумблер
  переноса исхода не делает обет круговоротным — `_applyAutoRepeatToTarget`
  ставит `obj.repeat`). Копирайт и замок писали ПО КОДУ, расхождение отражено в
  комментарии и коммите. Правило: находка аудита — гипотеза, а не факт.
- Мелочи харнесса: `import.meta.url` под happy-dom не file:-URL → путь через
  `join(process.cwd(), 'dusk')`; одноимённых `@media` в style.css несколько —
  склеивать ВСЕ блоки, а не первый; вычисленный стиль сразу после добавления
  класса отдаёт стартовые значения перехода (ждать ~700 мс).

**ДАЛЬШЕ: блок 5 — флагман Push + TWA (Android).** Визуальные пункты W3 ждут
прод-ревью юзера.
