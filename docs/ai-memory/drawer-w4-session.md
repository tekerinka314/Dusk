---
name: drawer-w4-session
description: "⭐⭐⭐ W4 drawer «Створка» ЗАКРЫТ (свайп-фикс 91ce1a3) + пилюля групп (ef499f8) + AZ6-уроборос ВЖИВЛЁН (`8065ee1`). **2026-07-24: ИКОНОЧНЫЙ ХВОСТ O-FINAL ИСЧЕРПАН — блок A (билды -2…-6 + BD1 `73ca94b` + BI1/BM1 `fb8447b` + полоса под шапкой `f59c01a`) ПРИНЯТ юзером; блок B P3 two-state = билды -4/-5 (`63ccdff` + фикс инверсии `5a5a24f`); блок C BL2 рерабока `7ebb87e` ПРИНЯТА + вживлена в GIC.barLvl билдом -6 (`62058fb`, ждёт прод-ревью).** ДАЛЬШЕ: **W2 (кириллица квик-эдда ПЕРВОЙ)** по `remaining-scope-map`; в O-FINAL остался только явно отложенный творческий блок (archive-крипта де-алиасинг · N-сигилы · X1). Урок темпа п.3е СМЯГЧЁН — только вкус/рисование, не механика."
metadata: 
  node_type: memory
  type: project
  originSessionId: 6cab3fd7-22c8-4f24-9bae-dd8a35d42548
  modified: 2026-07-24T19:24:58.334Z
---

## 2026-07-24 вечер — БЛОКИ B и C ЗАКРЫТЫ, иконочный хвост исчерпан
- **Блок B · P3 two-state** билд `-24-4` `63ccdff`. `IC.gothEye` распался на
  `gothEyeHalf`/`gothEyeOpen`; свап через единый `eyeGlyph(narrowed)` (01-core,
  сразу после `IC`, добавлен в верхний `Object.assign(globalThis,…)`).
  **Грабля разведки: тулбар-фильтр НЕ в `04-tasks.ts:109`** (там кнопка
  sub-notes) — он **инлайн-svg `index.html:504`**, `#btn-filter`, стейт
  `isFiltered`. Пять точек свапа: `01:loadUiState` (восстановление на буте),
  `07:toggleFilter` (клик), `04:109` (рендер меты), `04:1988` (живой тогл),
  `05:272` (ленивое создание кнопки).
- **⚠ Фикс инверсии `5a5a24f` (билд `-5`) — юзер поймал.** Аргумент `eyeGlyph`
  = «ЧТО ОКО ВИДИТ», а не «нажата ли кнопка». Флаги двух кнопок ПРОТИВОПОЛОЖНЫ:
  `isFiltered=true` = список сужен → веко опущено; `subNotesAlwaysOpen=true` =
  заметки РАСКРЫТЫ → око ОТКРЫТО (`eyeGlyph(!task.subNotesAlwaysOpen)`).
  **Урок: для two-state глифа проверять семантику КАЖДОГО потребителя
  отдельно — общий «active» ≠ общий смысл.**
- **Блок C · BL2** рерабока `7ebb87e` (принята юзером с первого показа) →
  вживление билд `-6` `62058fb` в `GIC.barLvl.{auto,open,closed}` (02-grimoire,
  тумблер тулбара Гримуара; потребители не трогались — оба через
  `GIC.barLvl[grimBarMode]`, инлайн-копий старого свитка нет).
  4 правки юзера: пунктир auto → сплошная; засов у open убран; кольцо у closed
  убрано + пики решётки 17.6→18.9 (до нижней кромки планки); содержимое планки
  переработано на СОБСТВЕННЫЕ мотивы DUSK — стрельчатая арка с окулюсом ·
  крест pattée идиомой BD1 · флёр-де-лис идиомой BM1.
  **Приём:** в генератор добавлен `tidy()` = `s.replace(/\d+\.\d{3,}/g, …)` —
  вычисленные координаты дают хвосты `6.9799999999999995`; после tidy превью и
  приложение совпадают символ в символ, вживление = машинная выемка svg из
  сгенерённого html (`indexOf('>BL2 auto<')` → `<svg…</svg>`), не ручной копипаст.
- Ратчет отработан оба раза (189 глифов; роли `P3` ×2, `BL2` ×3).
- `npx tsc --noEmit` шумит 27 строк (`ACT` redeclare, `_lp*` в 08) — это
  **baseline**, проверено `git stash`: до и после правок одинаково. Не пугаться.

## 2026-07-24 добивка — БЛОК A ЗАКРЫТ (билды -24-1 и -24-2), юзер принял
- **BD1 гроб** `73ca94b`: параметрический билдер `coffinSVG(w,filled,cycle)`.
  Приём для параметрических глифов: арт из превью авторен на ФИКСИРОВАННОЙ канве
  (тут 21×27), поэтому в билдере он живёт как `k = w/21` и каждая координата =
  арт-значение × k → при w=21 совпадение с превью до 0.01, при w=14/18
  (подпункты/архив) корректно ужимается. Обводки НЕ масштабировал (в билдере они
  и раньше были константами), радиусы точек — масштабировал.
  Правило «все точки» сработало и тут: `cycleCoffinSVG` — вторая копия тела гроба
  (его комментарий обещает «identical coffin body»), получила ту же столярку,
  метка осталась своим return-cross.
- **BI1 + BM1** `fb8447b`: степперы dl-dur ×4 + углы ×4 (`index.html`), флёр-де-лис
  в `GIC.dividerFleur` (`02:215`). Углам пришлось добавить `stroke="currentColor"`
  на root — дети BI1 наследуют обводку, у старого глифа она была на path.
- **Грабля разведки:** доки 07-18 заявляли у BM «CSS-дубли style.css:371/8134» —
  ИХ НЕТ. 371 = другой орнамент (полоса 160×14 под шапкой: линии+ромбы+кольцо),
  8134 = blockquote. Проверять заявленные дубли grep'ом по ФРАГМЕНТУ пути, а не
  верить строкам; лишняя «синхронизация» испортила бы чужой глиф.
- **Полоса под шапкой** `f59c01a` (билд `-24-3`): юзер велел синхронизировать —
  `.app-header::after` (единственная точка, `style.css:~373`, URL-encoded data-URI)
  переведена с «ромб+бусины» на тот же BM1. Приём: арт 40×12 транслируется в
  канву 160×14 сдвигом +60,+1, альфы ЗАПЕКАЮТСЯ (в data-URI нет currentColor):
  штрих 0.48, усики 0.26, сердцевины колец 0.55, эхо 0.22 от `rgba(130,50,220,…)`.
  Фланговые правила и след орнамента 64..96 сохранены = ритм шапки не поехал.
  Проверка data-URI без браузера: выдрать все `url("data:image/svg+xml,…")`
  регуляркой, `decodeURIComponent`, найти нужный по viewBox и сверить счёт
  элементов/закрытий (первый матч в файле — шумовая текстура, не он).
  **Ждёт прод-ревью юзера.**
- **Косяк процесса:** PowerShell-here-string `@'…'@` в Bash-туле НЕ раскрывается —
  сабжект коммита получил префикс `@`. Для многострочных сообщений в Bash-туле
  писать файл и `git commit -F <файл>`.

## Сессия 2026-07-24 — блок A иконок (билды -2…-6), ЮЗЕР ПРИНЯЛ ВСЁ
Задеплоено и **отсмотрено юзером на проде** («все иконки прошли ревью»):
**-2** AX1 кованая цепь → `IC.drag` · **-3** FL2 «кодекс-разворот» →
`GIC.focusLvl[0..2]` (фокус-цикл Гримуара: [0] разворот · [1] список в кромку ·
[2] заметка во весь экран) · **-4** BB1 ключ ризницы + BC1 чешуя-глаз синка
(стейт-классы `.brow/.lids/.ball/lid-up/lid-lo/tear/iris/pupil/glint` целы —
свапнуты ТОЛЬКО декоративные внутренности) · **-5** BH1·BR1·BE1(symbol,
shared-градиенты)·BQ1×2·BJ1×2·BF1×2 · **-6** BG1 сплит-заголовки (03-const +
07-инлайн, `.split-chevron`=AU-меч НЕ тронут) + BK1 тома fold (02-билдер +
инлайн `gt-open/gt-closed`).

**Рабочие приёмы, подтверждённые этой сессией (механика ≠ вкус — п.3е не триггерить):**
- Ратчет: `node scripts/icon-inventory.mjs --update` даёт только счётчик. Чтобы
  получить ХЭШИ новых глифов — `node -e "const {diff}=require('./scripts/icon-inventory.mjs');
  diff().unknown"` ДО апдейта (после апдейта они уже в реестре и не ищутся).
  Роли проставлять скриптом с `JSON.stringify(d,null,1)+"\n"` — формат реестра.
- `role` в реестре тест НЕ проверяет (гейт = только присутствие хэша), но
  проставлять обязательно по процессу.
- Строки из разведки 07-18 УСТАРЕЛИ (файлы дрейфуют) — каждый пик искать grep'ом
  по фрагменту пути старого svg; билдер+инлайн-дубль после правки дают ОДИН хэш
  (подтверждение, что заменены обе точки).
- Превью-обвязка ≠ приложение: у арта из fb3/fb4 срезать `width/height`, сохранять
  апповые атрибуты (`width="15"`, `class=`, `aria-hidden`).

# Сессия 2026-07-19 — drawer + ратчет + мини-глифы (последний бесплатный лимит)

## Решения юзера (обсуждение перед стартом)
- **Effort = high** (xhigh отклонён: прецедент Ф3, покрытие лимита важнее).
- **Drawer вживлять сразу**, не ждать вердикта превью (правки по прод-ревью потом).
- **Иконки Fable НЕ забирает** — только рисование AZ/focusLvl (AX = готовый fb3-арт,
  Opus вживляет). Coverage-проблема = ратчет (сделан) + ADDENDUM v3 + точечная
  добивка Opus'ом.
- **Пуши ВЫМЕСТИЛИ календарь**: push-уведомления = флагман платного П1;
  календарь → П2. Обе спеки пишутся в этом лимите (PUSH первой).
- **Календарь = гибрид**: агенда-лента (мобила, фаза 1) + месячная сетка (широкий, фаза 2).
- **В реестр приняты**: пуш-уведомления, TWA-иконка+splash (Fable, «как хочешь можно
  сегодня хвостом»), README-витрина (Opus), Web Share Target + manifest shortcuts (Opus).
- **Стрик** — юзер сделает Opus'ом, не приоритет. **Master-detail** — ценен, но ниже
  приоритетом (П2/П3); объяснение дано, принято.
- **Хэндофф инкрементально** (не последним пунктом), ратчет — сразу (сделано).

## Сделано + задеплоено
1. **Ратчет-инвентарь** `84c7a01`: `scripts/icon-inventory.mjs` (все svg-литералы
   index.html/style.css/dusk/src, raw + URL-encoded CSS, sha-хэш) +
   `audit-v2/icon-registry.json` (187 глифов/246 вхождений; census видел ~164 = дрейф
   подтверждён) + `tests/icon-ratchet.test.js` (unknown-хэш = красный npm test).
   `--update` сохраняет роли. Боем проверен: поймал новый drawer-глиф.
   **Opus в O-FINAL: прогонять --update после вживлений + проставлять роли.**
2. **W4 drawer «Створка»** `7073e0c` build **2026-07-19-16**, тег отката
   `v2.5-pre-drawer`: `.side-rail` = fixed-панель ≤820px (тот же DOM: рельса ≥1440 /
   инлайн 821-1439 / drawer ≤820). Триггер = кромка-таб #drawer-tab (глиф «дверное
   кольцо на пластине», в реестре) + свайп от кромки ≤24px; закрытие: скрим/Esc/
   свайп-влево/фокус-группы/дайджест/нав-таб. inert на закрытой панели (только
   мобильный тир). Дайджест «Грядущее» + gp-count теперь и в drawer (стили rd-*
   вынесены из ≥1440-медиа в общий скоуп, display-гейт по тирам). FAB'ы (sync-glyph/
   btn-sound/pen-sound/shortcuts z50 body-уровня) глушатся при открытой створке —
   панель заперта в стекинг-контексте .todo-app, FAB иначе всплывает над скримом.
   Капканы, пойманные пробой: coarse-тир растит кнопки до 44px → toolbar-group-btns
   нужен wrap; #search-box flex min-width капкан. JS в 08 (низ файла), ACT расширен
   drawerToggle/drawerClose. Пруф `D:\tmp\pw\b1\w4_drawer.mjs` 10/10, vitest 37/37.
   **ЖДЁТ прод-ревью юзера на телефоне.**
3е. **УРОК ТЕМПА 2026-07-20 — УЗКО про ВКУС/РИСОВАНИЕ иконок и субпиксельную
   геометрию, НЕ про механику (смягчено 2026-07-22 по прямому слову юзера:
   «ты будешь впустую всегда на него триггериться»).** Применять ТОЛЬКО когда
   работа творческая/вкусовая/суб-пиксельная (форма глифа, выравнивание кромок,
   заполнение серпов): там правка → одна проба-замер на реальном DOM → результат,
   без перебора вариантов на глаз. **НЕ применять к механике** (вживление готового
   арта по известной схеме, замена строк, рефактор): там риск низкий, лишние
   пробы/перестраховка = впустую жгут токены — делать прямо, верификация = vitest
   ратчет + build, без харнесс-пробы на каждый шаг. Конкретика того случая
   (пилюля/уроборос-нюансы съели лимит+): замерять СРАЗУ, не с 4-й попытки
   (`_pillseam.mjs` DPR8 нашёл причину за прогон); не «чинить root cause» ценой
   поведения (вытягивание кнопки `7c4e869` сломало слайд-аут → откат `c506bdb`);
   итог обводки `top:-0.5px` (`ef499f8`) — не идеал, но юзер принял ради лимита.
  
3д. **✅ 2026-07-20 — ОБЕ ТЕМЫ РАСКРЫТЫ ЗАНОВО ПО ПОСТАНОВКЕ ЮЗЕРА (откат 3г снят).**
   Билды -1…-10, финал `ef499f8`. Свайп-фикс створки — `91ce1a3`.
   · **Пилюля групп ЗАКРЫТА** (юзер: «пил нормально»). Задача была: НЕ сливать,
     а заполнить серпы между круглым торцом пилюли и кнопкой-гробом телом самой
     кнопки. Итог: `.btn-pill-delete::before` — полоса 13px, `background:
     var(--pill-bg)` (инлайн из рендера), `border-top/bottom` + `border-color:
     inherit`, вырез торца через `mask: radial-gradient(circle 13px at left
     center…)`, `top/bottom:-1px` (абсолют от padding-box → иначе обводка на
     пиксель ниже), `overflow:visible` на кнопке в обоих рельса-медиа.
     ТРИ провальные итерации до этого: (1) заполнитель на кнопке без
     overflow/цвета → невидим, (2) на wrap с своим цветом → не совпал по яркости
     и без обводки, (3) без -1px → обводка на пиксель ниже.
   · **Уроборос AZ6 — превью `audit-v2/previews/fb4-az4-uroboros.html`,
     генератор `audit-v2/previews/gen-az5.mjs` (геометрия считается, НЕ рисуется
     руками — правка = число).** Требования юзера, все выполнены: ровное
     замкнутое кольцо; кончик хвоста немного виден в пасти и СУЖАЕТСЯ, сходясь в
     точку ровно в глубине пасти; никакой отдельной «шеи» (была шире тела, с
     гэпом и плотнее полупрозрачного тела); нижняя челюсть опущена и укорочена;
     язык не висит в воздухе и не пересекает ни хвост, ни челюсть, ни кольцо;
     впуклая (вогнутая) галтель затылок↔тело. Ключевое решение: сужение = СПИРАЛЬ
     (радиусы кромок 8.15→7.55 и 6.85→7.55 на последних ~54°), стык с дугой
     касательный → излом невозможен по построению. Голова = принятая 8da6acd,
     не перерисована (посадка ×1.25, rot 8°). cycleReturn = тот же глиф меньше (вживлён как
     AZ6 **bare** — без чешуи, для 9px cycle-until бейджа).
     **✅ ВЖИВЛЁН 2026-07-22 `8065ee1` build -22-1.** IC.ouroboros (билдер →
     repeat-теги/кнопки/шиты + **тост обновления index.html:1794** авто),
     IC.cycleReturn (04:127), И инлайн-копия старого глифа в кнопке «Еженедельно»
     модалки повтора `index.html:1420`. Ратчет `--update` + роли (хэши
     `618850e…`=AZ full, `7cfe38d…`=AZ bare) тем же коммитом.
     **⭐ ПРАВИЛО (юзер 2026-07-22): замена редизайн-иконки на новую = заменить
     старый глиф во ВСЕХ точках, не только у основной роли — билдер (покрывает
     потребителей через IC.), ПЛЮС инлайн-копии, ПЛЮС тосты/модалки. Искать
     grep'ом по ФРАГМЕНТАМ пути старого svg (не только по имени IC.*), иначе
     инлайн-дубли остаются старыми. То же для всех будущих icon-замен.**
   · Урок: юзер требует темпа — считать зазоры числами и рендерить пробу
     (`D:\tmp\pw\_az4shot.mjs` + playwright-core, запускать ИЗ `D:\tmp\pw`),
     а не перебирать варианты на глаз.
   · **W4 drawer: жест открытия ЧИНЕН `91ce1a3`.** Не работал root cause'ом:
     зона старта была `clientX ≤24px`, а системный back-жест Android съедает
     первые ~20px. Теперь открывают кромка ≤40px (порог 30) ИЛИ свайп вправо
     откуда угодно по «спокойной» области (порог 70) — исключены
     `.todo-item/.subtask-item` (DnD), поля ввода, шиты, сама рельса; добавлен
     замок `|dx|>|dy|`. Проба `D:\tmp\pw\b1\_dwswipe.mjs` (кромка/середина
     открывают, короткий свайп нет). **Юзер вердикт: «свайп и уроборос отлично».**
   · Пилюля, финальное состояние: заполнитель = `.btn-pill-delete::before`,
     `right:100%; top:-0.5px; bottom:-1px; width:13px`, `background:var(--pill-bg)`,
     `border-top/bottom:1px` + `border-color:inherit`, вырез торца `mask:
     radial-gradient(circle 13px at left center,…)`, `overflow:visible` на кнопке
     в обоих рельса-медиа, `--pill-bg` инлайном из `renderGroupBar` (03-render).
     **НЕ трогать геометрию самой кнопки — сломается слайд-аут.**

3г. **⛔ ОТКАТ (build -23, `c495ddd`) — исторический, СНЯТ пунктом 3д.**
   Итерации по ПИЛЮЛЕ групп и по ГОЛОВЕ ЗМЕИ провалились (4+ круга правок,
   юзер прекратил работу по обеим темам). Откачено байт-в-байт:
   · `style.css` + `dusk/03-render.ts` → состояние `7673644` (ДО всех моих
     пилюльных правок): слайд-аут кнопки по hover, hoverBg/outBg, исходные
     фон/границы. Всё, что я делал с пилюлей, отменено ПОЛНОСТЬЮ.
   · `audit-v2/previews/*` → `8da6acd` (версия AZ3, которую юзер принял:
     «голова намного лучше»). Последующая переделка головы/хвоста/языка
     отменена.
   **Юзер доделает пилюлю и иконку САМ в новой сессии и САМ пропишет, что и как
   конкретно нужно сделать. Не начинать эти две темы по своей инициативе и не
   реконструировать требования по этой сессии — ждать его постановку.**
3в. **ВТОРОЕ РЕВЬЮ ЮЗЕРА → ещё два фикса (build -19 + fb4 v3):**
   (1) **Пилюля групп и кнопка-гроб не сливались** — ни в рельсе, ни в створке:
   слияние было завязано на `:hover` (слайд-аут), а там тумбстоун СТАТИЧЕН →
   круглый правый край пилюли + отдельная коробка. Фикс `ffd5eb4` build -19:
   в обоих тирах пилюля постоянно держит плоский правый край без правой границы,
   кнопка без левой, фокус-свечение перенесено на `.group-pill-wrap` (`:has`),
   иначе кольцо резало слитую капсулу. Пруф `pill_fuse.mjs` (gap=0, границы 0,
   фон идентичен) на 390 и 1500.
   (2) **fb4 v2 ЗАРУБЛЕН** («AZ2 мультяшно и примитивно», «cycleReturn2 ни на
   что не похожа»; FL1 и BL1 «принимаются, но ожидал большего»). Юзер приложил
   РЕФЕРЕНС-гравюру уробороса. Переделка `526a9a0` = **fb4 v3**: AZ3 по
   референсу в двух трактовках (a сплошной штрих / b гравюрные кромки с чешуёй),
   cycleReturn3 микро-уроборос, **FL2 «кодекс-разворот»** и **BL2 «кованая
   шторка над инструментами»** как новые альтернативы (FL1/BL1 оставлены).
   Генератор геометрии заархивирован в репо: `audit-v2/previews/gen-fb4-v3.mjs`.
   Уроки рисования: на 24px «лента с фактурой внутри» невозможна (обводки
   слипаются) — либо сплошной штрих, либо двойные кромки ≥2 units врозь;
   в верхней четверти иконки держать МИНИМУМ линий (надбровье/ноздрю убрал);
   голова должна ВЫРАСТАТЬ из торца кольца, иначе узел из пересечений.
3b. **ДОБИВКА ПОСЛЕ ревью-фиксов** (`ed881fe`, build **-18**, доделано Opus'ом в
   роли Fable — лимит Fable кончился): проба на 360/768/1000 вскрыла, что реплика
   рельсы была НЕПОЛНОЙ — кластер **ДАННЫЕ** прятался (F2-правило для тесного
   ГОРИЗОНТАЛЬНОГО тулбара) и вместо него жил ⋯-заместитель. Вернул все 4 кластера
   как на десктопе, ⋯ убрал (дубль действий). + режим выбора теперь закрывает
   створку (bulk-бар за панелью), тумблеры-фильтры по-прежнему нет. Пробы:
   `w4_drawer.mjs` 12/12 (+2 ассерта), `w4_widths.mjs` 13/13 (новая: вылезания,
   лейблы, швы, пилюли, средний тир не протёк). Z-ловушек нет: тост 400 / шиты 320
   > скрим 185 — undo из створки доступен.
3a. **РЕВЬЮ ЮЗЕРА + ФИКСЫ (конец сессии):** drawer-ревью «кромке не хватает
   прозрачности + тулбар/группы должны повторять десктоп-рельсу 1-в-1» →
   **ИСПРАВЛЕНО И ЗАДЕПЛОЕНО** `c20dd03` build **-17**: полная реплика рельсы в
   ≤820-медиа (лейблы ВИД/ФИЛЬТР/ВЫБОР display:block против coarse-глушилки,
   :has(#btn-filter)-обрубок перебит той же ID-специфичностью, группы-плита
   рядами, max-width:100% против coarse-капа 45vw B1-05, кромка-таб
   полупрозрачная+blur). fb4 v1 (AZ/cycleReturn) юзер ЗАРУБИЛ («не змея,
   детализация на нуле») → **ПЕРЕДЕЛАНО** `07e5414`: fb4 v2 = AZ2 (тело-лента
   2 контура + чешуя по кругу + клин-голова вдоль кольца: глаз/бровь/ноздря/
   клык/пасть-на-хвосте/зачёсанные рога) + cycleReturn2 (микро-змей 10×10) +
   BL1 портикулис ×3 (юзер сказал barLvl «можно было бы») + FL1 принят v1.
   Генератор геометрии = scratchpad/gen-fb4.mjs (сессионный, тривиально
   пересоздаваем: тригонометрия чешуи по дуге). **fb4 v2 ЖДЁТ вердикта.**
3. **fb4 мини-превью v1** `c0c2aff` (superseded v2, история): `audit-v2/previews/fb4-mini-glyphs.html` —
   AZ1c змей-круг (голова+глаз+рога, хвост в пасти, чешуя; по согласованной
   рекомендации «один круг») + cycleReturn производный (круг-возврат 9px, был ромб) +
   FL1 складень-диптих ×3 (леджер/письмо-строки, ромб-финиал активной створки,
   петли, скоба-возврат в [2]) + barLvl идея-портикулис (словами, не форсировано).
   **ЖДЁТ вердикта юзера → вживляет Opus** (для focusLvl: btn 02:709 + 02:1130,
   grimFocus 0..2; для AZ: IC.ouroboros 01:~510 + IC.cycleReturn 01:521, потребители
   03:2204, 04:93/127/182/509/696/1445/2302/2520, index.html:1794).

## Бумаги — СДЕЛАНЫ и задеплоены
4. **PUSH-SPEC.md** `ef3e447` (корень репо): syncId-идентичность, KV-схема,
   cron, RFC8291-план с тест-векторами, фазы Ф1-Ф5 (Ф1 работает без
   payload-крипты — пустой пуш). Механика Opus, Ф4-UX = Fable П1.
5. **CALENDAR-SPEC.md** `ef3e447`: calendarItems чистая fn (уроки B6-06 _ymd),
   агенда-часослов Ф1 → сетка Ф2 (≥900) → готик-слой Ф4 = Fable П2.
   Новый модуль dusk/13-calendar.ts; только по явному «go» юзера.
6. **OPUS-QUEUE §W4-FABLE-HANDOFF** `a9faf50`: ратчет-процедура (--update+роли
   тем же коммитом), fb4/AX1 вживление после вердикта, TWA-добавки
   (shortcuts+share-target+push-смок), README, календарь-гейт.
7. **TWA-иконка+splash — ОТЛОЖЕНА осознанно** (юзер разрешил «как хочешь»):
   самый видимый ежедневный ассет заслуживает полноценного превью-цикла, не
   хвоста на остатке контекста → платный П1/П2, Fable.

## Платные лимиты (утверждённая рамка)
П1 = правки drawer по ревью + пуш-UX/дизайн; П2 = календарь-дизайн + ратификация
Opus-пачек; П3 = резерв (master-detail/типографика/хвосты).

Связи: [[remaining-scope-map]], [[batch10-backlog]], [[plan-2026-07-17-final-limits]].
