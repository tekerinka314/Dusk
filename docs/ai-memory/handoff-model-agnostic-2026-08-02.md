---
name: handoff-model-agnostic-2026-08-02
description: "⭐⭐⭐ 2026-08-02 ПАКЕТ ПЕРЕДАЧИ (`3debf75`): у юзера кончается подписка на Claude Code, дальше возможен Gemini/Antigravity (вариант вернуться к Claude НЕ закрыт). AGENTS.md = единственный источник законов, CLAUDE.md/GEMINI.md = указатели; память и зонды перенесены В РЕПО; `npm run gates`."
metadata:
  type: project
---

**Слово юзера 2026-08-02:** подписка кончается, работа над DUSK со мной
прекращается; есть Pro на Gemini, возможен переход на него и Antigravity вместо
Claude Code. Просил подготовить всё к переходу так, чтобы не потерять память,
контекст и наработки и чтобы качество не просело. **Вариант вернуться ко мне
просил НЕ закрывать** — поэтому всё сделано нейтрально, а не «под Gemini».

**Что сделано (`3debf75`):**
- `AGENTS.md` — единственный источник законов проекта (перенос `CLAUDE.md` без
  привязки к скиллам Claude + новые разделы: дисциплина перед задачей, гейты,
  зонды). `CLAUDE.md` и `GEMINI.md` — тонкие указатели (`@AGENTS.md`) плюс своя
  специфика. Правила больше не могут разъехаться в двух копиях.
- `HANDOFF.md` — состояние, очередь, пять привычек качества, капканы.
- `docs/ai-memory/` — копия приватной памяти Claude Code (51 файл). ⚠ **Новые
  записи писать в ОБЕ папки**, пока работает Claude, иначе следующий исполнитель
  их не увидит.
- `tools/probes/` — 228 зондов, скопированы из `D:\tmp\pw` (ВРЕМЕННАЯ папка —
  могла исчезнуть в любой момент; в `lib.mjs` абсолютные пути под машину юзера).
- `npm run gates` = test → types → build → icons → e2e одной командой;
  `scripts/tsc-baseline.mjs` делает базлайн типов (27) машинно проверяемым:
  падает на РОСТЕ, подсказывает опустить базлайн при уменьшении.
- Стенограммы 57 сессий (289 МБ) → `D:\DUSK-ai-archive\transcripts\`; после
  этого `C:\Users\serge\.claude\` можно удалять безболезненно.

**Вывод, который стоит помнить:** качество здесь держалось не на модели, а на
ритуалах (красный замок до фикса · замер вместо рассуждения · находка аудита =
гипотеза · вкус за юзером · память после каждой единицы работы). Ратчеты и гейты
переносят часть этого механически — их нельзя отключать «чтобы пройти гейт».
