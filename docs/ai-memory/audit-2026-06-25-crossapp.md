---
name: audit-2026-06-25-crossapp
description: "Аудит-4 (2026-06-25) DUSK+Grimuar с нуля по коду — согласованный план фиксов для сессии внедрения; что делать, что обсуждать отдельно, что gated"
metadata: 
  node_type: memory
  type: project
  originSessionId: 02755f52-20d4-4bd9-8bdc-285f24a83684
---

# ⭐ Аудит-4 — 2026-06-25 — DUSK + Grimuar (cross-app), СОГЛАСОВАННЫЙ план для внедрения

> **ОБНОВЛЕНИЕ 2026-06-25 (поздняя сессия) — POLISH-БЭКЛОГ ИСЧЕРПАН.** Транш-1 (F-A/D-1/F-B) и
> Транш-2 (D-4/X-2/X-6) ЗАКОММИЧЕНЫ (`9518aff`,`cd23502`). Сверх плана закрыт **весь блок дедлайна**:
> X-7 окно-горение (`deadlineWindow`→live/over, секунды в последней минуте), X-8 общий авто-повтор-тумблер
> (time→ежедн./weektime→еженед./monthday→ежемес., `_AUTO_REPEAT_BY_MODE`), пульс на `.meta-tag-wrap`,
> валидация полей, и дизайн длительности **мини-панель «Свеча»** (2 свечи + стрельчатые арки-степперы
> `_stepDlDuration` + кинжал-разделитель + углы; `#dl-duration-row`) — коммиты `6cf2245`,`60c73ac` (push).
> Тесты: dur-wire 14/14, X-7 14/14, X-8 16/16 (один флап `#5` по wall-clock), audit3 103/103.
> ОТКРЫТО только: X-4 (обсудить, польза~0), отложенные F-D/F-E/F-F/X-1, GATED-стадия (F-C uuid→7c→синк).
> Следующий шаг — НЕТ мелкой полировки; либо отложенные рефакторы по желанию, либо «go» на gated-стадию.

Сквозной повторный аудит обоих приложений (задачи DUSK + заметки Grimuar) с нуля по фактическому коду
(не по старым выводам). Ветка `fix/ui-repeat-meta-subtasks`. Критичных багов потери данных НЕ найдено —
код зрелый, правило «не теряем данные» соблюдается (undo везде, `taskFromArchive`, кольцевой бэкап P-C,
`_grimVersions` в `exportData`). Этот файл = исходная база для отдельной сессии ВНЕДРЕНИЯ. Связан с
[[audit-2026-06-24-audit3.md]] (предыдущее состояние — весь аудит-3 закрыт).

## Что прочитано (карта)
- `index.html` целиком (1443). `app.js` (13630) — полная карта функций + построчно 1–9215, 11029–12144,
  13027–13870 (весь Grimuar 1440–5240; ядро DUSK: render/filter/sort/schedule/split/pinned, createTaskEl,
  подпункты, addTask/toggleCheck, повторы getNextResetTimestamp/checkCycleResets, _syncParentDone,
  дедлайн-математика getDeadlineTimestamp/deadlineStatus/calDayDiff, DnD, confirmDeadline, quick-add,
  клавиатура, exportData). `style.css` — карта секций/keyframes/collapse-механик.

## СОГЛАСОВАННЫЙ ПОДХОД (решение пользователя 2026-06-25)
- Делать ТОЛЬКО лёгкие / лёгко-средние, безопасные, однозначно полезные фиксы. Начинать с самого простого.
- Спорные / риск-регрессионные / способные противоречить видению — НЕ трогать без отдельного обсуждения.
- F-B решено: **переименовать + развести** (минимальная правка, без новой кросс-импорт-логики).
- Motion-правки **проверять живьём** через Playwright/headless харнесс `D:\tmp\pw` (рецепт в
  [[audit-2026-06-plan]]/AUDIT-FINDINGS.md): локальный http-сервер обязателен, `__preview.html` без внешних
  ресурсов, успех PNG > 50 КБ, не использовать `--virtual-time-budget`.
- Правила прежние: surgical-правки, готик-стиль обязателен; пользователь сам смотрит визуал в браузере;
  `node --check` перед коммитом; коммит+пуш (трейлер `Co-Authored-By: Claude Opus 4.8`) ТОЛЬКО по просьбе.

## СДЕЛАНО в сессии 2026-06-25 — ЗАКОММИЧЕНО+ЗАПУШЕНО (`9518aff` → origin/fix/ui-repeat-meta-subtasks)
(Grimuar IO-меню: «Полный бэкап записей» в обоих местах — «Перенос» и экспорт выбранных.)
- **F-A ✅** — `pen-asset.js` + `app.js` перенесены в конец `<body>` (index.html, после import-choice-overlay);
  убраны из позиции после inline-SW-скрипта. `node --check app.js` чисто. ⚠️ нужно разово открыть приложение
  и убедиться, что грузится/рендерится (изменён порядок загрузки скриптов).
- **D-1 ✅** — комментарий-заголовок `parseQuickInput` (app.js): `#tag !priority ~date` → `*tag %date !priority`.
- **F-B ✅ (РАСШИРЕНО по уточнению пользователя — экспорт-меню с двумя scope):**
  * DUSK-экспорт теперь = **поповер по клику** (одна иконка тулбара `#btn-export` → `openExportMenu(event)`
    через готовый `_openFloatMenu`, класс `export-menu`). Две строки: **«Только задачи»** (`exportData('tasks')`,
    файл `dusk-tasks-<date>.json`) и **«Всё — полный бэкап»** (`exportData('all')`, `dusk-backup-<date>.json`).
    Глиф «только задачи» = `_EXPORT_TASKS_IC` (гробик, без width/height), «всё» = `GRIM_IO_IC.full` (ларец).
  * `exportData(scope)`: 'tasks' = ВСЕ данные DUSK (задачи/группы/архив/заметки задач/подпункты/шаблоны
    задач/сорт/счётчики), деструктуризацией ВЫНУТЫ только данные Гримуара (notes/notesArchive/noteTemplates/
    notesSort/_grimVersions). 'all' = прежний полный бэкап {...state,_grimVersions}.
  * **Импорт «Заменить» теперь сохраняет данные Гримуара**, если файл их не нёс (`keepGrim = !('notes' in
    loaded) && !('notesArchive' in loaded)` → запоминаем и возвращаем notes/notesArchive/noteTemplates/notesSort;
    `_grimRestoreVersions(replace)` зовём ТОЛЬКО при !keepGrim; `delete state._grimVersions` всегда). Правка в
    ОБЕИХ replace-ветках (`_showImportChoiceModal` + fallback). Полный бэкап (есть notes) — заменяет как раньше.
  * Grimuar IO-меню: «Полный бэкап» → «Бэкап записей» (desc «все/выбранные записи Гримуара + летопись»).
  * Понятные сообщения при «не том» файле: DUSK-import видит `_grimFull` без `.tasks` → шлёт в Гримуар;
    Grimuar-import видит `loaded.tasks` → шлёт в задачи.
  * Проверено Node-тестом (11/11): tasks-export отбрасывает только Гримуар и хранит все данные DUSK;
    replace tasks-only сохраняет записи+склеп+шаблоны+сорт+летопись; replace полным — заменяет. Файл теста
    `/tmp/fb_test.mjs` (WSL/bash) — сессионный.
  * Видимые изменения (глянуть в браузере): поповер на кнопке экспорта, два пункта, имена файлов, тосты.
- **X-4 ⟂ ПЕРЕНЕСён в «обсудить отдельно»**: при правке выяснилось, что это НЕ чистый дедуп —
  `IC.crossedSwords` ≠ `GIC.dismiss` (разные `d`/stroke-width), а меч-chevron'ы делят только path, но имеют
  разные классы-обёртки (поворот-при-collapse/размер). Свести = визуальное изменение + регресс-поверхность,
  пользы юзеру 0 (только поддержка). Под критерий «точно полезно и не рискованно» не проходит.

## ТРАНШ 1 — делаем первыми (безопасно, согласовано)
- **F-A. Порядок загрузки скрипта.** `index.html:1375` грузит `app.js` синхронно ДО 5 модалок (color-filter,
  templates, backup, bulk-group, import-choice; 1376–1439), а `init()` зовётся в конце app.js (`:14524`) —
  узлов ещё нет в DOM. Работает лишь из-за ленивого доступа. Фикс: перенести `<script src="app.js">` в конец
  `<body>` (после всех модалок) ИЛИ добавить `defer`. Перепроверить, что `pen-asset.js` остаётся ДО app.js.
  Риск минимальный. ⚠️ есть отдельный `DOMContentLoaded`-хендлер (`app.js:10252`) — учесть при переносе.
- **D-1. Устаревший комментарий** `parseQuickInput` (`app.js:13115`): «#tag !priority ~date» → фактически
  `*тег %дата !приоритет`. Просто поправить текст комментария.
- **X-4. Дубли SVG-глифов.** Готик-меч-chevron определён инлайн многократно: `GRIM_SORT_SWORD` (~2032),
  `_PIN_HDR_CHEVRON` (~5453), `chevronSvg` в `_buildSubListContent` (~7107), крипт-месяц chevron (~2287),
  архив-месяц chevron в renderArchive (~6655), HTML-пикеры (index.html много раз). `IC.crossedSwords` ≡
  `GIC.dismiss` (один path, два определения). Свести в один реестр и переиспользовать. Низкий риск, чистка.
- **F-B (вариант «переименовать+развести»).** Две кнопки «полный бэкап» дают НЕсовместимые JSON:
  DUSK `exportData` = весь стейт (задачи+заметки+история), Grimuar `grimExportFullBackup` = только заметки;
  взаимный импорт отвергается. Согласовано: НЕ делать кросс-приём, а РАЗВЕСТИ названия/подписи так, чтобы было
  однозначно — DUSK-экспорт = «полный бэкап всего», Grimuar — «бэкап только заметок». Правка строк/тостов,
  возможно подписей кнопок; новую логику импорта НЕ добавлять.

## ТРАНШ 2 — РЕЗУЛЬТАТ (решение пользователя 2026-06-25: «берём все кроме среднего риска и скипа»)
Сделано в коде (на ветке, **ещё НЕ закоммичено** — ждём ревью визуала тумблера пользователем; коммит только по «комить»):
- **D-4 ✅ ИСПРАВЛЕНО+воспроизведено.** `renderTasks()` теперь первой строкой зовёт `_closeSortPicker()`
  (app.js ~5276) — иначе при фоновом `render()` (cycle-reset) портал-список группового сорт-пикера
  оставался висеть осиротевшим во `<body>`. Repro `D:\tmp\pw\_d4repro.mjs`: до=1 видимый orphan → после=0.
- **X-2 ✅ СДЕЛАНО.** В поповер экспорта (`openExportMenu`) добавлен 3-й пункт **«Задачи — markdown-чеклист»**
  (`exportData('md')` → `_exportTasksMarkdown`/`_tasksToMarkdown`, файл `dusk-tasks-<date>.md`). Группы,
  вложенные подпункты, суффикс дедлайна `(до: …)` через `formatDeadlineAbsolute`. Новый готик-глиф
  `_EXPORT_MD_IC` (пергамент-чеклист, мотив свитков). Лосси/человекочит., НЕ реимпортится. Тест `_x2check.mjs`.
- **X-6 ✅ СДЕЛАНО+РАСШИРЕНО (по уточнению пользователя — это НЕ было product-решение).** Авто-повтор
  weektime→weekly теперь **ВКЛ по умолчанию ВЕЗДЕ**: существующая задача/подпункт/форм-подпункт (уже было)
  + **добавлено**: путь СОЗДАНИЯ через форму (`confirmDeadline` else-ветка → `setFormRepeat('weekly')`+
  `_formWdPickerSet`) и **quick-add `%пн`** (`addTask`: `_effRepeat/_effAnchorDay`). Новый **готик-тумблер**
  `#dl-repeat-toggle` в секции «День+Время» дедлайн-модалки (`_dlAutoRepeat`, `toggleDlAutoRepeat`,
  `_setDlAutoRepeat`; CSS `.dl-repeat-toggle.on`). По умолчанию ВКЛ, выкл = нестандарт; при ре-редактировании
  задачи с weektime+repeat='none' тумблер отражает прежний отказ (default OFF). Все 4 weektime-ветки
  `confirmDeadline` гейтят авто-weekly на `_dlAutoRepeat`. Экспонирован `window._formWdPickerSet=setDay`.
  Тест `_x6check.mjs` 12/12. Скрины тумблера вкл/выкл `D:\tmp\pw\_x6_toggle_{on,off}.png` — готик-ок.
- Регрессы: audit3 **103/103**, оффлайн pen **6/6**. `_t1check.mjs` 8/9 — единств. «fail» устаревший
  (ждал 2 пункта экспорта, теперь 3 из-за X-2; перекрыт `_x2check.mjs`). НЕ регресс.
- **ОТЛОЖЕНО (решение пользователя 2026-06-25, не делать без отдельной просьбы):** **F-D** единый collapse
  grid-rows, **F-E** фабрика готик-пикеров, **F-F** общий архив-helper (все средний риск, польза юзеру ~0),
  и **X-1** встроенные шаблоны задач (низкая ценность). Не заброшены — вернуться только по «go».

## ТРАНШ 2 — обсудить ОТДЕЛЬНО перед внедрением (риск/видение) [ИСХОДНЫЙ список, статус выше]
- **F-D. Единый collapse на `grid-template-rows:0fr↔1fr`** (отложенный 6a). Сейчас ДВЕ системы для одного
  жеста: max-height+JS-замер (`extra-fields`, `group-body`, `subtask-section`, **архив-месяцы задач**) vs
  grid-rows (sub-note, task-note, split-зоны, **крипт-месяцы заметок**). Даже архив-месяцы в задачах и в
  склепе реализованы по-разному. grid-rows иммунен к джиттеру при смене высоты во время анимации. Системная
  чистка motion + ~80 строк JS, НО средний риск (DnD-ghost, клип форм-пикеров при overflow:hidden) → отдельно.
- **F-E. Фабрика готик-пикеров** `createGothicSelect`. Сорт-дропдаун задач (`toggleSortPicker`, +портал в body,
  +open-up) и Grimuar (`grimToggleSortMenu`, без портала/open-up) — два кодопути одного UI; Grimuar-вариант
  может клипаться/не переворачиваться. Свести месяц/день-недели/сорт-задач/сорт-заметок/группу в одну фабрику.
  Это gated S1-10, обычно идёт с 7c → обсудить.
- **F-F. Общий helper «архив по месяцам»** для `renderArchive` (задачи) и `_grimCryptMonthsHTML` (склеп) —
  дублируют группировку YYYY-MM + сворачивание. Связан с F-D (унифицировать заодно collapse). Обсудить.
- **X-1. Встроенные шаблоны задач** (как Дневник/Ритуал/Кодекс/Скрижаль у заметок). Ценность низкая/спорная.
- **X-2. Мини-меню экспорта задач** («полный бэкап» / «задачи как markdown-чеклист») по образцу Grimuar. Связан
  с F-B; средняя ценность. Обсудить после транша 1.
- **X-6. quick-add `%пн`** (weektime) НЕ включает авто-weekly-repeat, тогда как выбор weektime в модалке —
  включает (V-7). Решить: намеренно (одноразовый) или выровнять.
- **D-4. Краевой случай sort-picker портала:** если `render()` пересоздаёт группу при открытом портал-листе её
  пикера — `_openSortPicker` ссылается на detached-узел до след. outside-click (`_closeSortPicker` чистит
  orphan по `parent.isConnected`). Низкий импакт; проверить живьём, чинить только если воспроизводится.

## GATED — только по отдельному явному «go» (CLAUDE.md это запрещает начинать само)
- **F-C. Idea 8 — uuid/updatedAt/tombstones для задач.** Сейчас задачи int-id (nextId/nextGroupId/nextSubId),
  заметки уже uuid (`uid()`). Merge-импорт задач держится на хрупком `idOffset`-ремапе (app.js:6140+). Это
  фундамент синка (Google Drive appDataFolder). НЕ начинать без go.
- **D-3 / 7c.** Модульный split + `data-action`-делегирование вместо тяжёлого инлайн-`onclick` (в `createTaskEl`
  ~15 хендлеров/задача → все глобальны, мешают строгому CSP). Крупный рефактор, в паре с F-C/Svelte.
- **D-2.** `execCommand` в Grimuar-редакторе deprecated — тех-долг, не срочно.

## MOTION-гипотезы (проверять живьём в браузере; статикой не подтвердить)
- M-1. max-height блоки (F-D) джиттерят при смене высоты контента во время анимации; grid-rows иммунен.
- M-2. `onDragEnd` ghost-settle = инлайн-transition 0.75s (app.js:12080+) — возможен конфликт с
  `applyListStagger`, если render идёт в процессе.
- M-3. NA-4: пин ширины `.grim-page` в px при сворачивании заметки (`grimToggleCollapse` ~1855) — проверить
  артефакт «вертикальной линии» на очень длинных заметках.
- M-4. `switchPage` page-transition: animationend + 600ms fallback на обеих страницах (`:1339–1370`);
  `_pageTransitioning`-гард — убедиться, что нет двойного render.

## Cross-app: уже в паритете (НЕ трогать)
- Цвет-метка с контраст-safe `_grimInk` в обоих (createTaskEl taskInk + архив; заметки тинтят весь leaf).
- Цвет-фильтр (кристалл), ✕-кинжалы очистки поиска, bulk-цвет, клавиатура (J/K/N/E/T/Del, Ctrl+F, F3,
  контекстная подсказка `?`, раскладко-независимость через `e.code`) — перенесены и согласованы в аудите-3.
- Архив-по-месяцам есть в обоих (но код дублирован — см. F-F).
- Асимметрия дедлайны/повторы/schedule/today/split/группы/snooze (DUSK) vs Летопись/find/TOC/focus-режимы
  (Grimuar) — ОЖИДАЕМАЯ (разная природа сущностей), не баг, не переносить.

## Рабочие артефакты
- Durable-список с привязкой к строкам: scratchpad `audit-findings.md` (вне репо, сессионный).
- Репо-реестр старых находок: `AUDIT-FINDINGS.md` (аудиты 1–3, уже закрыты).
