{
  "name": "dusk",
  "private": true,
  "version": "2.0.0",
  "description": "DUSK — offline-first gothic task journal (PWA)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:portable": "vite build && node scripts/build-portable.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "sortablejs": "^1.15.2"
  }
}
