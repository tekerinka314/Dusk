# E2E-SPEC — Playwright-смоук в репо (Этап 5)

**Статус:** утверждён юзером 2026-07-28 (глубина «средний», без пиксельных
базлайнов, прогон локальный, `D:\tmp\pw` остаётся черновиком).
Место в программе — `STRATEGY.md`, блок 2.

## 1. Зачем

`npm test` (27 vitest-файлов) пинит логику, мерж, копирайт, контраст и реестр
иконок — но happy-dom **не считает layout**, не имеет реального service worker,
IndexedDB-бута, настоящих кликов и фокуса. Весь год ломалось именно там:
boot-гонка `V2-B6-01`, жёлоб тулбара Гримуара, drawer, sticky на F3, подсказка
клавиш шириной 1521px, переполнение строк после готических слов.

Смоук — страховочная сеть под блок 5 (пуш + TWA): пуш трогает `sw.js` и бут,
TWA — это WebView с чужой геометрией и своим SW.

**Принцип отбора кейсов:** в репо живут только **инварианты, которые не должны
меняться никогда**. Всё, что меняется вместе с копирайтом или вёрсткой,
остаётся одноразовым зондом в `D:\tmp\pw\b1`.

## 2. Что НЕ делаем (и почему)

- **Пиксельные базлайны.** Системный Chrome автообновляется, шрифты системные,
  DPR 2.625, фон — JPEG с артефактами, дедлайны пульсируют по таймеру. Каждое
  обновление браузера = массовый ложный красный; глушится это порогом
  `maxDiffPixels`, а порог убивает ровно ту чувствительность, ради которой
  базлайн заводят. Вместо картинок — **вердиктные замеры геометрии**
  (перекрытия, overflow, резолвинг `<use>`). Остаточный риск принят: правка
  цвета/тени/радиуса без сдвига геометрии смоуком не ловится — это остаётся за
  прод-ревью юзера и ратчетом иконок.
- **CI на GitHub Actions.** Репо приватное, гейт остаётся дисциплинарным, как
  сейчас у `npm test`.
- **DnD** (перетаскивание задач/звеньев/заметок). Самый хрупкий сценарий;
  реконсиляция уже покрыта vitest'ом. Ручной зонд `_dndintegrity.mjs` живёт в
  черновике.
- **Живой OAuth/Drive.** Проверяем только UI панели синка.
- **Перенос зондов `b1` целиком.** Они писались как «докажи, что ЭТОТ фикс
  сработал» и красят от любой правки текста.

## 3. Устройство

```
playwright.config.mjs        — корень (стандартное обнаружение)
tests/e2e/
  fixtures/
    seed.mjs                 — richSeed / seqSeed (порт из D:\tmp\pw\b1\seed.mjs)
    app.mjs                  — test-фикстура: сид → boot → { page, errors }
  *.e2e.mjs                  — сами кейсы
```

**Имя файлов — `*.e2e.mjs`, НЕ `*.spec.mjs`.** У vitest дефолтный include =
`**/*.{test,spec}.?(c|m)[jt]s?(x)`, и `.spec.mjs` попал бы в `npm test`, где
happy-dom не умеет Playwright. Развязка именем надёжнее, чем `exclude` в
конфиге: её нельзя случайно потерять при правке `vite.config.js`.

**Браузер — системный Chrome** (`channel: 'chrome'`), как во внешнем харнессе.
Ноль скачивания (~130 МБ бандла не нужны), и тестируется тот же движок, что у
юзера. Переопределение — `PW_CHANNEL=chromium npx playwright test`.

**Сервер — `vite preview` над `dist/`**, не dev-сервер: проверять надо то, что
уезжает на Pages (стабильные имена `app.js`/`style.css`, реальный `sw.js`,
`public/` целиком). `webServer.command` = `npm run build && vite preview`.

**Сид — через `addInitScript`** до `goto`: чистит LS, кладёт `duskState_v4`,
сносит `keyval-store`, чтобы IDB-first бут упал на засеянный LS. Ровно та же
механика, что в `b1/lib.mjs` — она проверена сотней прогонов.

**Артефакты:** `trace: 'retain-on-failure'`, `screenshot: 'only-on-failure'`,
`video: 'off'`. Выход — `test-results/` (в `.gitignore`).

## 4. Команды

| Команда | Что делает |
|---|---|
| `npm run e2e` | сборка + прогон всего смоука (Chrome, headless) |
| `npm run e2e:ui` | то же в UI-режиме Playwright (отладка) |
| `npm run e2e:headed` | видимый браузер |

`npm test` (vitest) остаётся отдельным и быстрым — смоук в него не входит.

## 5. Кейсы (31 — итог; план был «~26»)

Один файл = одна поверхность.

**`boot.e2e.mjs` (3)**
1. Приложение поднимается: ноль ошибок консоли, сид отрисован (14 обетов, 3 свода).
2. IDB-first бут: перезагрузка отдаёт то же состояние.
3. **seq-aware бут** — LS `_saveSeq:2` против IDB `_saveSeq:1`: выигрывает LS.
   Это закрытие долга W0 (`FIX-PLAN.md`: зонд `s4_p0_idbboot.mjs` сеял блобы
   БЕЗ `_saveSeq` и потому показывал легаси-путь, а не фикс `4b18b42`).

**`pages.e2e.mjs` (2)**
4. Три нав-таба переключают страницы, активный помечен.
5. Выбранная страница переживает перезагрузку.

**`tasks.e2e.mjs` (5)**
6. Дать обет → строка в списке и запись в состоянии.
7. Исполнить → чек проставлен.
8. Уничтожить → тост с отменой → отмена возвращает обет.
9. Смена ранга через ⋯-меню.
10. Приковать → обет поднимается наверх списка.

**`subtasks.e2e.mjs` (2)**
11. Добавить звено → счётчик звеньев вырос.
12. Все звенья исполнены → обет исполняется автоматически.

**`quickadd.e2e.mjs` (2)**
13. `!высокий` кириллицей ставит ранг (сквозной регресс `V2-B1-11`).
14. `*тег` и `%` дают тег и исход.

**`deadlines.e2e.mjs` (2)**
15. Классы статусов чипа: просрочено / критично / скоро.
16. Отсрочка сдвигает исход.

**`crypt.e2e.mjs` (2)**
17. Предать склепу → пропал из списка, появился в склепе.
18. Воскресить → вернулся со ВСЕМИ полями (регресс C-1: ранг, витраж, звенья).

**`grimoire.e2e.mjs` (2)**
19. Создать запись, набрать текст, уйти и вернуться — текст на месте.
20. Поиск фильтрует; пустой зов даёт полную плиту.

**`persist.e2e.mjs` (1)**
21. Пачка из 8 быстрых правок → перезагрузка → ни одна не откатилась
    (регресс `V2-B6-08` / хвост последовательности).

**`pwa.e2e.mjs` (2)**
22. Service worker регистрируется и активируется.
23. `manifest.json` и `version.json` отдаются и валидны.

**`layout.e2e.mjs` (6 = 2 проверки × 3 вьюпорта, каждая по трём страницам)**
24. Ноль горизонтального скролла и ничего не вылезает за правый край на
    360 / 412 / 1280 (мобильные тиры — с `isMobile`+`hasTouch`, т.е. по
    coarse-ветке разметки).
25. Все `<use>` резолвятся в ненулевой bbox (ловит молчаливо мёртвую иконку).
    ⚠ Фильтровать `checkVisibility()` — у потомка скрытого предка
    `getComputedStyle` отдаёт свой `display` и даёт ложный ноль.
    ⚠ Оба зонда возвращают ЧИСЛО осмотренных узлов, и тест требует его > 0:
    иначе «всё отфильтровалось как невидимое» читается как зелёный результат.
    Страницы переключаются вызовом `switchPage()`, а не кликом: на мобиле нав
    живёт в створке, а предмет теста — геометрия, не навигация.

**`sync-ui.e2e.mjs` (1)**
26. Панель синка открывается, мёртвый пункт `disabled`, Esc закрывает.

## 6. Слайсы исполнения (по коммиту на слайс)

Коммиты тестовые, приложение не меняют → **`version.json` НЕ бампаем**
(бамп — только у деплой-коммитов; это правило `STRATEGY.md`).

- **S1 — каркас.** `@playwright/test`, `playwright.config.mjs`, `fixtures/`,
  npm-скрипты, `.gitignore`, `boot.e2e.mjs` целиком (3 кейса). Гейт: `npm run
  e2e` зелёный, `npm test` по-прежнему 102 и НЕ видит e2e-файлов.
- **S2 — ядро задач.** `pages`, `tasks`, `subtasks`, `quickadd` (11 кейсов).
- **S3 — сроки и хранилища.** `deadlines`, `crypt`, `grimoire` (6 кейсов).
- **S4 — durability.** `persist`, `pwa` (3 кейса).
- **S5 — геометрия и хвост.** `layout`, `sync-ui` (5 кейсов) + правка
  `CLAUDE.md`/`STRATEGY.md` + запись в память.

## 7. Definition of done — ✅ ВЫПОЛНЕНО 2026-07-28

Замер на закрытии: `npm run e2e` **31/31** (~42 с), `npm test` 102/102,
`tsc --noEmit` 27 (базлайн не вырос), `npm run build` зелёный, ратчет иконок
чист (197 глифов).

- `npm run e2e` зелёный локально, 31 кейс.
- `npm test` = 102 (e2e-файлы в него не попали), `tsc --noEmit` ≤ 27 (базлайн),
  `npm run build` зелёный, `node scripts/icon-inventory.mjs` чист.
- Ни один тест не зависит от копирайта, который может измениться партией языка:
  якоря — `data-act`, `id`, роли и классы, а не русский текст. Исключение —
  кейс 13 (кириллица квик-эдда), где текст и есть предмет.
- `test-results/` в `.gitignore`.
- Долг W0 (seq-aware сид) закрыт кейсами 3 и 4.

## 8. Грабли, стоившие времени (переносимые)

1. **`addInitScript` не ждёт промис.** Запись в IndexedDB асинхронна → бут
   успевал прочитать пустую базу. Лечение — сеять ОТДЕЛЬНОЙ навигацией на
   route-заглушку того же origin и только потом идти на `index.html`.
2. **Заглушка обязана нести `<link rel="icon">`.** Любой документ без неё
   заставляет Chrome просить `/favicon.ico`; 404 всплывает в консоли и
   выглядит как баг приложения. Час ушёл на поиск «чужого» 404.
3. **Сетевые события service worker'а на `page` НЕ приходят** — слушать надо
   `page.context()`. Иначе провал прекэша невидим.
4. **`console`-сообщение о провале загрузки не содержит URL** — брать
   `msg.location().url`, иначе «Failed to load resource» ни о чём.
5. **Позиции строк сравнимы только внутри одной секции:** обеты со сводом
   рисуются своим блоком, «выше/ниже» относительно чужой секции бессмысленно.
6. **Зелёный по пустоте.** Любой зонд-обходчик обязан возвращать число
   осмотренных узлов, а тест — проверять его.
